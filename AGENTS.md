# SectorFlow Agent Rules

> **규칙 우선순위**: "실행 우선 카드" > 섹션1 > 섹션2 > 섹션3 > 섹션4. 상위가 하위보다 우선. 완료 보고 봉인 규칙은 완료 후 추가 질문·다음 단계 제안 금지에 관한 모든 하위 문구보다 우선.
>
> **상세 절차는 별도 문서에서 읽어라**: 본 파일은 핵심만. 상세 절차는 아래 "상세 절차 문서 인덱스"의 별도 문서에서 작업 유형에 맞춰 읽어 사용.

---

## ⚡ 빠른 룰 인덱스 + 상세 절차 문서 인덱스

> **작업 전환 시 재점검 (강제)**: 작업 유형이 바뀔 때마다 아래 표에서 상세 문서 재확인.

| 작업 유형 | 읽을 상세 문서 |
|---|---|
| **모든 작업 시작 전** | 본 파일 "실행 우선 카드" + "판정 게이트" + `docs/절차규칙/절차규칙_세션절차_상세.md` |
| **조사** | `docs/절차규칙/절차규칙_조사_상세.md` |
| **사용자 보고** | `docs/절차규칙/절차규칙_보고_상세.md` + `docs/절차규칙/완료보고_템플릿.md` |
| **설계서·태스크·HANDOVER** | `docs/절차규칙/절차규칙_문서포맷_상세.md` |
| **코드 수정·거래 로직·세션 절차·다단계·작업 완료·커밋 전 검토** | `docs/절차규칙/절차규칙_세션절차_상세.md` (+ safe-trade 스킬 for 거래) |
| **코드 작성 시 품질 패턴** | `docs/절차규칙/코드품질가이드.md` (에러 처리·비동기·네이밍·프론트엔드·테스트·DB/WS 표준 패턴) |
| **보안 점검** | security-check 스킬 |
| **독립 검증** | independent-verify 스킬 |
| **컨텍스트 임박** | `docs/절차규칙/절차규칙_컨텍스트관리_상세.md` |

### 문서 역할 원칙 5항목 (최상위 — 모든 문서 작성 전 필수 확인)

> **역할이 깨지면 포맷이 맞아도 의미 없음 — 5항목은 불가침.** 상세 전문은 `docs/절차규칙/절차규칙_문서포맷_상세.md` 0절 참조.
>
> 한 줄 요약: 설계서="왜", 태스크="어떻게", 구현=태스크 따름, 검증=설계 완료기준 따름, 사용자 보고=결과·상태 알리고 필요시에만 결정받기, 핸드오버=다음 세션 인계만.

## ⚡ 실행 우선 카드 (모든 작업 시작·종료 시 최우선)

> 다른 지시와 충돌 시 이 카드 따름. 읽기 전용 조사만 먼저 가능, 변경·외부 영향은 절차 통과 후.
>
> **작업 시작**
> 1. **범위 고정** — 요청한 결과와 이번 세션 한 단계를 한 문장으로 정의. 요청 외 개선 금지.
> 2. **사전 확인** — 현재 상태·관련 자료·영향 범위·기존 공통 기능 읽기 전용 확인.
> 3. **아키텍처 판정** — P1~P25 전체 확인. 하나라도 `❌`/`보류`이면 변경 금지, 사용자에게 알리고 대안만 제시.
> 4. **승인 확인** — 대부분 바로 진행. 실전 모드·DB 삭제·API 키·매매 코드만 사전 승인.
> 5. **한 단계 실행** — 승인된 범위만 작업, 검증 완료.
>
> **작업 종료**
> 6. **완료 판정** — 검증 결과·요청 일치·아키텍처 판정·미해결 위험 확인.
> 7. **종료 상태** — `작업 완료`(다음 제안·질문 금지) / `단계 완료·대기`(다음은 인계 문서에만) / `중단·보류`(원인·결정만 알림).
>
> **종료 순서 (강제)**: 검증 통과 → 저장(코드만) → **완료된 작업 문서 삭제(설계서·태스크·조사보고서 — 전체 작업 완료 시만)** → 인계 문서 갱신(파일만, 삭제 반영) → 완료 보고. 보고 전 저장·삭제·갱신 먼저.
> **강제 중단** — 아키텍처 위반·검증 실패·범위 초과·상태 손상 시 즉시 중단. 위험 작업 4종은 위험 설명 후 승인 대기.

---

## 섹션1. 프로젝트 개요 (최상위)

SectorFlow is a local real-time stock auto-trading web app for one person.

- Backend: FastAPI + Python + aiosqlite (SQLite `backend/data/stocks.db`) | Frontend: TypeScript (Vanilla)
- Pipeline: 장마감 → 5일봉 다운로드 → 거래대금 필터 → 업종 그룹화 → 업종 스크리닝 → 업종 랭킹 → 상위 N업종 → 매수 후보 → 매수(시장가) → 보유목록 → 매도
- Key frontend state: `frontend/src/stores/store.ts`

### 디렉터리 구조 (주요)

- `backend/app/core/` 브로커·설정 | `backend/app/services/` 엔진·매매·리스크·WS (가장 큼)
- `backend/app/pipelines/` 장마감 | `backend/app/web/` 라우트·WS | `backend/data/stocks.db` (삭제 금지)
- `frontend/src/components/common/` 공통 | `frontend/src/stores/` 상태 | `frontend/src/pages/` 페이지 | `docs/` 문서

### 핵심 SSOT 위치 (P10)

| 대상 | 위치 |
|------|------|
| 백엔드 진입점 | `main.py` → `backend/app/web/app.py` |
| 엔진 | `backend/app/services/engine_lifecycle.py`, `engine_loop.py` |
| 설정 | `backend/app/core/settings_store.py` + `settings_defaults.py` (P10) |
| 설정 캐시 | `integrated_system_settings_cache` (P17) |
| 주문 경로 | `backend/app/services/trading.py` `execute_buy()`/`execute_sell()` (P15) |
| 프론트 상태 | `frontend/src/stores/store.ts` (메인), `uiStore.ts` |
| WS 바인딩 | `frontend/src/binding.ts` · 라우터 `frontend/src/router.ts` |

### 검증 명령어 (프로젝트 루트에서 실행)

| 대상 | 명령어 |
|------|--------|
| 백엔드 테스트 | `.venv/bin/python -m pytest backend/tests -q` |
| 백엔드 부분 | `.venv/bin/python -m pytest <변경 파일> -q` |
| 백엔드 린트 | `.venv/bin/ruff check backend/app/` |
| 런타임 | `.venv/bin/python main.py` / `-W error::RuntimeWarning main.py` |
| 프론트엔드 | `cd frontend && npm run typecheck` / `lint` / `build` / `test` |

> `mypy`는 사전 에러로 표준 게이트 아님. 부분 테스트는 빠른 회수용, 전체는 완료 전 1회 필수.

#### 검증 게이트 원칙

> **완료 판정은 기계적 검증 결과로만** — 자기 평가 금지. "기계적 검증"= 위 명령어 통과.
> **완료 보고 전 스크립트**: `scripts/pre-complete-check.sh` 실행 — 실패 시 완료 보고 금지. 모드별 실행:
> - `auto` — **권장·Stop 훅 기본값**. 세션 시작 스냅샷과 비교하여 이번 세션에서 새로 바뀐 파일만 보고 알맞은 모드 자동 판단. 백엔드 수정 시 런타임 기동 검증까지 자동 진입, 프론트엔드 수정 시 frontend 모드, 코드 변경 없으면 docs 모드. 이전 세션 잔재는 이번 세션 수정으로 잘못 인식되지 않음.
> - `docs` — 문서 전용 세션 (코드 검증 스킵, 백엔드 프로세스 절대 건드리지 않음). 단, 작업 창에 코드 변경이 감지되면 거부(실패) — 이전 세션 잔재든 이번 세션 수정이든 코드 변경이 있으면 `backend`/`frontend`/`all` 모드로 실행해야 함.
> - `frontend` — 프론트엔드 수정 시 (백엔드 프로세스 건드리지 않음)
> - `backend` — **이번 세션에서 백엔드를 수정했을 때만** (런타임 기동 검증을 위해 백엔드 프로세스 종료·재기동)
> - `all` — 백엔드 린트/테스트 + 프론트엔드 검증 (수동 지정, 백엔드 프로세스 건드리지 않음)
> **핵심 원칙**: 백엔드 프로세스 종료·재기동은 `backend` 모드(또는 `auto`가 backend로 판정한 경우)에서만. 이전 세션 잔재가 작업 창에 남아 있어도, 이번 세션에서 백엔드를 수정하지 않았으면 백엔드 프로세스를 절대 종료하지 않는다. `auto` 모드는 세션 시작 스냅샷으로 이를 자동 보장.
> **3계층 검증**: 기계적 + 요청 의도 사후 확인 + 독립 검증자(거래·핵심 로직 시 필수).
> **2단계**: 부분 검증(변경 파일만) → 전체 검증(완료 전 1회 필수). 실패 시 다음 단계 금지.
> **상세**: `docs/절차규칙/절차규칙_세션절차_상세.md` "작업 완료 체크리스트" 1단계.

### 실전 모드 vs 테스트 모드 (핵심 역할 차이)

> 본 테이블이 역할 정의 SSOT. `safe-trade/SKILL.md`·`ai새채팅창용.txt`는 본 테이블 참조.

| 구분 | 실전 모드 | 테스트 모드 |
|---|---|---|
| 매매 판단 | 앱 계산 (동일) | 앱 계산 (동일) |
| 매매 주문 | 실제 증권사 전송 | 가상 주문 |
| 돈 수치 | **증권사가 SSOT** | **가상 시뮬레이터가 SSOT** |
| 파생 지표 공식 | **공통 공식** (원재료만 증권사값) | **공통 공식** (원재료만 가상값) |
| 시뮬레이터 응답 | — | 실전과 동일 구조 (W7) |

> **정제 핵심**: 공식은 테스트/실전 동일, 원재료만 출처 다름. 확정 체결/잔고는 증권사 응답 그대로 저장, 파생 지표 공식만 공통. **실전 전환 전 보완 갭**: `docs/설계서/실전모드_불일치_조정.md` 참조.

### 사용자 프로필 (필수 전제)

- 사용자는 UI로만 조작. 에이전트가 기술 작업을 전담, 일반 사용자 용어로 설명 (상세는 "사용자 의사소통 규칙" 및 `docs/절차규칙/절차규칙_보고_상세.md`).

---

## 섹션2. 아키텍처 원칙 (불변 원칙)

> `ARCHITECTURE.md`의 25개 불변 원칙(P1~P25)과 안전 규칙 포함. 코드 변경 시 반드시 준수.

### 안전 규칙 (항상 적용)

1. 모든 `*.db` 파일 직접 삭제·덮어쓰기 금지.
2. DB 스키마 변경 전 `stocks.db`·`-shm`·`-wal` 백업 생성.
3. 사용자가 실제 돈 사용을 명시적으로 확인하기 전까지 매매 코드는 테스트 모드 유지. **"명시적 확인"= 3가지 인지 후 승인**: (a) 실제 증권사 계좌 자금 사용 (b) 자동 매매 손실 가능 (c) 실제 주문이 증권사 서버로 전송.
4. 증권사 API 키·토큰 하드코딩 금지.
5. 사용자 승인 없이 `sudo`·`rm -rf`·`curl`·`wget`·`sqlite3` 실행 금지.
6. `~/.bashrc`·`~/.zshrc`·시스템 경로 수정 금지.

### 아키텍처 원칙 참조

- `ARCHITECTURE.md` 기준: W1~W12·D1~D17. P1~P25는 부록 매핑표의 감사용 식별자.
- 판정 근거는 연결된 W/D에서 가져옴. 매핑되지 않은 번호 추측 금지.
- 주요 원칙: P10(SSOT)·P16(살아있는 경로)·P20(폴백 금지)·P21(사용자 투명성)·P22(데이터 정합성)·P23(일관성)·P24(단순성)·P25(격리된 실패). 상세는 `ARCHITECTURE.md` 제1부 참조.

### ARCHITECTURE.md 주요 섹션 맵 (빠른 참조)

- 통합 원칙·P1~P25 매핑: 제1부 + 부록 A | 금지 패턴 5개: W1·W6·W8 (부록 A)
- 프론트엔드 페이지: 17.1 | 용어 사전: 부록 B | 부호 규칙: 부록 B.2

### 작업 시작 전 아키텍처 판정 게이트 (모든 작업·강제)

> 코드·설계서·태스크·규칙·설정·조사 결과 수정에도 적용. 읽기 전용 확인은 허용, 변경은 판정 통과 후. P1~P25는 W1~W12·D1~D17 매핑의 감사용 번호.
>
> **판정 절차**
> 1. 요청 한 문장 요약, 범위 고정.
> 2. P1~P25를 모두 `✅`/`❌`/`보류`로 판정. `해당 없음` 생략 금지.
> 3. 하나라도 `❌`/`보류`이면 즉시 중단. 위반 사유·영향 알리고 대안만 제시. 위반 감수 금지.
> 4. 모두 `✅`면 실행. 위험 작업(실전 모드·DB 삭제·API 키·매매 코드)만 사전 승인.
> 5. 작업 중 범위 변경 시 재판정. 새 `❌`/`보류` 시 즉시 중단.
> 6. 완료 보고에 P1~P25 전체 판정+근거 포함. 시작·완료 판정 다르면 완료 보고 금지.
>
> **P1~P25 전체 판정 표·기록·위반 시 알림 형식**: `docs/절차규칙/절차규칙_세션절차_상세.md` "판정 게이트 — P1~P25 전체 표" 참조.
> **코드 수정 체크리스트·작업 완료 체크리스트·코드 제거 규칙·문서 수정 절차**: `docs/절차규칙/절차규칙_세션절차_상세.md` 참조.

---

## 섹션3. 수행 규칙 (모든 작업 시 준수)

### 규칙 등급 (핵심 vs 부수)

> 충돌 시 **핵심이 부수보다 우선**.

**핵심 (절대 불가침 — 6개)**:
1. **승인 전 수정 금지** — 명시적 승인 없이 코드 수정 절대 금지.
2. **일반 사용자 용어** — 모든 응답은 일반 사용자 용어. 기술 참조 + 메타 용어 금지.
3. **완료 판정은 기계적 검증 결과로만** — 자기 평가 금지.
4. **안전 규칙** — DB 삭제 금지, 실전 모드 전환 3조건 승인, 증권사 키 금지.
5. **작업 시작 전 아키텍처 판정** — P1~P25 중 하나라도 위반·보류이면 중단.
6. **핸드오버는 인덱스 역할만** — 상세 인라인 기록 금지.

**부수**: 위 6개 제외 나머지. "유연"은 "무시 가능"이 아니라 "상황에 따라 형태 조정 가능".

> **조사 및 문제 해결 규칙 (규칙 0~0-10)**: `docs/절차규칙/절차규칙_조사_상세.md` 참조. 핵심: 승인 전 수정 금지, 사전조사 4항목, 가정 선언, 수정 전 의도 파악, 롤백 금지, 핵심 로직 변경 보고, 자동 커밋+핸드오버 갱신, 완료 보고 명시, 미해결 문제 기록, 수치 변경 드리프트 방지.

### 사용자 의사소통 규칙 (필수)

사용자는 UI로만 조작하므로 모든 소통은 아래 원칙 준수:

1. **UI 기준 검증 안내**: 확인/검증 요청 시 항상 UI 조작으로 안내. 터미널 요청 금지.
2. **응답 전송 직전 점검 게이트 (강제 — 규칙 0 동급)**: 응답 전송 직전 점검. 위반 시 일반 사용자 용어로 재작성.
   - **(a) 코드 기술 참조**: 파일 경로·줄번호·함수명·코드 인용 태그·명령어 포함 여부
   - **(b) 규칙·원칙 메타 용어**: 규칙 번호·원칙 번호·메타 명칭 포함 여부. 원칙 언급 시 "원칙에 부합합니다" 정도로만.
   - **예외**: 사용자가 명시적으로 기술 세부사항 요구 시 통과.
   - **강제 등급**: 규칙 0과 동일 최우선. **모든 사용자 응답**에 적용.

> **사용자 보고서 표준 형식 + 완료 보고 템플릿**: `docs/절차규칙/절차규칙_보고_상세.md` 참조. 완료 보고 시 `docs/절차규칙/완료보고_템플릿.md` 복사 사용. **세션 종료 계열 보고(완료·단계 완료·중단·보류)는 표 형식 강제** — 이모지 요약 형식은 1줄 질답·진행 중 간단 알림에만 허용. **다단계 작업(태스크 파일 있는 작업) 보고 제목은 `전체 N단계 중 N단계 시작/완료` 형식 강제** — 단계 변경 시 태스크 파일 먼저 갱신 후 보고에 반영. **단, 마지막 단계(전체 완료) 시에는 태스크 파일 갱신 생략** — 갱신 직후 삭제되므로 무의미 (P24 단순성).

### 금지 표현 (사용자 응답 본문 노출 금지)

- 파일 경로·줄번호·함수명·변수명·코드 인용 태그·스택 트레이스·명령어
- 규칙 번호·원칙 번호·메타 명칭 ("단일 진실 소스", "역할 원칙", "다단계 워크플로우" 등)
- SQL·커밋 해시·내부 상태값·터미널 명령어·API 직접 호출 안내

### 허용 표현

- "매매 대상 종목만 남기고 나머지는 자동 정리" (O) — 파일 경로+DELETE (X)
- "원칙에 부합합니다" (O) — 원칙 번호+위반 (X)
- "화면에서 매수 후보 목록이 N개로 표시되는지 확인해 주세요" (O) — 터미널 명령 (X)

---

## 섹션4. 작업 프로세스 (워크플로우)

> **세션 시작·종료 절차·다단계 워크플로우·검증 계층 게이트**: `docs/절차규칙/절차규칙_세션절차_상세.md` 참조. 핵심: 세션 시작 시 HANDOVER.md 자동 확인, 승인 상태별 분기, 종료 시 잔존 프로세스 0건 확인.
> **문서 표준 포맷 (역할 기반) 전문**: `docs/절차규칙/절차규칙_문서포맷_상세.md` 참조. 0절 "문서 역할 원칙" 5항목은 절대 불가침.

### 문서 저장 경로 규칙 (P10 SSOT · P23 일관성)

모든 문서는 `docs/` 한 곳에서 통합 관리. 코드 디렉터리 하위 문서 폴더 신규 생성 금지. **폴더명은 한글** — 사용자가 폴더명만 봐도 내용 파악 가능 (P23 일관성).

**작업 산출물 (전체 작업 완료 시 삭제 — 종료 순서 3단계, 일부 단계만 완료 시 유지)**:
- 설계서 `docs/설계서/*.md` — "왜 바꾸는가"
- 태스크 `docs/태스크/*.md` — "어떻게 바꾸는가"
- 조사 보고서 `docs/조사보고서/*.md` — 조사 결과

**영구 문서 (유지 — SSOT·참고용)**:
- 절차 규칙 `docs/절차규칙/*.md` — 작업 절차 규칙 상세, 완료 보고 템플릿, 코드 품질 가이드
- 참고 자료 `docs/참고자료/*.md` — 참고 자료, 사례 조사, 거래시간표 등
- API 스펙 `docs/api_specs/` — 증권사 API 명세

**문서 생성 시 폴더 선택 기준**: 문서의 역할(위 표)에 따라 폴더가 기계적으로 결정됨. 역할이 모호하면 역할 원칙 5항목(상단) 재확인. 문서 이동 시 모든 참조 경로 갱신 필수.

### 스킬 자동 호출

사용자 요청에 맞는 스킬을 자동 호출. 확실하지 않으면 `/problem-solve` 먼저. 미호출 시에도 본 파일 규칙 따름.

- **버그·문제 분석**: `/problem-solve` | **백엔드**: `/backend-fix` | **프론트엔드**: `/frontend-fix` — **코드 작성 시 `docs/절차규칙/코드품질가이드.md` 표준 패턴 준수**
- **DB 스키마 변경**: 먼저 `/db-backup` | **매매 로직**: 먼저 `/safe-trade`
- **신규 로직**: 계획 `/problem-solve`, 구현 `/backend-fix` 또는 `/frontend-fix`
- **설계서·태스크 작성**: `/problem-solve` + `docs/절차규칙/절차규칙_문서포맷_상세.md` 재독
- **대규모 전수 조사**: `/continuity-investigation` | **커밋 전 검토**: `/pre-commit-review`
- **보안 점검**: `/security-check` | **독립 검증**: `/independent-verify` (위험도 높음 시 필수)

> **컨텍스트 관리 규칙**: `docs/절차규칙/절차규칙_컨텍스트관리_상세.md` 참조. 핵심: 작업량 기반 사전 분할(수정 파일 3개 초과 또는 50줄 초과 시), 단계 완료 시 컨텍스트 점검, HANDOVER.md 기록 의무(인덱스 원칙), read-before-write, 작업 중 발견 문제 기록.
