# SectorFlow Agent Rules

> **규칙 우선순위**: 섹션1(프로젝트 개요) > 섹션2(아키텍처 원칙) > 섹션3(수행 규칙) > 섹션4(작업 프로세스).
> 상위 섹션 규칙이 하위 섹션 규칙보다 우선. 충돌 시 상위 섹션 기준으로 판단.

---

## 섹션1. 프로젝트 개요 (최상위)

SectorFlow is a local real-time stock auto-trading web app for one person.

- Backend: FastAPI + Python + aiosqlite (SQLite `backend/data/stocks.db`)
- Frontend: TypeScript (Vanilla)
- Pipeline: After market close → download 5-day bars → filter by trading amount → group by custom sector → sector screening → sector ranking → top N sectors → buy candidates → buy (market order) → hold list → sell
- Key frontend state: `frontend/src/stores/store.ts`

### 디렉터리 구조 (주요)

- `backend/app/core/` — 브로커 연결·설정·상수·로깅 (`broker_factory.py`/`broker_registry.py` 레지스트리, `settings_store.py`, `constants.py`)
- `backend/app/services/` — 엔진·매매·리스크·WS 디스패치 (가장 큼, 40+ 파일)
- `backend/app/pipelines/` — 장마감 파이프라인
- `backend/app/web/` — FastAPI 라우트·WS 매니저
- `backend/data/stocks.db` — SQLite DB (삭제 금지, 안전 규칙 1)
- `frontend/src/components/common/` — 공통 컴포넌트 (P23 재사용 1순위, 29파일)
- `frontend/src/stores/` — 상태 관리 (`store.ts` 메인, `uiStore.ts` UI 상태)
- `frontend/src/pages/` — 페이지 진입점
- `docs/` — 설계·태스크·조사 문서

### 핵심 SSOT 위치 (P10)

| 대상 | 위치 | 관련 원칙 |
|------|------|-----------|
| 백엔드 진입점 | `main.py` → `backend/app/web/app.py` | — |
| 엔진 진입점 | `backend/app/services/engine_lifecycle.py`, `engine_loop.py` | — |
| 설정 저장소 | `backend/app/core/settings_store.py` + `settings_defaults.py` | P10 |
| 설정 메모리 캐시 | `integrated_system_settings_cache` | P17 |
| 주문 단일 경로 | `backend/app/services/trading.py` `execute_buy()`/`execute_sell()` | P15 |
| 프론트엔드 상태 | `frontend/src/stores/store.ts` (메인), `uiStore.ts` (UI) | — |
| WS 바인딩 | `frontend/src/binding.ts` | — |
| 프론트엔드 라우터 | `frontend/src/router.ts` | — |

### 검증 명령어 (프로젝트 루트에서 실행)

| 대상 | 명령어 | 비고 |
|------|--------|------|
| 백엔드 테스트 | `.venv/bin/python -m pytest backend/tests -q` | 2697 tests, asyncio_mode=auto |
| 백엔드 런타임 기동 | `.venv/bin/python main.py` | 0-1-3 명령어로 잔존 프로세스 0건 확인 |
| RuntimeWarning 검증 | `.venv/bin/python -W error::RuntimeWarning main.py` | await 누락 검증 (금지 패턴 4번째) |
| 프론트엔드 타입체크 | `cd frontend && npm run typecheck` | `tsc --noEmit` |
| 프론트엔드 빌드 | `cd frontend && npm run build` | `tsc -b && vite build` |
| 프론트엔드 테스트 | `cd frontend && npm run test` | vitest, 116 tests |

> `mypy`는 사전 에러 존재로 표준 검증 게이트 아님 (사용 시 에이전트가 기존 실패 추적 비용 발생).

### 실전 모드 vs 테스트 모드 (핵심 역할 차이)

> 본 프로젝트는 **실전 모드**와 **테스트 모드** 두 가지 운영 모드를 가집니다. 이 역할 차이는 모든 설계·수정·질문의 전제입니다.
> **본 테이블이 역할 정의의 단일 진실 소스(SSOT)입니다.** `safe-trade/SKILL.md`와 `ai새채팅창용.txt`는 본 테이블을 참조·요약합니다.

| 구분 | 실전 모드 | 테스트 모드 |
|---|---|---|
| 데이터·계산 책임 | 증권사 서버가 담당. 앱은 증권사에서 받은 값을 그대로 수신·표시 | 가상 데이터로 앱이 직접 계산 |
| 매매 주문 | 실제 증권사 서버(또는 모의투자)로 전송 | `dry_run.fake_send_order()` 등 가상 주문 |
| 수익·수수료·세금 | 증권사 데이터를 SSOT로 사용. 앱에서 재계산 금지 | 앱 내부 로직으로 계산 |
| 업종 순위·필터링·타이밍 | 테스트모드와 동일한 로직 경로 사용. (돈 I/O와 데이터·계산 SSOT만 다름) | 가상 데이터로 동일 로직 실행 |

### 사용자 프로필 (필수 전제)

- 사용자는 UI로만 조작. 에이전트가 기술 작업을 전담하고, 일반 사용자 용어로 쉽게 설명 (상세는 규칙 0-8).

---

## 섹션2. 아키텍처 원칙 (불변 원칙)

> 본 섹션은 `ARCHITECTURE.md`에 정의된 25개 불변 원칙(P1~P25)과 안전 규칙을 포함. 코드 변경 시 반드시 준수.

### 안전 규칙 (항상 적용)

1. `backend/data/stocks.db`를 포함한 모든 `*.db` 파일을 직접 삭제하거나 덮어쓰지 않는다.
2. DB 스키마 변경이나 마이그레이션 전, `stocks.db`, `stocks.db-shm`, `stocks.db-wal`의 타임스탬프 백업을 생성한다.
3. 사용자가 실제 돈을 사용하겠다고 명시적으로 확인하고 경고를 받기 전까지 매매 코드는 시뮬레이션/테스트 모드로 유지한다.
4. 증권사 API 키, 토큰, 자격 증명을 하드코딩하지 않는다.
5. 사용자 승인 없이 `sudo`, `rm -rf`, `curl`, `wget`, `sqlite3`를 실행하지 않는다.
6. `~/.bashrc`, `~/.zshrc`, 시스템 경로를 수정하지 않는다.

### 아키텍처 원칙 참조

- 코드 변경 시 `ARCHITECTURE.md`의 25개 불변 원칙(P1~P25)을 준수. 주요 원칙:
  - **P10 (SSOT)**: 같은 데이터가 여러 곳에서 독립 관리되지 않도록 단일 진실 소스 유지.
  - **P16 (구현 = 살아있는 경로 배선)**: 호출되지 않는 안전코드/dead code 금지.
  - **P20 (폴백 금지)**: 정상 경로의 빈 문자열/None/누락을 폴백으로 덮지 않음, silent `except: pass` 금지.
  - **P21 (사용자 투명성)**: 사용자 모르는 중요 의사결정 금지, 백엔드 상태의 UI 표시 의무.
  - **P22 (데이터 정합성)**: 파생 데이터 모델, 기동 시 대조(reconciliation), 불일치 시 즉시 차단.
  - **P23 (일관된 통일성)**: 용어 사전 준수, 에러/비동기/네이밍/상수 패턴 파일 간 일관, UI 패턴 공통 컴포넌트 추출.
  - **P24 (단순성)**: 중복 제거 우선(줄 수보다 중복이 근본 단순성 위반), 더 단순한 대체 가능성, 불필요한 추상화 금지, 함수/파일 길이·복잡도 기준.
  - **P25 (격리된 실패)**: 한 구성요소의 실패가 전체 시스템 기동/운영을 블로킹하지 않아야 함. 실패는 해당 구성요소에서 차단+로깅, 다른 구성요소는 정상 작동 유지.
- 전체 원칙 목록은 `ARCHITECTURE.md` 제1부 "불변 원칙 25개" 참조.

### ARCHITECTURE.md 주요 섹션 맵 (빠른 참조)

| 참조 대상 | ARCHITECTURE.md 위치 |
|---------|---------------------|
| 불변 원칙 25개 (P1~P25) | 제1부 "불변 원칙 25개" (18번 줄) |
| 금지 패턴 5개 | 제1부 "금지 패턴 (확정)" (267번 줄) |
| 프론트엔드 페이지 구조 | 17.1 페이지 구조 (1191번 줄) |
| 표준 용어 사전 | 부록 M "표준 용어 사전" (1868번 줄) |
| 후안 B 부호 규칙 | 부록 M.2 "후안 B 부호 규칙" (1893번 줄) |

### 코드 수정 시 점검 체크리스트 (수정 전/중/후 필수)

> 아래 체크리스트는 25개 원칙 중 스킬 파일에 개별 명시되지 않은 원칙들을 실행 가능한 점검 항목으로 구체화. 모든 코드 수정 시 수정 전 조사(규칙 0-2) 단계에서 확인.

#### 백엔드 수정 시 필수 점검
- [ ] **P1-P3 (async 일관성)**: 모든 I/O는 `async def`, 동기 함수(`requests`, `sqlite3`, `time.sleep`, `threading.Lock`) 금지, `run_in_executor()` 우회 금지
- [ ] **P4 (증권사명 침투 금지)**: 공통 로직에 `kiwoom_`/`ls_` 접두사가 없는가? 증권사별 코드는 `core/kiwoom_*.py`·`core/ls_*.py`에 분리 허용(레지스트리 패턴, `broker_factory.py`/`broker_registry.py` 경유), 공통 로직 침투 금지.
- [ ] **P5 (현재 단일 프로세스에서는 EventBus 미사용)**: 현재 구조에서는 Redis/Pub-Sub/분산 메시지 브로커/콜백 리스트 옵서버 패턴을 도입하지 않았는가? 직접 호출 체인을 유지했는가? (프로세스 분리나 다중 소비자가 필요해질 경우 재검토 대상 — ARCHITECTURE.md "현재 구조 선택")
- [ ] **P7 (블로킹 금지)**: 틱 핸들러에 per-tick O(n) 연산, 매 틱 DB 조회, 매 틱 전체 리스트 순회가 없는가?
- [ ] **P11 (폴링 금지)**: `while + sleep` 폴링을 도입하지 않았는가? `asyncio.Queue` + `asyncio.wait()` 이벤트 기반인가?
- [ ] **P12 (DB 연결)**: 매 요청마다 `connect()`를 호출하지 않았는가? 싱글톤 연결을 유지했는가?
- [ ] **P13 (설정 메모리 상주)**: 틱 연산 단계에서 DB 설정 조회를 하지 않았는가? 메모리 캐시를 참조했는가?
- [ ] **P14 (멀티스레드 금지)**: `threading.Thread()` 신규 생성이 없는가? `asyncio.create_task()` 무분별 분리가 없는가?
- [ ] **P15 (단일 주문 경로)**: 주문 로직이 `execute_buy()`/`execute_sell()` 단일 경로인가? 분기/우회 경로가 없는가? (거래 로직 수정 시 safe-trade 스킬 필수)
- [ ] **P16 (살아있는 경로)**: 작성한 안전장치가 실제 실행 경로에 연결되었는가? 호출되지 않는 dead code가 아닌가?
- [ ] **P17 (플래그 단일 소스)**: 플래그(`auto_buy_on` 등)를 여러 곳에서 직접 수정하지 않았는가? `integrated_system_settings_cache`에서만 관리되는가?
- [ ] **P20 (폴백 금지)**: 정상 경로의 빈 문자열/None/누락을 폴백으로 덮지 않았는가? silent `except: pass`가 없는가?
- [ ] **P22 (데이터 정합성)**: 파생 데이터를 중복 저장하지 않았는가? 원본에서 파생하는 모델인가? 불일치 시 즉시 차단하는가?
- [ ] **P23 (용어 통일)**: 로그/문서에 "섹터" 대신 "업종", "주식" 대신 "종목"을 사용했는가? (`ARCHITECTURE.md` 부록 M 참조)
- [ ] **P23 (로그/용어 생성 시 기존 용어 사전 확인)**: 신규 로그·상태명·용어 생성 시, 동일 동작의 기존 표현을 코드베이스에서 먼저 검색하여 일치시켰는가? 동일 동작에 서로 다른 표현(예: "준비" vs "생성")이 공존하지 않는가? (`ARCHITECTURE.md` 부록 M 표준 용어 사전 + 기존 표현 이중 확인)
- [ ] **P23 (공통 자산 재사용)**: 신규 함수/상수/패턴 구현 전, 기존 공통 자산(`core/constants.py`, 공통 유틸, 기존 함수 등)을 먼저 검색했는가? 같은 기능을 새로 만들지 않았는가? (섹션3 규칙 0-2.4)
- [ ] **P23 (네이밍 일관성)**: 새 함수/파일/이벤트명 생성 시 `ARCHITECTURE.md` P23 네이밍 규칙(Python `snake_case` / TS `camelCase`·`PascalCase` / 파일명 `kebab-case.ts` / WS 이벤트 `kebab-case`) 먼저 확인했는가? `_`/`-` 혼용 없는가?
- [ ] **P24 (단순성)**: 동일 로직 중복이 없는가? (중복 제거 우선 — 줄 수 단축보다 중복 제거를 먼저 검토, P10/P23과 교차 강화) 함수/파일 길이가 50/500줄을 초과하면 책임 분리를 검토했는가? (50/500줄은 분할 검토 참고 기준, 자동 위반 아님) 순환 복잡도 10 이하? 불필요한 추상화/1회용 래퍼가 없는가?
- [ ] **P25 (격리된 실패)**: 한 태스크/코루틴 실패가 루프 전체를 중단하지 않는가? `schedule_engine_task()` 사용? 격리 시 에러 로깅(silent pass 금지)?

#### 프론트엔드 수정 시 필수 점검
- [ ] **P21 (사용자 투명성)**: 백엔드 상태 변화(매수 차단, 리스크 초과 등)를 UI에 표시했는가? 사용자가 "왜 이 종목이 매수되지 않았지?"라고 의문을 갖지 않도록 했는가?
- [ ] **P23 (용어 통일)**: UI 텍스트에 "섹터" 대신 "업종", "주식" 대신 "종목", "바이 리스트" 대신 "매수 후보"를 사용했는가? (`ARCHITECTURE.md` 부록 M 참조)
- [ ] **P23 (UI 텍스트/주석 용어 생성 시 기존 용어 사전 확인)**: 신규 UI 텍스트·주석·상태 표시 생성 시, 동일 개념의 기존 UI 표현을 먼저 검색하여 일치시켰는가? 동일 개념에 서로 다른 표현이 공존하지 않는가? (`ARCHITECTURE.md` 부록 M 표준 용어 사전 + 기존 UI 텍스트 이중 확인)
- [ ] **P23 (공통 자산 재사용)**: 신규 컴포넌트/색상/패턴 구현 전, `components/common/` 및 기존 공통 함수/표준 색상을 먼저 검색했는가? 같은 기능을 새로 만들지 않았는가? (섹션3 규칙 0-2.4)
- [ ] **P23 (네이밍 일관성)**: 새 함수/파일/이벤트명 생성 시 `ARCHITECTURE.md` P23 네이밍 규칙(TS `camelCase`·`PascalCase` / 파일명 `kebab-case.ts` / WS 이벤트 `kebab-case`) 먼저 확인했는가? `_`/`-` 혼용 없는가?
- [ ] **P23 (UI 패턴 일관성)**: 동일한 UI 패턴이 2회 이상 반복 시 공통 컴포넌트로 추출했는가?
- [ ] **P25 (격리된 실패)**: 개별 칩/컴포넌트 렌더링 실패가 전체 화면을 중단하지 않는가? store listener 루프에 전파 차단(try/catch + 로깅)이 있는가? 격리 시 에러 로깅(silent pass 금지)?

#### ARCHITECTURE.md 금지 패턴 5개 (수정 후 필수 확인)
- [ ] `asyncio.run()` 사용 금지 → `async def` + `await` 직접 호출
- [ ] `create_task` 무분별 분리 금지 → `schedule_engine_task()` 사용 (add_done_callback 포함)
- [ ] `except Exception: pass` 금지 → `logger.warning(..., exc_info=True)`
- [ ] async 함수 `await` 누락 금지 → `python -W error::RuntimeWarning main.py`로 검증
- [ ] dead code 방치 금지 → 호출되지 않는 함수 삭제 또는 명시적 `# DEPRECATED` 표시

### 코드 제거 규칙

함수, 변수, 클래스, 모듈 등 코드를 제거할 때 다음을 반드시 함께 수행:

1. **참조 주석 정리**: 제거된 코드를 참조하는 모든 주석, docstring, 파일 헤더 설명을 함께 수정 또는 제거.
2. **불일치 금지**: 주석과 코드의 불일치는 원칙10(SSOT) 및 원칙21(사용자 투명성) 위반. 제거된 함수가 docstring에 남아 있으면 신규 코드에서 해당 함수를 찾는 오류 발생.
3. **검색 범위**: 제거된 코드의 이름으로 전체 코드베이스(backend + frontend + tests + docs)를 검색하여 잔존 참조 확인. 단, 완료된 감사·조사 문서 내 역사적 로그는 해당 문서가 규칙 10-(1)에 따라 삭제될 때 함께 정리되므로, 삭제 전까지는 잔존 참조로 간주하지 않음.
4. **테스트 파일 포함**: 테스트 파일의 docstring, 헤더 주석도 코드와 일치해야 함.

---

## 섹션3. 수행 규칙 (모든 작업 시 준수)

### 조사 및 문제 해결 규칙

0. **승인 전 코드 수정 절대 금지 (최우선 규칙).** 어떤 작업이든 — 디버깅, 새 구현, 개선, 리팩토링, 1줄 변경 포함 — 사용자의 명시적 승인 없이는 절대 코드를 수정하지 않는다. 분석, 조사, 계획 제안, 추천까지만 수행하고 승인 전까지 수정 금지.
   - **승인 트리거**: 사용자가 "진행해", "수정해", "구현해", "go", "적용해" 등 **실행 동사 + 해/어** 형태의 명시적 실행 지시어를 사용한 경우만 승인으로 간주. 단독 "해줘"는 승인 트리거에서 제외 — 동사 없는 "해줘"는 맥락상 분석/조사 요청일 수 있어 오분류 위험.
   - **미승인 상황 (수정 금지)**: 사용자가 "분석해", "분석해줘", "조사해", "조사해줘", "확인해", "확인해줘", "검토해", "검토해줘", "의견 줘", "추천해", "추천해줘", "어떻게 생각해", "계획 보여줘" 등을 요청한 경우. 이 경우 분석/조사/제안까지만 하고 응답 종료. 사용자가 추가로 실행 지시어를 줄 때까지 대기.
   - **추천 요청 시 주의**: 사용자가 "추천해줘", "너의 추천은?" 등을 물을 때, 추천을 제시한 후 사용자의 명시적 승인 없이 자동으로 수정에 들어가는 것은 절대 금지. 추천 제시 → 사용자 승인 → 수정 순서 엄수.
   - **Plan 모드가 아닐 때도 동일**: Normal 모드라도 승인 전 수정 금지. 모드와 무관하게 항상 적용.
0-1. **세션당 1단계 원칙 (강제).** 각 세션은 1단계(Step)만 진행. 한 세션에서 여러 단계를 연속 진행 금지.
   - 한 단계 내에서는 한 파일 또는 한 블록씩 수정하고 검증한다.
   - 검증 이상 없으면 커밋 + `HANDOVER.md` 갱신 + 사용자 보고 후 세션 종료. 검증 실패 시 다음 세션으로 넘기지 않고 해당 세션에서 원인 해결.
   - 다음 단계는 다음 세션에서 `HANDOVER.md` 기반으로 이어서 진행 (섹션4 컨텍스트 관리 규칙 6 참조).
   - 작업량이 많은 경우 사전 분할 후 사용자에게 분할 계획 보고 (섹션4 컨텍스트 관리 규칙 1과 연계).
   - **다단계 작업 적용**: 본 규칙은 구현 단계뿐 아니라 설계·태스크 작성 단계에도 동일 적용. 다단계 워크플로우(설계→태스크→구현)는 섹션4 "다단계 작업 워크플로우" 참조.

0-1-1. **요청 범위 외 구현 금지 (강제).** 사용자가 명시적으로 요청하지 않은 기능·리팩토링·개선·정리는 본 작업에 포함시키지 않음. 발견한 아키텍처 위반·개선점·잠재 버그는 코드에 반영하지 말고 `HANDOVER.md` "미해결 문제" 섹션에만 기록(규칙 9). 예외: (a) 수정한 코드와 직접 의존하여 같이 바꾸지 않으면 빌드/테스트가 깨지는 필수 연쇄 수정, (b) 사용자가 명시적으로 승인한 추가 범위. P24·P21 부합.

0-1-2. **표준 검증 절차 (강제).** 모든 검증은 `.venv` 가상환경을 기준으로 실행. pyenv 글로벌 환경을 직접 사용하지 않음 (P10 SSOT · P22 정합성 — 실행 환경과 검증 환경 단일화).
   - **테스트 실패 추적 의무**: 테스트 실행 시 실패가 발생하면 "내 수정과 무관한 기존 실패"라고 단정하지 말고, 반드시 아래 절차를 수행:
      1) **연관성 조사**: 실패한 테스트가 수정한 코드/함수/모듈/설정 키를 직접 또는 간접적으로 참조하는지 확인.
      2) **수정 전 상태 비교**: `git stash`로 수정을 임시 되돌린 후 동일 테스트를 실행하여 수정 전에도 실패하는지 확인. 수정 전에도 실패하면 기존 실패로 판정, 수정 후에만 실패하면 내 수정이 원인.
      3) **기존 실패로 판정된 경우**: 사용자 보고에 "기존 실패 N건 (수정 전 동일 실패 확인)"으로 명시하고, `HANDOVER.md` "미해결 문제" 섹션에 기록.
      4) **내 수정이 원인인 경우**: 즉시 원인 추적 및 수정. "기존 실패"로 치부하고 넘어가는 것은 절대 금지.
      - 테스트 실패는 무언가 잘못됐다는 신호. 무시하지 말고 반드시 추적하는 것이 원칙.
   - **표준 검증 명령**:
      - 백엔드 테스트: `.venv/bin/python -m pytest backend/tests -q` (전체) 또는 `.venv/bin/python -m pytest backend/tests/<파일> -q` (개별)
      - 백엔드 타입체크: `.venv/bin/python -m mypy backend` (mypy.ini 설정 기준)
      - 백엔드 런타임 기동: `.venv/bin/python main.py` (10~30초 대기 후 종료, 잔존 프로세스 0건 확인)
      - 커버리지: `.venv/bin/python -m pytest backend/tests --cov=backend --cov-report=term-missing`
      - 의존성 재설치 시: `.venv/bin/python -m pip install -r requirements.txt`
   - **비실패 경고 등 미해결 문제 등록**: 테스트 실행 시 실패가 아니더라도, RuntimeWarning·coroutine 미-await·비동기 누수 등 원칙 19(런타임 검증 게이트)에 해당하는 경고가 발생하면 `HANDOVER.md` "미해결 문제" 섹션에 등록한다. 등록 필수 항목: 발견일, 발견 경위, 증상, 영향, 수정 방향, 상태(조사 예정/수정 예정). "한계/후속" 줄만으로는 부족. 근거: 0-1-2 테스트 실패 추적 의무는 "테스트 실패"에만 적용되므로, 비실패 경고 이슈는 본 항목으로 추적 의무화. P19(런타임 검증 게이트)·P21(사용자 투명성) 부합.
   - **검증 자동화 루프(하네스)**: 수정 후 표준 검증(0-1-2 표준 검증 절차) 실패 시 테스트 실패 추적 → 근본 원인 해결 → 재검증을 통과까지 자동 반복. 3회 반복 후에도 동일 실패 지속 시 근본 원인 파악 실패로 판단, 사용자 보고 후 대기. 루프 중 규칙 0-3(롤백)·0-5(사용자 설계 로직 변경)·0-6-1(원칙 위반 발견)·0-1-1(범위 외 구현) 해당 시 즉시 중단+보고. 검증 루프 내 수정 사이클은 본 규칙(0-1 세션당 1단계)의 1단계 내 허용. P19·P21 부합.
   - 백엔드 수정 시: pytest + 런타임 기동. 프론트엔드 수정 시: build + 브라우저 확인.

0-1-3. **세션 종료 전 잔존 프로세스 완전 종료 (강제).** 모든 작업 완료 후 세션 종료 전 반드시 다음 절차 수행:
   1) **잔존 프로세스 확인**: `ps aux | grep -E "python|main.py|pytest|vite" | grep -v grep`로 백엔드/테스트/프론트엔드 프로세스 잔존 여부 확인.
   2) **잔존 시 즉시 종료**: 1개라도 잔존하면 `kill <PID>`로 종료. 응답 없으면 `kill -9 <PID>`.
   3) **재확인으로 0건 확인**: 종료 후 재확인하여 잔존 0건까지가 세션 종료 조건.
   - **적용 범위**: 백엔드 런타임 기동(0-1-2 표준 검증 절차), pytest 실행, db-backup 앱 종료 등 프로세스를 띄운 모든 작업. 프론트엔드 전용 작업이라도 백엔드 기동을 동반한 경우 포함.
   - **세션 종료 조건**: 잔존 프로세스 0건 확인 전에는 세션 종료 불가. HANDOVER.md 갱신 + 사용자 보고 전에 반드시 수행.
0-2. **수정 전 사전조사 의무 (강제).** 코드 수정 착수 전 반드시 다음 4항목 조사:
   1) **의존성**: 이 수정이 다른 어떤 코드(함수/변수/모듈/타입/설정 키)에 영향을 주는지 전체 코드베이스 검색으로 식별. **표준 검색 범위: `app`, `src`, `tests`. `docs`는 설계 참조 시에만.**
   2) **영향범위**: 백엔드/프론트엔드/테스트 중 어디까지 바뀌는지 명시.
   3) **아키텍처 원칙 부합 여부**: P10(SSOT)/P16(살아있는 경로)/P20(폴백 금지)/P21(사용자 투명성)/P22(데이터 정합성)/P23(일관성)/P24(단순성)/P25(격리된 실패) 중 해당 원칙 확인.
   4) **기존 공통 자산 확인 (P23 사전 절차)**: 구현하려는 기능(함수/컴포넌트/상수/표준 색상/패턴)과 동일 또는 유사한 공통 자산이 프로젝트에 이미 있는지 먼저 검색. 있으면 반드시 그것을 활용하고, 같은 기능을 새로 만들지 않음. (백엔드: `core/constants.py`, 공통 유틸, 기존 함수 / 프론트엔드: `components/common/`, 공통 함수, 표준 색상)
   - 조사 결과를 수정 계획에 포함하여 사용자에게 제시.
   - 사용자 승인(실행 지시어, 규칙 0) 후에만 수정 시작.
   - 스킬(problem-solve/backend-fix/frontend-fix)의 사전조사 절차와 동일 기준 적용. 스킬 미호출 시에도 본 규칙 적용.
0-2-5. **사전조사 후 작업량 판명 시 워크플로우 전환 규칙 (강제).** 규칙 0-2 사전조사 완료 후, 작업량을 판단하여 아래 절차 적용. 처음부터 다단계로 명시된 요청은 섹션4 "다단계 작업 워크플로우" 그대로 적용.
   1) **작업량 판단**: 사전조사 결과(의존성·영향범위·수정 파일 수·검증 부담) 기준으로 단일 세션 완료 가능 vs 다단계 워크플로우(설계→태스크→구현) 필요 여부 판단. **정량 기준(강제 — P10 SSOT, 컨텍스트 관리 규칙 1과 동일 기준): 수정 파일 3개 초과 또는 수정 라인 50줄 초과 시 다단계 워크플로우 권장.** 기준 미달이더라도 의존성·영향범위가 복잡한 경우 다단계 전환 가능하되, 그 사유를 사용자에게 명시.
   2) **단일 세션 가능**: 사전조사 결과 + 수정 계획을 사용자에게 보고 후, 승인(규칙 0) 시 기존 단일 세션 절차로 진행.
   3) **다단계 필요 시 전환 제안 + 승인**: 사용자에게 "작업량이 커 다단계 워크플로우(설계→태스크→단계별 구현)로 진행을 권장"이라고 제안. 전환 승인(규칙 0 실행 지시어)을 받은 후에만 섹션4 "다단계 작업 워크플로우" 1세션(설계)으로 진입. 승인 전까지 임의로 다단계로 전환하거나 임의로 단일 세션으로 진행 금지.
   4) **이미 다단계로 시작한 경우**: 섹션4 워크플로우 그대로 적용, 본 규칙 미적용.
   - **금지**: 사전조사 후 작업량이 큼이 판명되었는데도 사용자 승인 없이 다단계로 전환. 반대로 작업량이 큰데 임의로 단일 세션에서 진행하려는 것도 금지.
   - **사용자 소통**: 일반 사용자 용어로 작업량 근거(수정 범위, 예상 단계 수)를 간결히 설명. 기술 참조 노출 금지(규칙 0-8 1항).
0-2-6. **가정·트레이드오프 선언 의무 (강제).** 사전조사(규칙 0-2) 후 수정 계획 제시 시, 코드/설정/기존 패턴으로 확정 불가한 부분은 추측을 숨기지 말고 "가정: X로 간주(틀린 경우 Y 영향)" 형태로 명시. 사전조사로 해결 가능한 것은 가정으로 남기지 않음(P24). P21(사용자 투명성) 부합.
0-3. **사용자 승인 없는 롤백 절대 금지 (강제).** 기존 코드를 이전 상태로 되돌리는 모든 행위(롤백, 되돌리기, 이전 커밋 상태로 복원, 함수/변수/로직 제거 후 이전 구조로 회귀)는 반드시 사용자 승인을 받아야 한다.
   - **금지**: "조용히 되돌리면 해결된다"는 판단으로 사용자 모르게 롤백. "dead code로 판단되어 제거" 등의 사유로 사용자가 설계한 로직을 승인 없이 되돌리는 행위.
   - **롤백 사유 명시**: 롤백이 필요한 경우 사유, 되돌릴 대상(파일/함수/커밋), 영향 범위를 사용자에게 일반 사용자 용어로 보고 후 승인받아 진행.
   - **적용 대상**: 코드 제거, 변수/함수 삭제, 이전 커밋으로 체크아웃, git revert/reset, 로직 구조를 이전 형태로 되돌리는 리팩토링 등 코드를 이전 상태로 회귀시키는 모든 행위.
   - **롤백 사유 기록 의무 (강제)**: 사용자 승인을 받아 롤백을 진행한 경우, 승인만으로 끝내지 않고 반드시 아래 2곳에 사유를 기록. 나중에 git log나 HANDOVER.md만 보는 사람(또는 다음 세션 에이전트)이 "왜 이전 상태로 되돌아갔지?" 오인하는 것을 방지.
     1. **git commit 메시지**: 롤백 커밋 메시지에 사유·되돌린 대상(파일/함수/커밋)·영향 범위를 상세 기록. "revert" 한 단어로 끝내지 않음.
     2. **HANDOVER.md**: "직전 완료 작업" 섹션에 롤백 내용과 사유 명시. 다음 세션이 롤백 맥락을 이어서 파악할 수 있도록.
0-4. **핵심 로직 변경 시 일반 사용자 용어 설명 + 승인 필수 (강제).** 수신률, 업종 점수, 매매 로직, 매수 후보 선정, 매도 조건 등 핵심 기능을 변경할 때는 사용자가 이해할 수 있도록 일반 사용자 용어로 변경 내용을 설명하고 승인을 받아야 한다. 승인 전까지 수정 금지 (규칙 0과 동일 적용).
   - **설명 의무**: 변경 전 동작(화면에서 어떻게 보였는가), 변경 후 동작(화면에서 어떻게 달라지는가), 사용자가 확인할 수 있는 영향(매수 후보 목록 변화, 업종 점수 표시 변화 등)을 일반 사용자 용어로 설명.
   - **금지**: 기술 참조(함수명, 변수명, FID 번호 등)만 나열하고 일반 사용자 용어 설명을 생략하는 행위(규칙 0-8 1항). 사용자가 "이게 화면에서 어떻게 달라지는지 모르겠다"고 느끼면 규칙 위반.
   - **교차 참조**: 사용자 의사소통 규칙 1~4(기술 명령어 안내 금지, UI 기준 검증 안내, API 직접 호출 안내 금지, 일반 사용자 용어 사용)와 동일 기준 적용.
0-5. **사용자가 설계/승인한 로직은 더 엄격하게 (강제).** 사용자가 이전에 직접 설계하거나 명시적으로 승인한 로직(예: 누적 수신 세트, 업종 점수 계산식, 매수/매도 조건 등)을 변경 또는 제거할 때는 일반 변경보다 더 엄격한 절차를 따른다.
   - **변경 사유 상세 보고**: 왜 변경해야 하는지, 기존 로직이 어떤 문제를 일으키는지(또는 일으키지 않는지)를 구체적으로 보고.
   - **영향 범위 상세 보고**: 기존 로직이 연결된 모든 경로(화면, 매수 후보 선정, 업종 점수 등)에 미치는 영향을 명시.
   - **대안 제시**: 변경 외에 다른 해결 방법이 있는지, 변경하지 않고 유지하는 것이 가능한지 대안 포함.
   - **승인 없이 진행 금지**: 사유/영향/대안 보고 후 사용자 명시적 승인(규칙 0 승인 트리거)이 있어야만 수정. "dead code로 판단되어 제거" 등의 단독 사유로 사용자 설계 로직을 승인 없이 제거하는 것은 절대 금지.
0-6. **수정안 제시 원칙 (강제).** 수정안/구현안을 사용자에게 제시하기 전에 반드시 아키텍처 원칙(P10~P25) 부합 여부를 체크. 아키텍처 원칙에 부합하지 않는 임시방편·땜빵·우회 수정안과 "임시로/우선/나중에" 등 우회 표현은 제시하지 않음. 제시 방식은 작업 유형에 따라 아래 기준 적용:
   - **버그 수정**: 아키텍처 원칙에 가장 부합하는 근본 해결안 1개만 제시. 임시방편/땜빵/우회 수정안 제시 금지. 선택지 나열 금지.
   - **잘못된 로직 유지한 채 추가 금지 (강제)**: 기존 잘못된 로직(버그, 아키텍처 원칙 위반, 오작동 코드)을 제거·수정하지 않고 그대로 둔 채 그 위에 새 로직을 추가하는 우회 수정 금지. 반드시 잘못된 로직 자체를 먼저 제거·수정한 뒤, 새 로직을 동일 경로에 반영. 예: 잘못된 캐시 기반 delta 계산을 두고 news_boost만 별도 처리 추가 → 금지. 잘못된 delta 계산 자체를 수정 후 news_boost를 동일 경로에 반영. (근본 해결 — P10 SSOT · P16 살아있는 경로 · P24 단순성 교차 강화, 규칙 0-6 "임시방편/땜빵 금지" 구체화)
   - **신규 구현**: 사용자 요구사항이 `ARCHITECTURE.md` 원칙에 위반되는 경우, 에이전트가 위반 사실(위반 원칙 번호 + 사유)을 사용자에게 경고하고 원칙에 부합하는 대안을 제시. 사용자는 본인 요청의 원칙 위반 여부를 스스로 판단할 수 없으므로, 에이전트의 경고·대안 제시 의무가 필수 (섹션1 사용자 프로필 연계).
   - **예외 (기존 규칙 유지)**: (a) problem-solve 스킬 섹션 1-1 "사용자 의도 파악 질문" 시 선택지+트레이드오프 제시, (b) 규칙 0-5 "핵심 로직 변경 시 대안 제시"는 본 규칙과 별개로 그대로 적용.

0-6-1. **아키텍처 원칙 위반 발견/발생 시 즉시 중단 + 사용자 보고 (강제).** 작성 중이거나 기존 코드에서 P10~P25 위반을 발견한 경우, 즉시 작업을 중단하고 사용자에게 보고(위반 원칙 번호 + 사유 + 영향 범위) 후 승인 대기. "임시로/우선/나중에" 금지는 0-6 수정안 제시 원칙에 이미 포함되므로 본 항목은 '발견 시 행동'만 명시 (P24 중복 회피). 단, 사용자가 위반 영향을 이해하고도 명시적으로 감수를 결정한 경우 그 결정을 존중 (P21 사용자 투명성 + 사용자 최종 결정권).
0-6-2. **작업 완료 시 자동 커밋 + 자동 핸드오버 갱신 (강제).** 사용자가 "커밋해"/"핸드오버 갱신해"라고 별도 지시하지 않아도, 작업 완료(검증 통과) 시 **기본적으로** 아래를 자동 수행. 사용자가 매번 지시해야 하는 부담 제거 (P21 사용자 투명성 · P24 단순성).
   - **git 커밋 (자동 — 코드만)**: 코드 변경 사항이 있으면 즉시 커밋. **커밋 대상은 코드 파일만** — `HANDOVER.md`는 커밋에 포함하지 않음 (P21 사용자 투명성 — 코드 변경과 핸드오버 갱신이 단일 커밋에 묻혀 사용자가 분리 확인하기 어려운 문제 회피 · P24 단순성 — 단일 세션完結 작업에도 핸드오버를 매번 통합 커밋하는 절차 과잉 회피). 변경 사항이 없으면 커밋 생략.
   - **HANDOVER.md 갱신 (자동 — 커밋 제외)**: 작업 완료 즉시 **파일만 갱신** (read-before-write, 규칙 8). **`HANDOVER.md`는 git 추적 대상 아님 — 커밋에 포함하지 않고 작업 디렉터리 파일로만 유지** (세션 간 에이전트 간략 소통 역할, 규칙 8-1/8-2 참조). 필수 기재 항목은 규칙 8-1 "필수 기재 항목" 참조.
   - **수행 순서**: 검증 통과 → 코드 커밋(코드만) → `HANDOVER.md` 갱신(파일만, 커밋 제외) → 세션 완료 보고(채팅 출력 — '커밋 및 핸드오버 표'로 사용자가 갱신 여부·내용 확인).
   - **갱신 의무 예외 (코드 수정 없는 작업)**: 순수 문서 변경(AGENTS.md/SKILL.md/ARCHITECTURE.md 등)·설정 파일 변경·조사 보고서 작성 등 **코드 수정이 없는 작업**은 `HANDOVER.md` 갱신 의무 없음. 갱신할 실질 내용(다음 세션에 전달할 코드 작업 맥락)이 없으므로 (P24 단순성). 단, 사전조사 중 미해결 문제(규칙 9) 발견 시는 예외 — "미해결 문제" 섹션에 기록해야 하므로 이 경우 한해 `HANDOVER.md` 갱신 수행 (코드 수정 없는 작업이라도).
   - **승인 전 수정 금지 (규칙 0) 상태**: 사전조사·추천·제안 단계에서는 코드 수정이 없으므로 커밋 수행하지 않음. 핸드오버 갱신은 위 "갱신 의무 예외" 항 적용 (미해결 문제 발견 시에만 갱신).
   - **푸시는 예외 (기존 유지)**: 푸시는 사용자가 명시적으로 요청한 경우에만 별도 수행 (Git 규칙 "DO NOT push unless explicitly asked").
0-7. **세션 완료 보고 명시 의무 (강제).** 세션 완료 보고는 커밋(변경 시)과 `HANDOVER.md` 갱신이 끝난 뒤 출력한다. 보고서 본문에 아래 3항목을 **각각 독립된 줄**로 명시한다.
   - **커밋**: 커밋 해시 (변경 커밋 시) 또는 "없음(변경 없음)"
   - **푸시**: "안 함(기본)" 또는 "완료(사용자가 명시 요청 시)"
   - **HANDOVER**: 아래 3가지 상태 중 하나로 명시
     - `완료(내용 요약)` — 핸드오버 갱신 완료. 내용 요약 1줄 포함 (예: "완료(직전 완료 작업 1건 갱신)").
     - `보류(사유)` — 갱신 보류. 사유 명시 (예: "보류(코드 수정 없는 문서 작업 — 규칙 0-6-2 갱신 의무 예외)").
     - `갱신 없음(코드 수정 없음)` — 코드 수정 없는 작업으로 갱신 의무 예외 적용 (규칙 0-6-2).
   - **독립 줄 표시 (강제)**: 위 3항목은 보고서 본문에서 **각각 독립된 줄**로 명시적 표시. 한 줄로 통합·병합 금지. "커밋에 포함"·"커밋과 함께" 등 간접·통합 표현 금지 — 반드시 "📝 HANDOVER: 완료(...)" / "📝 HANDOVER: 보류(...)" / "📝 HANDOVER: 갱신 없음(...)" 형태로 명시.
   - **상세 포맷**: 섹션4 "세션 완료 보고 포맷" 준수. 본 항목은 강제성 명시용이며 포맷 항목과의 중복 기재 금지 (P10 SSOT · P24 단순성 — "중복 금지"는 포맷 항목 간 중복을 의미, 3항목 독립 줄 표시와 충돌 아님).
0-8. **사용자 보고 의무 (강제).** 사용자에게 전달하는 **모든 보고**(사전조사 보고, 수정 계획 보고, 진행 중 알림, 세션 완료 보고 등)는 아래 4항목을 준수. 본 규칙은 기존 분산 규칙(섹션1 사용자 프로필, 사용자 의사소통 규칙 1~4, 규칙 0-4, 세션 완료 보고 포맷)의 **단일 강제 소스** 역할 (P10 SSOT — 기존 규칙과 중복 기재하지 않고 본 규칙이 통합 강제).
   - **1항 (일반 사용자 용어로 설명)**: 모든 보고는 **일반 사용자 용어**(코딩을 모르는 사용자가 이해할 수 있는 용어)로 설명. 기술 참조(파일 경로, 줄번호, 함수명, 변수명, 스택 트레이스, 패키지명, 명령어 등)는 사용자 보고에서 생략. UI 변화가 있는 보고에서는 화면에서 어떻게 달라지는지를 먼저 설명. UI 변화가 없는 내부 작업(조사·설치·설정 등) 보고에서는 작업의 목적·결과·영향을 일반 사용자 용어로 설명 (화면 변화 강제 아님). 단, 사용자가 명시적으로 기술 세부사항을 요구한 경우는 예외 (사용자 의사소통 규칙 4 예외 조항과 동일).
   - **2항 (P10~P25 부합 여부 명시)**: 보고의 모든 단계(사전조사·수정안 제시·세션 완료 보고)에서 아키텍처 원칙(P10~P25) 부합 여부를 명시. 사전조사 단계는 규칙 0-2 3)항, 수정안 제시 단계는 규칙 0-6, 세션 완료 보고 단계는 섹션4 "세션 완료 보고 포맷"의 "아키텍처 원칙 부합" 항목 준수. 위반 발견 시 규칙 0-6-1.
   - **3항 (보고 유형별 이모지 포맷 적용 — 강제)**: 모든 보고는 아래 표에 따라 이모지 포맷을 적용. 이모지는 섹션4 "세션 완료 보고 포맷"의 6개(📦 커밋 · 🚀 푸시 · 📝 HANDOVER · ✅ 원칙 · ⚠️ 경고 · 💡 변경내용)를 사용. 다른 이모지 임의 사용 금지 (P23 일관성). 각 이모지는 독립된 줄로 표시, 생략 시 "없음"으로 표시.

     | 보고 유형 | 적용 이모지 | 비고 |
     |-----------|-------------|------|
     | 사전조사 보고 | 💡 ✅ ⚠️ | 커밋/푸시/HANDOVER 제외 (코드 수정 전) |
     | 수정 계획 보고 | 💡 ✅ ⚠️ | 커밋/푸시/HANDOVER 제외 (코드 수정 전) |
     | 진행 중 알림 | ⚠️ (해당 시) | 위험·경고 발생 시만 |
     | 세션 완료 보고 | 📦 🚀 📝 ✅ ⚠️ 💡 | 전체 6개 적용 (섹션4 포맷 준수) |

     - 코드 수정 없는 세션의 세션 완료 보고: 📦 커밋 "없음(변경 없음)" · 🚀 푸시 "안 함(기본)" · 📝 HANDOVER "갱신 없음(코드 수정 없음)"으로 표시 (항목 자체를 생략하지 말고 명시 — 규칙 0-6-2 갱신 의무 예외).
   - **4항 (보고서 작성 전 규칙 재확인 — 강제)**: 보고서 작성 전 반드시 본 규칙(0-8)과 섹션3 "작업 완료 보고서 표준 형식 (필수 포함 표)", 섹션4 "세션 완료 보고 포맷"을 재확인 후 작성. "중간 보고라서 이모지 포맷이 적용되지 않는다"는 해석 금지 — 본 규칙 3항 표에 따라 모든 보고 유형에 해당 이모지 적용.
   - **강제성 (규칙 0-6 동일 등급)**: 본 규칙 위반(기술 용어 남용, P10~P25 부합 여부 누락, 이모지 포맷 누락, 또는 재확인 절차 미수행) 시 규칙 0-6(근본 해결만 제시) 위반과 동일 수준으로 간주 — 임시방편/땜빵 보고 금지, 위반 시 해당 보고를 다시 작성하여 제출.
0-9. **오류·위험 알림 의무 (강제).** 사용자가 명시적으로 지시한 작업이라도, 실행 과정에서 오류·데이터 손실·매매 영향·안전 규칙 위반 소지가 감지되면 즉시 사용자에게 알리고 승인을 다시 받아야 한다. "사용자가 지시했으니 그대로 진행"은 알림 의무를 면제하지 않음 (P21 사용자 투명성).
   - **알림 대상**: 오류/예외, 데이터 손실·덮어쓰기, 매매 로직·잔고·주문 영향, 안전 규칙 1~6 위반 소지, 아키텍처 원칙 충돌(규칙 0-6-1), 롤백 필요(규칙 0-3).
   - **알림 방식**: 일반 사용자 용어로 위험 내용·영향·대안 설명(규칙 0-8 1항). "위험합니다"만 단독으로 쓰지 말고 화면에서 어떻게 달라지는지 명시.
   - **승인 대기**: 알림 후 사용자 승인(규칙 0) 전까지 해당 위험 작업 진행 금지. 단, 즉시 중단하지 않으면 되돌릴 수 없는 피해가 발생하는 경우만 먼저 중단 후 알림.
0-10. **수치 변경 시 드리프트 방지 절차 (강제).** 코드 상수(큐 크기·배치 주기·쓰로틀 간격·임계치·타임아웃·기본값 등 수치)를 변경할 때, `ARCHITECTURE.md` "수치 소유권 분리 기준(W3 SSOT)"과 L3 참조표 규칙에 따라 아래 절차를 수행. 본 규칙은 문서-코드 불일치(드리프트)의 구조적 원인 — 수치가 문서·코드 양쪽에 존재하여 어느 쪽을 고쳐도 다른 쪽이 낡는 문제 — 를 차단하는 장치.
   1) **소유권 판별**: 변경 대상 수치가 어느 소유권인지 판별 (`ARCHITECTURE.md` L3 참조표 기준).
      - **코드 상수 소유(L3 참조표 항목)**: 큐 크기, 배치 주기(0.1/0.2/0.5초), 쓰로틀 간격, fake_fill 지연, 알림 쿨다운 등 구현 세부 수치. 주인은 코드 상수.
      - **문서 소유(L1/L2 전략·안전 수치)**: 50ms 경고/200ms 중단, CircuitBreaker 5회·60초, 구독 한도 기본값 200, 일일 손실 한도 기본값 등. 주인은 `ARCHITECTURE.md`(값+근거 명시).
   2) **코드 소유 수치 변경 시**: 코드 상수만 수정. **`ARCHITECTURE.md` L3 참조표는 값이 바뀌어도 갱신하지 않음** (L3 참조표는 위치만 참조). 단, 상수의 위치(파일명 또는 상수명)가 바뀐 경우에만 L3 참조표 갱신.
   3) **문서 소유 수치 변경 시**: **`ARCHITECTURE.md`(L1/L2)를 먼저 수정** — 값 + 근거를 함께 갱신. 값만 바꾸고 근거를 남기지 않는 것 금지(W10 사용자 투명성·W3 SSOT). 문서 갱신 후 코드 상수 수정.
   4) **드리프트 점검(수정 후)**: 변경된 상수명으로 코드베이스 전체(`app`, `src`, `tests`, `docs`, `ARCHITECTURE.md`, `AGENTS.md`) 검색하여, 코드 소유 수치인데 문서에 값이 직접 기재된 잔존 여부 확인. 잔존 시 위치 참조(`파일명:상수명`)로 교정.
   5) **커밋 메시지 명시**: 수치 변경 커밋 메시지에 (a) 변경된 상수명·위치, (b) 소유권(코드/문서), (c) 변경 사유를 명시.
   - **금지**: 코드 소유 수치를 바꾸면서 `ARCHITECTURE.md`에도 같은 값을 직접 기재(이중 관리 → 드리프트 재발). 문서 소유 수치를 코드만 바꾸고 문서 근거를 갱신하지 않는 행위.


### 사용자 의사소통 규칙 (필수)

사용자는 UI로만 조작하므로(섹션1 참조), 모든 소통은 아래 원칙 준수:

1. **기술 명령어 안내 금지**: `curl`, `npm`, `cd`, `httpx`, `pytest`, `python`, `git`, `node` 등 기술 명령어를 사용자에게 직접 안내하지 않음. 에이전트가 직접 실행.
2. **UI 기준 검증 안내**: 사용자에게 확인/검증을 요청할 때는 항상 UI 조작으로 안내. 예: "설정 페이지에서 X를 확인해 주세요", "브라우저에서 Y 화면을 봐 주세요". 터미널/콘솔 확인 요청 금지.
3. **API 직접 호출 안내 금지**: 사용자에게 API 직접 호출(curl, httpx, Postman 등)로 검증을 요청하지 않음. 에이전트가 내부적으로 API 호출 후 결과를 UI 화면 또는 일반 사용자 용어로 보고.
4. **일반 사용자 용어 사용**: 기술 참조(파일 경로, 줄번호, 함수명, 스택 트레이스 등)는 사용자 보고에서 생략. "화면의 매수 후보 목록", "설정의 증권사 항목" 등 UI 요소 기준으로 설명. 단, 사용자가 명시적으로 기술 세부사항을 요구한 경우는 예외.

### 작업 완료 보고서 표준 형식 (필수 포함 표)

> **목적**: 사용자가 코딩 지식 없이도 한눈에 파악할 수 있도록, 모든 세션 완료 보고는 아래 필수 표를 포함한다. 본 형식은 `규칙 0-8`과 `세션 완료 보고 포맷`의 구체적 표준(P10 SSOT)이다. 각 `SKILL.md`는 본 형식을 참조하고, 스킬별 추가 항목만 행(row)으로 추가한다.
>
> **필수 표 (5개)**: 변경 내용 표 · 아키텍처 원칙 판별 표 · 검증 결과 표 · 최종 판별 표 · 커밋 및 핸드오버 표. 이 5개는 모든 세션 완료 보고에 필수 포함.
>
> **추가 표 허용**: 작업 성격에 따라 필수 5표 외에 추가 표를 포함할 수 있다. 예: 조사 보고서의 '조사 범위 표'·'비교 분석 표', 다단계 작업의 '단계별 진행 표', 영향도 분석의 '영향 도메인 표' 등. 추가 표는 필수 5표 뒤에 배치하되, 필수 표의 행을 중복 생성하지 않고 별도 관점을 다루는 경우에만 추가.
>
> **표 작성 공통 원칙**: `ARCHITECTURE.md` 부록 M 표준 용어 사전 준수 — "업종" not "섹터", "종목" not "주식", "매수 후보" not "바이 리스트", "보유 종목" not "홀딩/포지션", "매수" not "Buy/구매", "매도" not "Sell/판매".

> **이모지-표 연결**: `규칙 0-8 3항`의 6개 이모지는 요약 제목으로 유지하되, 아래 필수 표로 상세 전개한다.
> - `💡 변경내용` → **변경 내용 표**
> - `✅ 원칙` → **아키텍처 원칙 판별 표**
> - `⚠️ 경고` → **최종 판별 표**의 '경고/특이사항' 행
> - `📦 커밋` / `🚀 푸시` / `📝 HANDOVER` → **커밋 및 핸드오버 표**
> - `검증 결과 표`는 `✅ 원칙` 표 다음 또는 별도로 배치(P21 사용자 투명성 강화).

#### 1. 변경 내용 표

| 항목 | 변경 전 (문제) | 변경 후 (해결) |
|---|---|---|
| 문제 현상 | <사용자가 본 화면 증상, 일반 사용자 용어> | — |
| 근본 원인 | <실제 코드/설정 근본 원인, 내부 분석 시 파일:줄번호> | <해결된 근본 원인 요약> |
| 수정 방안 | — | <구체적 수정 내용, 화면에서 어떻게 달라지는지> |
| 영향 범위 | — | <영향 받는 화면·기능·종목/업종 범위> |

> **작성 요령**: '변경 전'과 '변경 후'가 좌우로 바로 비교되도록 작성. 사용자가 확인할 수 있는 화면 변화를 먼저 기술.

#### 2. 아키텍처 원칙 판별 표

| 원칙 | 판별 | 근거 |
|---|---|---|
| P10 SSOT | ✅/❌ | <데이터/설정/형식의 단일 진실 소스 여부> |
| P16 살아있는 경로 | ✅/❌ | <안전장치가 실제 실행 경로에 연결되었는가> |
| P20 폴백 금지 | ✅/❌ | <빈 값/None/누락을 폴백으로 덮지 않았는가, silent except pass 없음> |
| P21 사용자 투명성 | ✅/❌ | <백엔드 상태가 UI에 표시되고 사용자 의문이 제거되는가> |
| P22 데이터 정합성 | ✅/❌ | <파생 데이터가 중복 저장되지 않고 원본에서 파생하는가> |
| P23 일관성 | ✅/❌ | <용어 사전·네이밍·공통 자산 재사용·UI 패턴 일관성> |
| P24 단순성 | ✅/❌ | <중복 제거·함수/파일 길이·불필요한 추상화 없음> |
| P25 격리된 실패 | ✅/❌ | <한 태스크 실패가 루프 전체를 중단하지 않는가> |

> **위반 발견 시**: `규칙 0-6-1`에 따라 즉시 중단 + 사용자 보고. 완료 보고 단계에서는 위반이 해결된 후 부합한 원칙만 ✅로 표시.

#### 3. 검증 결과 표

| 검증 항목 | 목적 | 결과 |
|---|---|---|
| <정적 검증/빌드/테스트/런타임 기동/브라우저 등> | <왜 이 검증이 필요한지> | <통과/실패, 주요 수치(예: pytest 2806 passed, RuntimeWarning 0건)> |

> **필수 포함(해당 시)**:
> - 백엔드 수정: `pytest` + `RuntimeWarning` 기동 + 잔존 프로세스 0건
> - 프론트엔드 수정: `npm run typecheck` + `npm run build` (+ `npm run test`) + 브라우저 화면 확인
> - DB 작업: 백업/복원 파일 존재 및 크기 확인
> - 거래 로직: `test`/`simulation` 모드 확인, `execute_buy()`/`execute_sell()` 경로 로그 확인

#### 4. 최종 판별 표

| 항목 | 판별 | 비고 |
|---|---|---|
| 근본 원인 해결 | ✅/❌ | <증상 재발 방지 여부> |
| 아키텍처 원칙 부합 | ✅/❌ | <P10~P25 위반 없음> |
| 안전 규칙 준수 | ✅/❌ | <DB/주문/API 키/안전 규칙 1~6 준수> |
| 검증 통과 | ✅/❌ | <모든 표준 검증 통과> |
| 사용자 승인 | ✅/대기 | <핵심 로직·롤백 등 승인 필요 시 여부> |
| 경고/특이사항 | <없음/내용> | <작업 중 발견한 오류·위험·잠재 버그, 없으면 "없음"> |

#### 5. 커밋 및 핸드오버 표

| 항목 | 상태 | 비고 |
|---|---|---|
| git 커밋 | <해시/없음(변경 없음)> | <변경 없으면 "없음(변경 없음)"> |
| git 푸시 | <완료/안 함(기본)> | <사용자 명시 요청 시에만 완료> |
| HANDOVER.md 갱신 | <완료/보류(사유)> | <보류 시 사유 명시> |
| 설계 문서 갱신 | <해당 없음/완료 + 파일명> | <`docs/architecture_*_design.md` 갱신 시 파일명 명시, 다단계 설계 단계에서만 해당> |
| 태스크 문서 갱신 | <해당 없음/완료 + 파일명> | <`docs/plan_*.md` 갱신 시 파일명 명시, 다단계 태스크 단계에서만 해당> |
| 미해결 항목 | <없음/목록> | <HANDOVER "미해결 문제"에 등록된 항목> |
| 다음 세션 예정 | <요약/없음> | <다단계 작업 시 다음 단계 + 진행률(예: 단계 3/8)> |
| 사용자 확인 항목 | <없음/요약> | <브라우저·UI에서 직접 확인할 것> |

> **이모지 독립 줄 유지**: 위 필수 표와 별개로 `규칙 0-8 3항`의 6개 이모지 요약 줄은 세션 완료 보고 최상단에 반드시 독립된 줄로 출력.

### 금지 표현

"아마도", "아마", "대부분", "일반적으로", "일단", "우선", "임시로", "추후 개선", "나중에 리팩토링", "제 생각에는"

### 허용 표현

"코드에서 확인됨", "검색 결과 해당 클래스가 남아 있음", "빌드 로그에서 타입 오류가 확인됨", "이 부분은 아직 확인 필요"

---

## 섹션4. 작업 프로세스 (워크플로우)

### 세션 시작 절차 (필수 — 모든 세션 첫 응답 전 수행)

1. **HANDOVER.md 자동 확인 (강제)**: 세션 첫 응답 전 반드시 `HANDOVER.md`를 읽고 현재 상태 파악. 사용자가 "핸드오버 확인해줘"라고 별도 요청하지 않아도 자동 수행.
   - 확인 항목: 직전 완료 작업, 현재 상태, 진행 중 작업, 다음 단계, 미해결 문제.
2. **현재 상태 간략 보고**: 확인 후 사용자에게 현재 진행 상태를 간략히 요약 보고 (직전 완료 작업, 진행 중 작업, 다음 단계 우선순위).
3. **사용자 지시 대기**: 보고 후 사용자의 다음 작업 지시 대기. 사용자가 첫 응답에서 특정 작업을 지시한 경우(예: "X 수정해줘") 핸드오버 요약은 생략하고 지시된 작업에 집중 가능.
4. **완료 작업 정리 (규칙 7 연계)**: 새 세션 시작 시 `HANDOVER.md`의 모든 섹션에서 완료된 항목 정리 (규칙 7 준수). "직전 완료 작업"·"세션 개요"는 최근 1건만 유지하고 과거 완료 작업은 삭제. "미해결 문제"·"다음 세션 진행 대기"에서 완료 항목 제거.

### 문서 저장 경로 규칙 (P10 SSOT · P23 일관성)

모든 프로젝트 문서는 `docs/` 한 곳에서 통합 관리. `backend/docs/` 등 하위 디렉터리에 문서를 분산 배치하지 않는다.

| 문서 종류 | 경로 패턴 | 비고 |
|---|---|---|
| 설계서 (디자인 파일) | `docs/architecture_*_design.md` | 다단계 작업 1세션 산출물. 완료 시 삭제(규칙 10) |
| 태스크 파일 | `docs/plan_*.md` | 다단계 작업 2세션 산출물. 완료 시 삭제(규칙 10) |
| 조사 보고서 | `docs/*_investigation.md` | 역사적 기록. 유지 (삭제 금지) |
| 아키텍처 감사 문서 | `docs/architecture_audit_*.md` | 감사 계획·태스크. 코드 제거 규칙 3 역사적 로그 유지 대상 |
| 참고자료 | `docs/reference_*.md` | 외부 자료 정리 (시장 운영 시간 등). 유지 |
| API 스펙 | `docs/api_specs/` | 증권사 API 문서 등 |

- **금지**: `backend/docs/`, `frontend/docs/` 등 코드 디렉터리 하위에 문서 폴더 신규 생성.
- **기존 참조 갱신**: 문서 이동 시 `AGENTS.md`·`HANDOVER.md`·태스크 파일 등 모든 참조 경로를 함께 갱신 (P10 SSOT — 잔존 참조 방지).

### 다단계 작업 워크플로우 (설계 → 태스크 → 구현)

신규 기능, 구조 변경, 다단계 작업은 다음 순서로 진행. 각 세션은 섹션3 규칙 0-1(세션당 1단계) 준수.

#### 1세션: 설계 검토 + 디자인 파일 작성
1. 표준 아키텍처(`ARCHITECTURE.md` 25개 불변 원칙) + 프로젝트 아키텍처 검토
2. 사용자에게 질문하여 요구사항 확인 — **problem-solve 섹션 1-1 "사용자 의도 파악 질문 프로세스" 적용** (사용자 의사소통 규칙 1~4 준수). **사전조사(코드/설정/기존 패턴)로 확정 불가한 항목만 선별 질문** (규칙 0-2-6과 동일 기준 — 사전조사로 해결 가능한 것은 질문 대상 아님, 과잉 질문 금지 — P24 단순성). 질문·답변은 설계서 "사용자 결정 항목" 섹션에 기록 (2세션 태스크 파일에서 활용).
3. 디자인 파일 작성: `docs/architecture_*_design.md` — **포맷은 "문서 표준 포맷" 섹션 참조** (P10 SSOT — 포맷 정의 단일화)
4. 사용자 승인 (규칙 0 승인 트리거) — 승인 전 다음 세션 진행 금지

#### 2세션: 심층 사전조사 + 태스크 파일 작성
1. 디자인 파일 기반 심층 사전조사 (규칙 0-2 4항목: 의존성·영향범위·아키텍처 원칙 부합·기존 공통 자산 확인)
2. 작업량 계산 → 단계 분할 (컨텍스트 관리 규칙 1 연계)
3. 단계별 태스크 파일 작성: `docs/plan_*.md` — **포맷은 "문서 표준 포맷" 섹션 참조** (P10 SSOT — 포맷 정의 단일화)
4. 사용자 승인 — 승인 전 다음 세션 진행 금지

#### 3세션~: 단계별 구현
1. 재심층 사전조사 (규칙 0-2 — 태스크 파일 기반, 실제 코드 대상)
2. 사용자 승인 (규칙 0)
3. 아키텍처 원칙(섹션2) + 작업 규칙(섹션3) 준수하여 수정/테스트/검증
4. 코드 커밋(코드만) 및 HANDOVER.md 갱신(파일만, 커밋 제외 — 규칙 0-6-2)
5. 다음 단계는 다음 세션에서 HANDOVER.md 기반 이어서 진행 (규칙 0-1, 컨텍스트 관리 규칙 6)

#### 완료: 계획서 파일 삭제
- 모든 단계 완료 후 최종 커밋 시 계획서 파일(디자인 + 태스크 파일) 삭제 — 규칙 10 준수

#### 적용 기준 (P24 단순성)
- **적용**: 신규 기능, 구조 변경, 다단계 작업 (설계 검토가 필요한 경우)
- **미적용 (기존 단일 세션 절차 유지)**: 단일 버그 수정, 1파일 1함수 내 변경, 사용자가 직접 설계한 로직의 1회 변경 — 사전조사(규칙 0-2) → 승인 → 수정 절차 그대로 적용. 과잉 프로세스 금지. (작업량 기준은 규칙 0-2-5 · 컨텍스트 관리 규칙 1 참조 — "단순 조정" 등 주관 표현 대신 정량 기준 사용.)

### 문서 표준 포맷 (템플릿)

> 다단계 작업 산출물(설계서·태스크 파일), 인계 문서(HANDOVER.md), 세션 종료 보고서의 표준 포맷. P10(SSOT — 포맷 정의 단일화) · P23(일관성 — 공통 헤더/섹션 구조) · P24(단순성 — 필수/선택 분리) 부합.
> **FIX-WS-04/NWS 다단계 워크플로우에서 실제 검증된 구조**를 표준화.

#### 공통 원칙 (모든 파일)
- **공통 헤더**: 제목(`# <종류>: <기능 한글명>`) + 인용 블록(상태 · 작성일 · 관련 원칙 P번호 · 관련 파일 경로 · 관련 API 스펙). 설계서/태스크 파일에 적용. HANDOVER는 고정 헤더(`# SectorFlow HANDOVER`).
- **P10 준수**: 같은 정보가 여러 파일에 중복 기재 금지. 상세 구현 내역은 git 커밋 메시지가 단일 진실 소스.
- **P24 준수**: 필수 섹션만 템플릿화. 선택 섹션은 필요시 추가 — 불필요한 빈 섹션 생성 금지.

#### 1. 설계서 포맷 (`docs/architecture_*_design.md`)

**목적**: 다단계 작업 1세션 산출물. "무엇을 왜 이렇게 만들 것인가"를 확정. 2세션 태스크 파일의 입력. 완료 시 삭제(규칙 10).

**파일명**: `docs/architecture_<기능명_스네이크케이스>_design.md` (예: `architecture_news_boost_design.md`)

**필수 섹션**:
| 섹션 | 가이드라인 |
|---|---|
| 헤더 블록 | `# 설계서: <기능 한글명>` + 인용 블록(상태/작성일/관련 원칙/관련 파일/관련 API 스펙) |
| 1. 배경 및 목표 | 1.1 현재 상태(기존 구조/문제, 코드 위치 인용) · 1.2 목표(번호 목록, 사용자 관점 달성 항목) · 1.3 비목표(다루지 않는 것 + 사유) |
| 2. 설계 방향 | 2.1 핵심 설계 결정(결정 N번 항목, 각 결정마다 "왜" 명시) · 2.2 기각 방안(표: 방안 \| 기각 사유) |
| 3. 사용자 결정 항목 | problem-solve 섹션 1-1 의무 — 사용자 질문·답변 그대로 기록. 2세션 태스크 파일에서 활용 |
| 4. 아키텍처 원칙 부합 검토 | 각 P원칙별 ✅/위반 + 근거 1줄. 헤더 "관련 원칙"과 교차 검증 |

**선택 섹션** (필요시만, 번호는 작성 시 재정렬):
- `동작 원리` — 데이터 흐름/상태 전이가 복잡한 경우 (전이 경로 2개 이상 또는 상태 변수 3개 이상). 그렇지 않으면 2.1에 통합
- `영향 범위` — 백엔드/프론트/DB/거래 로직 어디까지 바뀌는지. 변경 범위가 1개 영역만이면 1.2에 통합
- `리스크/롤백 기준` — 거래 로직 변경 시 필수(safe-trade 스킬 연계)

**작성 완료 후 액션**: 사용자에게 설계 요약 보고(일반 사용자 용어, 규칙 0-4) → 승인 대기(규칙 0) → 승인 시 HANDOVER "다음 세션 진행 대기"에 설계서 경로 + 2세션 예정 명시.

#### 2. 태스크 파일 포맷 (`docs/plan_*.md`)

**목적**: 다단계 작업 2세션 산출물. 설계서를 실행 가능한 단계로 분해. 3세션~ 구현의 지시서. 완료 시 삭제(규칙 10).

**파일명**: `docs/plan_<기능명_스네이크케이스>.md` (설계서와 동일 기능명)

**필수 섹션**:
| 섹션 | 가이드라인 |
|---|---|
| 헤더 블록 | `# 태스크 파일: <기능 한글명> 구현` + 인용 블록(상태/작성일/설계서 경로/다단계 진행 상황 각 세션 ✅/관련 원칙) |
| 0. 사전조사 결과 요약 | 규칙 0-2 4항목: 0.1 의존성(표: 파일 \| 변경점 \| 기준 라인) · 0.2 영향 범위 · 0.3 아키텍처 원칙 부합(P번호 ✅) · 0.4 기존 공통 자산 확인(재사용 + 신규 생성) |
| 1. 단계 분할 | 세션당 1단계(규칙 0-1). 각 세션별: 목표(1줄) + 수정 파일 목록 + 파일별 변경점 + 검증 방법(py_compile/ruff/mypy/pytest/런타임 기동/프론트 빌드) |
| 2. 사용자 결정 항목 | 설계서 "사용자 결정 항목" 섹션에서 이관. 구현 중 추가 결정 시 누적 기록 |

**선택 섹션** (필요시만):
- `3. 테스트 계획` — 신규 테스트 케이스 목록 (테스트 추가 세션)
- `4. 런타임 검증 방법` — 기동 명령, 확인 API, 체크 포인트 (백엔드 변경 시)
- `5. 바로잡음 로그` — 구현 중 태스크 기재 오류 발견 시 원인+수정 기록 (NWS/FIX-WS-04에서 실제 사용 패턴)

**작성 완료 후 액션**: 사용자에게 단계 분할 요약 보고(예상 세션 수 + 각 세션 범위) → 승인 대기(규칙 0) → 승인 시 HANDOVER "다음 세션 진행 대기"에 태스크 파일 경로 + 3세션 예정 명시.

#### 3. HANDOVER.md 포맷

**목적**: 세션 간 작업 인계. **인덱스 역할만** 수행(규칙 8-2). 삭제 금지 — 살아있는 문서.

**파일명**: 고정 `HANDOVER.md` (프로젝트 루트)

**필수 섹션**: **규칙 8-1 "HANDOVER.md 표준 섹션 구조" 참조** (P10 SSOT — 중복 정의 금지). 요약:
- `세션 개요` (최근 1건) · `직전 완료 작업` (최근 1건, 항목당 3줄 이하) · `다음 세션 진행 대기` (완료 항목 즉시 제거, 항목당 3줄 이하) · `미해결 문제` (규칙 9, 완료 항목 즉시 제거)

**선택 섹션** (규칙 8-1 허용 범위 내):
- `참고 문서` — `docs/` 내 역사적 기록/감사 문서 경로 (삭제 금지 문서만)
- `사용자 직접 확인 항목` — 브라우저 최종 검증 등 사용자가 수행해야 할 항목 (FIX-WS-04에서 실제 사용)

**금지**: 상세 구현 내역 인라인 전개 · 보관용 임의 섹션 생성 · write 도구 덮어쓰기(규칙 8 read-before-write) — 상세는 규칙 8-1/8-2 참조.

#### 4. 세션 완료 보고 포맷 (채팅 출력, 파일 아님)

**목적**: 에이전트가 세션 종료 시 **채팅창에 출력**하는 보고서. 파일로 저장하지 않음(휘발 — 상세는 git 커밋 메시지에 영구 기록, HANDOVER는 세션 간 소통 인덱스). **일반 사용자 용어로 쉽게 설명**(규칙 0-8 1항 — 사용자 의사소통 규칙 1~4, 규칙 0-4 통합).

**출력 구조**:
1. 먼저 아래 **이모지 요약 6개**를 독립된 줄로 출력. 이모지 생략 금지(규칙 0-8 3항).
2. 이모지 요약 바로 아래에 **섹션3 '작업 완료 보고서 표준 형식 (필수 포함 표)'**을 실제 내용으로 채워 배치.
3. 이모지와 표의 연결:
   - `💡 변경내용` → **변경 내용 표**
   - `✅ 원칙` → **아키텍처 원칙 판별 표**
   - `⚠️ 경고` → **최종 판별 표**의 '경고/특이사항' 행
   - `📦 커밋` / `🚀 푸시` / `📝 HANDOVER` → **커밋 및 핸드오버 표**
   - **검증 결과 표**는 `✅ 원칙` 표 다음에 배치

**이모지 요약 예시** (각 항목은 독립된 줄, 없으면 "없음"):
```
## 세션 완료 보고: <세션 ID> — <작업 한글명>

💡 변경내용: <사용자가 확인할 수 있는 변화를 일반 사용자 용어로. 없으면 "화면 변화 없음(내부 개선)">

✅ 원칙: <이번 세션 수정이 부합한 원칙 목록 — "P10 ✅ P16 ✅ P21 ✅" 간결 형태. 위반 발견 시 0-6-1에 따라 즉시 중단+보고하므로 완료 보고 단계에서는 부합한 원칙만 명시>

⚠️ 경고: <작업 중 발견한 오류·위험·잠재 버그. 없으면 "없음". 규칙 0-9 · 규칙 9 — 발견 시 HANDOVER "미해결 문제"에도 기록>

📦 커밋: <해시> (변경 커밋 시) / 없음(변경 없음)
🚀 푸시: <여부 — 기본 "안 함(기본)" / 사용자가 명시 요청 시 "완료">
📝 HANDOVER: <완료 / 보류(사유)>
(위 3항목은 반드시 각각 독립된 줄로 표시. "커밋에 포함"·"커밋과 함께" 등 통합·간접 표현 금지 — 규칙 0-7)
```

**필수 표 배치**: 위 이모지 요약 바로 뒤에, 섹션3에서 정의한 다음 필수 5표를 실제 내용으로 채워 출력한다.
1. **변경 내용 표**
2. **아키텍처 원칙 판별 표**
3. **검증 결과 표**
4. **최종 판별 표**
5. **커밋 및 핸드오버 표**

> **추가 표**: 작업 성격에 따라 위 필수 5표 뒤에 추가 표를 둘 수 있다(조사 보고서의 조사 범위 표·비교 분석 표, 다단계 작업의 단계별 진행 표 등). 필수 표의 행을 중복 생성하지 않고 별도 관점을 다루는 경우에만 추가.

> `수정 범위`, `다음 세션 예정`, `사용자 확인 항목`, `사용자에게 알려드릴 점`은 위 필수 표의 해당 행/셀로 통합. '수정 범위'는 '변경 내용 표'의 '영향 범위' 행에, '사용자 확인 항목'과 '다음 세션 예정'은 '커밋 및 핸드오버 표'의 해당 행에 기재.

**선택 항목** (해당 시만):
- `롤백 사유` — 사용자 승인으로 롤백한 경우 사유 요약. '커밋 및 핸드오버 표'의 '비고' 또는 '최종 판별 표'의 '경고/특이사항' 행에 추가 (규칙 0-3).

**금지**: 기술 참조(파일 경로/줄번호/함수명) 노출(규칙 0-8 1항) · 터미널 명령어 안내(사용자 의사소통 규칙 1) · API 직접 호출 안내(규칙 3).

**작성 완료 후 액션**: 검증 완료 → (변경 있으면 커밋) → HANDOVER.md 갱신 → 세션 완료 보고(채팅 출력). 푸시는 사용자가 명시적으로 요청한 경우에만 별도로 진행(Git 규칙 "DO NOT push unless explicitly asked").

### 스킬 자동 호출

사용자 요청에 맞는 스킬을 자동으로 호출하고, 사용자가 특정 스킬을 직접 요청할 때까지 기다리지 않습니다.

- **버그, 디버깅, 문제 분석, 근본 원인**: `/problem-solve` 호출
- **백엔드 코드 변경 또는 API 오류**: `/backend-fix` 호출
- **프론트엔드 UI, 페이지, 빌드 오류**: `/frontend-fix` 호출
- **DB 스키마 변경, 마이그레이션, 테이블 수정**: 먼저 `/db-backup` 호출 후 진행
- **매매 로직, 증권사 API, 주문 코드**: 먼저 `/safe-trade` 호출
- **신규 로직 구현**: 계획은 `/problem-solve` 호출, 구현은 `/backend-fix` 또는 `/frontend-fix` 호출

확실하지 않으면 먼저 `/problem-solve`를 호출합니다. 스킬이 명시적으로 호출되지 않더라도 항상 이 파일의 규칙을 따릅니다.

### 컨텍스트 관리 규칙

1. **작업량 기반 사전 분할**: 다단계 작업(신규 기능/구조 변경)의 작업량 분할은 섹션4 "다단계 작업 워크플로우" 2세션에서 수행. 단일 세션 내에서 작업량이 많은 경우 본 규칙 적용 — **분할 필수 기준: 수정 파일 3개 초과 또는 수정 라인 50줄 초과 시** (규칙 0-2-5 작업량 판단 기준과 동일). 각 단위는 독립적으로 완료·검증 가능한 크기로 설정. 분할 시 사용자에게 분할 계획을 보고 후 진행. 기준 미달이더라도 에이전트가 분할이 필요하다고 판단한 경우 분할 가능하되, 그 사유를 분할 계획 보고에 명시.
2. **단계 완료 시 컨텍스트 점검**: 각 작은 단계 완료 시 현재 컨텍스트 사용량을 점검. 사용량이 높아 다음 단계에서 오류 가능성이 있다고 판단되면, 다음 단계 진행 전 `HANDOVER.md`에 현재 진행 상태를 갱신하여 만일의 중단에 대비.
3. **사전 종료 권한**: 에이전트는 작업 중 컨텍스트 사용량이 많아져 오류 가능성이 있다고 판단되면, 현재 진행 상태를 `HANDOVER.md`에 기록하고 사용자에게 보고한 후 세션을 종료할 수 있음.
4. **HANDOVER.md 기록 의무 (인덱스 원칙)**: 세션 종료 전 반드시 다음을 기록. **HANDOVER는 인덱스 역할만 수행 — 상세 내용(사전조사/구현 계획/검증 상세)의 인라인 기록 금지** (규칙 8-2).
   - 완료된 작업 요약 — 수정 파일명 + 검증 결과 + **git 커밋 해시** (상세는 커밋 메시지에 기록, HANDOVER에 인라인 전개 금지)
   - 진행 중인 작업의 현재 단계 — 어디까지 했고, 어디서부터 이어하면 되는지 (docs/ 참조 경로 포함)
   - 다음 세션에서 수행해야 할 항목 — docs/ 참조 경로 또는 git 커밋 해시로 표기 (인라인 상세 금지)
5. **사용자 보고**: 세션 종료 시 사용자에게 진행 상태와 다음 단계를 명확히 전달.
6. **다음 세션 연속성**: 다음 세션 시작 시 "세션 시작 절차"의 HANDOVER.md 자동 확인(강제)에 따라 이어서 작업 진행.
7. **완료 작업 정리**: `HANDOVER.md`의 모든 섹션에서 완료된 항목은 즉시 정리. git history에 이미 기록되어 있으므로 중복 누적 방지.
   - **정리 시점 (2개)**:
     1. **작업 완료 시점 (즉시)**: 다단계 작업의 모든 단계 완료 시, "미해결 문제"·"다음 세션 진행 대기"에서 해당 항목 즉시 제거. 완료 후 다음 세션까지 잔존 금지.
     2. **새 세션 시작 시**: "직전 완료 작업"·"세션 개요"·"현재 상태"·"다음 단계" 섹션 최신 상태로 업데이트. **미집행 시 강제 압축** — 직전 완료 작업·세션 개요가 1건 초과 누적 시, 새 세션 시작 절차에서 최근 1건만 남기고 나머지 제거 (git history 참조).
   - **섹션별 유지 건수 + 줄수 상한**:
     - `직전 완료 작업` — 최근 1건, **항목당 3줄 이하** (수정 파일 + 검증 결과 + 커밋 해시만, 상세는 git history 참조)
     - `세션 개요` — 최근 1건
     - `미해결 문제` — 완료된 항목 즉시 제거 (해결 완료 보관용 섹션 생성 금지 — 규칙 8-1)
     - `다음 세션 진행 대기` — 완료된 항목 즉시 제거, **항목당 3줄 이하** (작업명 + 현재 단계 + docs/ 참조 경로)
   - **사각지대 방지**: "미해결 문제"에 등록된 항목이 완료된 경우, 완료 보고와 함께 해당 항목을 섹션에서 제거. "완료된 항목을 별도 보관 섹션으로 이관" 금지 (git history가 단일 진실 소스).
8. **HANDOVER.md 수정 원칙 (read-before-write)**: `HANDOVER.md` 수정 시 반드시 read 먼저 수행. 기존 내용을 보존한 후 병합. write 도구로 덮어쓰기 금지 — 기존 섹션 내용을 읽어 확인한 후에만 수정/추가. 실수로 기존 내용을 삭제하는 사고 방지.
8-1. **HANDOVER.md 표준 섹션 구조 (P23 일관성)**: `HANDOVER.md`는 **세션 간 에이전트 간략 소통 + 인덱스 역할**만 수행 (규칙 8-2). **git 추적 대상 아님** — 작업 디렉터리 파일로만 유지되며 커밋에 포함하지 않음 (규칙 0-6-2). 다음 표준 섹션 구조 유지. **표준 섹션 외 임의 섹션 생성 금지** (예: "해결 완료 문제 (참고용)" 같은 보관용 섹션 생성 금지 — 완료된 항목은 git history가 단일 진실 소스이므로 HANDOVER에서 제거만 수행).
    - `세션 개요` — 날짜, 작업, 상태, 참조 문서(계획서 경로). 최근 1건 유지 (규칙 7).
    - `직전 완료 작업` — 최근 1건, **항목당 3~5줄** (수정 파일 + 검증 결과 + 커밋 해시 + 완료된 작업 요약). 상세 구현 내역은 git history 참조 (규칙 7, 8-2).
    - `다음 세션 진행 대기` — 진행 중 작업의 단계 진행 상황 + 승인 대기 항목 + 참조 문서(계획서 경로). **항목당 3줄 이하**. 완료 항목 즉시 제거 (규칙 7).
    - `미해결 문제` — 규칙 9 기록 대상. 완료 항목 즉시 제거 (규칙 7).
    - 다단계 작업 진행 중인 경우 `다음 세션 진행 대기` 섹션에 단계 진행 상황 + 참조 문서(디자인/태스크 파일 경로) 명시
    - 계획서 파일 삭제(규칙 10) 시 `세션 개요`·`다음 세션 진행 대기`의 참조 문서에서 해당 경로 제거 (P10 SSOT — 잔존 참조 방지)
    - **필수 기재 항목 (강제 — 규칙 0-6-2 자동 갱신 시 빠짐없이 기재)**: 코드 수정 작업 완료 시 `HANDOVER.md` 갱신 시 아래 3항목을 **빠짐없이** 기재. 해당 없음 시에도 "없음"/"해당 없음"으로 명시적 표기 (생략 금지 — P21 사용자 투명성). **코드 수정 없는 작업은 갱신 의무 예외 (규칙 0-6-2) — 본 항목 적용 안 함.**
      1. **완료된 작업 요약** — 이번 세션에서 완료된 작업 내용 3~5줄. `직전 완료 작업` 섹션에 기재. 일반 사용자 용어 우선(규칙 0-8 1항), 수정 파일 + 검증 결과 + 커밋 해시 포함.
      2. **다음 단계 요약 + 진행률** — 다음 세션에서 이어서 해야 할 작업과 현재 진행률. `다음 세션 진행 대기` 섹션에 기재. 진행률은 "단계 N/M" 형태 명시(예: "단계 3/8 완료, 다음 단계 4/8 예정"). 단일 세션 완료 작업은 "없음(전체 완료)".
      3. **참고 문서 경로** — 관련 설계 파일(`docs/architecture_*_design.md`)·태스크 파일(`docs/plan_*.md`)·조사 파일(`docs/*_investigation.md`)·테스트 파일 경로. `직전 완료 작업` 또는 `다음 세션 진행 대기` 섹션의 참조 문서 란에 기재. 해당 문서 없으면 "참고 문서: 없음" 명시.
8-2. **HANDOVER 인덱스 원칙 (강제 — P10 SSOT / P24 단순성)**: `HANDOVER.md`에 상세 내용을 인라인으로 기록하는 것을 금지. HANDOVER는 세션 간 에이전트 간략 소통 + 인덱스 역할만 수행. **git 추적 대상 아님** — 작업 디렉터리 파일로만 유지 (규칙 0-6-2). 상세 구현 내역의 단일 진실 소스는 git 커밋 메시지 + docs/ 파일.
    - **인라인 기록 금지 대상**: 사전조사 결과, 구현 계획, 수정 상세 내역(줄 단위 변경), 검증 상세(테스트 개수/로그), 아키텍처 원칙 부합 분석, 영향 범위 분석, UI 기준 화면 변화 설명.
    - **상세 보관 위치**:
      - 다단계 작업 (설계/태스크): `docs/architecture_*_design.md`, `docs/plan_*.md` (완료 시 규칙 10에 따라 삭제 → git history만 잔존)
      - 완료된 작업 구현 상세: **git 커밋 메시지** (단일 진실 소스 — 규칙 7 "git history가 단일 진실 소스" 부합)
      - 조사 보고서: `docs/*_investigation.md` (역사적 기록, 유지)
    - **HANDOVER에 허용되는 내용**: 작업 식별자(세션 ID), 1줄 요약, 수정 파일 목록, 검증 결과(합격/불합격 + 커밋 해시), docs/ 참조 경로, 미해결 문제(규칙 9 형식 준수).
    - **커밋 메시지 의무**: 구현 상세(수정 사유, 변경 내역, 검증 상세)는 코드 커밋 메시지에 기록. HANDOVER에 기재한 커밋 해시를 통해 코드 커밋을 조회. 커밋 메시지에 "왜(why)" 중심 간결 기록 (Git 규칙 준수).
    - **적용 예시 (직전 완료 작업 항목)**:
      ```
      ### T3-S32 체결강도 매수차단 제거 문서 정리 — 완료 (2026-07-23)
      - 수정: ARCHITECTURE.md + p25 문서 + HANDOVER.md (3파일), 삭제: 설계/태스크 파일 (2파일)
      - 검증: 잔존 참조 0건 / 커밋 3433391
      ```
9. **작업 중 발견 문제 기록 의무 (강제)**: 메인 작업 도중 우연히 발견한 아키텍처 위반(P원칙), 오류, 잠재적 버그, dead code, 폴백 패턴, 아키텍처 원칙에 부합하는 더 나은 구조(개선점) 등은 현재 세션에서 즉시 해결하지 못하더라도 **반드시** `HANDOVER.md`의 "미해결 문제" 섹션에 기록. 사용자가 직접 해결할 수 없으므로, 에이전트가 발견 즉시 기록하지 않으면 영영 누락됨.
   - **기록 형식**: 파일명:줄번호, 위반 원칙 번호(P10/P16/P20 등) 또는 부합 원칙 번호(개선점의 경우), 증상/개선내용 1문장, (가능시) 수정 방향.
   - **개선점 인정 기준 (P24 준수)**: 주관적 취향이 아닌 **객관적 근거**가 있는 것만 — (a) 특정 P원칙에 부합하여 현재 구조보다 정량적으로 더 단순/일관/정합 (b) 기존 공통 자산 재사용으로 중복 제거 (c) 명확한 중복·dead code·폴백 회피 가능. 근거 없는 "이렇게 하면 더 좋을 것 같음"은 기록 대상 아님.
   - **중복 회피**: 이미 "미해결 문제"에 등록된 항목은 중복 기록하지 않고, 새 정보가 있으면 기존 항목에 갱신.
   - **세션 종료 보고**: 세션 종료 시 "미해결 문제"에 신규 추가된 항목이 있으면 사용자에게 명시적으로 보고 ("이번 세션에서 N건의 신규 문제/개선점을 발견하여 미해결 문제에 등록했습니다").
   - **다음 세션 우선순위**: 다음 세션 시작 시 `HANDOVER.md`의 "미해결 문제" 섹션을 확인하고, 사용자와 함께 해결 우선순위를 정함.
10. **작업 산출물 정리 (강제 — 작업 완료 시).** 작업 완료 후 최종 커밋 시, 아래 3항목의 임시 산출물을 정리. git history에 영구 기록되므로 작업 디렉터리에 보관 불필요 — 루트 폴더 및 작업 디렉터리에 불필요 파일 누적 방지 (P24 단순성 · P10 SSOT).
    - **(1) 계획서 파일 삭제 (다단계 작업 완료 시)**: 다단계 작업 워크플로우(섹션4 "다단계 작업 워크플로우")의 모든 단계 완료 후 최종 커밋 시, 해당 작업의 계획서 파일을 함께 삭제.
      - **삭제 대상**: `docs/plan_*.md` (태스크 파일), `docs/architecture_*_design.md` (디자인 파일), `docs/*-design*.md`·`docs/*-tasks.md` (설계·태스크 파일 — 파일명 접두사 무관, 다단계 작업 완료 산출물 전체 포함), `docs/coupling-*-matrix.md`·`docs/coupling-*-index.md`·`docs/coupling-*-backref.md`·`docs/coupling-*-callgraph.md`·`docs/coupling-*-boundary.md`·`docs/coupling-*-direct.md`·`docs/coupling-*-normalization.md` (감사 산출 매트릭스 문서), `docs/*_audit_*.md`·`docs/*-audit-*.md` (감사 계획·태스크 문서 — architecture_audit_*, dead-code-audit_*, duplication-audit_*, coupling-audit-* 전부 포함), `docs/*_investigation.md` (조사 보고서). 모든 완료된 작업의 계획/태스크/감사/조사 문서는 삭제 대상 — git 히스토리가 단일 진실 소스(SSOT)이므로 작업 디렉터리에 별도 보존 불필요.
      - **삭제 시점**: 모든 단계 완료 후 최종 커밋에 삭제 포함 (동일 커밋)
      - **삭제 제외 (혼동 주의)**:
        - `docs/reference_*.md`·`docs/krx_nxt_*.md` 등 참고자료 (외부 자료 정리 — 유지)
        - `docs/api_specs/` 등 API 스펙 (사용 중인 외부 API 문서 — 유지)
        - 진행 중인 작업의 계획서·감사·조사 문서 (일부 단계만 완료 시 유지)
      - **삭제 전 확인**: `HANDOVER.md` "참조 문서" 섹션에서 해당 계획서 경로도 함께 정리 (P10 SSOT — 잔존 참조 방지)
      - **커밋 메시지**: 삭제된 계획서 파일명 명시
      - **승인**: 삭제 전 사용자 승인 필수 (규칙 0 — 파일 삭제는 코드 제거와 동일 취급, 규칙 0-3 롤백 규칙 연계)
    - **(2) DB 백업 파일 삭제 (마이그레이션 검증 완료 시)**: DB 스키마 변경/마이그레이션 작업 완료 후 런타임 검증(0-1-2 표준 검증 절차) 이상 없으면, db-backup 스킬이 생성한 타임스탬프 백업 파일(`*.db.*.backup`, `*.db-shm.*.backup`, `*.db-wal.*.backup`)을 삭제.
      - **삭제 조건**: 마이그레이션 후 런타임 기동 정상 + 핵심 데이터 조회 이상 없음 확인 후
      - **삭제 시점**: 검증 완료 후 다음 커밋에 포함 (또는 검증 완료 보고 시 사용자 승인 후 별도 삭제)
      - **승인**: 삭제 전 사용자 승인 필수 (안전 규칙 1 연계 — `.db` 원본 파일이 아닌 백업 파일만 대상)
    - **(3) 임시 산출물 정리 (작업 완료 시)**: 작업 과정에서 생성한 임시 파일(테스트용 스크립트, 디버그 로그, 임시 출력물 등)을 완료 시 삭제. 루트 폴더 및 작업 디렉터리에 불필요 파일 잔존 금지.
      - **승인**: 삭제 전 사용자 승인 필수 (규칙 0 — 파일 삭제는 코드 제거와 동일 취급)
    - **(4) 작업 상태 SSOT 정리 (코드 변경 없는 문서 상태 갱신 시)**: 코드 변경 없이 문서 상태만 갱신하는 작업 완료 시, `HANDOVER.md`의 상태 표기가 코드 실제 상태와 일치하는지 확인·갱신. 위반 항목 해결 완료 시 진행률 카운트·잔여 세션 수를 실제와 일치화. 감사 문서는 완료 시 규칙 10-(1)에 따라 삭제되므로, 감사 진행 중에는 해당 감사 문서의 상태 표(☐/◐/☑)도 실제와 일치시키되 완료 시 삭제. P10(SSOT — 코드-문서 불일치 차단) · P23(일관성 — 상태 표기 일관).
