# SectorFlow Agent Rules

> 로컬 실시간 주식 자동매매 웹 앱 (1인용). 백엔드 FastAPI+Python+aiosqlite, 프론트엔드 TypeScript(Vanilla).
> **우선순위**: 실행 우선 카드 > 층1 > 층2 > 층3. 상세 절차는 별도 문서에서 읽을 것.
> **사용자는 UI로만 조작** — 에이전트가 기술 작업 전담, 일반 사용자 용어로 설명.

---

## 층 1 — 핵심 (항상 준수)

### 핵심 6규칙 (절대 불가침)
1. **승인 전 수정 금지** — "승인" 단어만 승인 인정. "진행해/시작해/진행할게요" 등은 승인 아님.
2. **일반 사용자 용어** — 파일 경로·줄번호·함수명·코드 태그·명령어·규칙 번호·원칙 번호 등 기술·메타 용어 금지.
3. **완료 판정은 기계적 검증 결과로만** — 자기 평가 금지.
4. **안전 규칙** — DB 삭제 금지, 실전 모드 3조건 승인, 증권사 키 금지.
5. **작업 시작 전 아키텍처 판정** — 원칙 위반·보류이면 중단, 대안만 제시.
6. **핸드오버는 인덱스 역할만** — 상세 인라인 기록 금지.

### 실행 우선 카드
**시작**: 1.범위 고정 → 2.사전 확인(읽기 전용) → 3.아키텍처 판정 → 4.승인 확인(위험 4종만) → 5.한 단계 실행
**종료**: 6.완료 판정 → 7.종료 상태(완료=다음 제안 금지 / 대기 / 중단)
**종료 순서**: 검증 통과 → 저장(코드만) → 완료된 작업 문서 삭제(전체 완료 시) → 인계 문서 갱신 → 완료 보고

### 검증 명령어
| 대상 | 명령어 |
|---|---|
| 백엔드 테스트 | `.venv/bin/python -m pytest backend/tests -q` |
| 백엔드 린트 | `.venv/bin/ruff check backend/app/` |
| 런타임 | `.venv/bin/python main.py` |
| 프론트엔드 | `cd frontend && npm run typecheck` / `lint` / `build` / `test` |
| 완료 전 스크립트 | `bash scripts/pre-complete-check.sh auto` (실패 시 완료 보고 금지) |

> 완료 보고 전 `scripts/pre-complete-check.sh` 실행 필수. 모드: auto(권장) / backend / frontend / docs / all. 백엔드 프로세스 종료는 backend 모드만.

---

## 층 2 — 빠른 참조 (필요 시 읽기)

### 안전 규칙 (항상 적용)
1. `*.db` 파일 직접 삭제·덮어쓰기 금지. 2. DB 스키마 변경 전 백업 생성.
3. 실전 모드 전환 = 3가지 인지 후 승인 (a.실제 계좌 자금 b.손실 가능 c.실제 주문 전송).
4. 증권사 API 키·토큰 하드코딩 금지. 5. 승인 없이 `sudo`·`rm -rf`·`curl`·`wget`·`sqlite3` 실행 금지. 6. 시스템 경로 수정 금지.

### 실전 모드 vs 테스트 모드
| 구분 | 실전 모드 | 테스트 모드 |
|---|---|---|
| 매매 주문 | 실제 증권사 전송 | 가상 주문 |
| 돈 수치 | 증권사가 기준 | 가상 시뮬레이터가 기준 |
| 파생 지표 | 공통 공식 (원재료만 증권사값) | 공통 공식 (원재료만 가상값) |

### 핵심 SSOT 위치
| 대상 | 위치 |
|---|---|
| 백엔드 진입점 | `main.py` → `backend/app/web/app.py` |
| 엔진 | `backend/app/services/engine_lifecycle.py`, `engine_loop.py` |
| 설정 | `backend/app/core/settings_store.py` + `settings_defaults.py` |
| 주문 경로 | `backend/app/services/trading.py` `execute_buy()`/`execute_sell()` |
| 프론트 상태 | `frontend/src/stores/store.ts` (메인), `uiStore.ts` |
| WS 바인딩 | `frontend/src/binding.ts` · 라우터 `frontend/src/router.ts` |

### 문서 저장 경로
- 작업 산출물(전체 완료 시 삭제): 설계서 `docs/설계서/`, 태스크 `docs/태스크/`, 조사보고서 `docs/조사보고서/`
- 영구 문서: 절차 규칙 `docs/절차규칙/`, 참고 자료 `docs/참고자료/`, API 스펙 `docs/api_specs/`
- 모든 문서는 `docs/` 한 곳에서 관리. 폴더명은 한글. 코드 디렉터리 하위 문서 폴더 신규 생성 금지.

### 사용자 의사소통 (핵심)
- 확인·검증 요청 시 UI 조작으로 안내. 터미널 요청 금지.
- 응답 전송 직전 점검: 기술 참조·메타 용어 포함 시 일반 용어로 재작성.
- 세션 종료 계열 보고(완료·단계 완료·중단·보류)는 표 형식만(이모지 요약 금지). 모든 표 2칸 구조(`항목 | 내용`), 항목당 1행. 세션 유형별 고정 표 세트(7개 표, 순서 고정), 생략 불가 — 해당 없으면 "해당 없음" 행 명시. 다단계 작업 보고 제목은 `전체 N단계 중 N단계` 형식. 상세는 `절차규칙_보고_상세.md` + `완료보고_템플릿.md` 참조.

---

## 층 3 — 상세 절차 문서 인덱스 (작업 유형별 재독)

| 작업 유형 | 읽을 문서 |
|---|---|
| 모든 작업 시작 전 | `docs/절차규칙/절차규칙_세션절차_상세.md` (판정 게이트·체크리스트) |
| 조사 | `docs/절차규칙/절차규칙_조사_상세.md` |
| 사용자 보고 | `docs/절차규칙/절차규칙_보고_상세.md` + `완료보고_템플릿.md` |
| 설계서·태스크·HANDOVER | `docs/절차규칙/절차규칙_문서포맷_상세.md` (문서 역할 원칙 5항목) |
| 코드 수정·거래·세션 절차·다단계·커밋 전 검토 | `docs/절차규칙/절차규칙_세션절차_상세.md` (+ safe-trade 스킬 for 거래) |
| 코드 품질 패턴 | `docs/절차규칙/코드품질가이드.md` (에러·비동기·네이밍·프론트·테스트·DB/WS) |
| 보안 점검 | security-check 스킬 |
| 독립 검증 | independent-verify 스킬 (거래·핵심 로직 시 필수) |
| 컨텍스트 임박 | `docs/절차규칙/절차규칙_컨텍스트관리_상세.md` |

### 아키텍처 원칙
- `ARCHITECTURE.md` 기준: W1~W12·D1~D17. P1~P25는 부록 매핑표의 감사용 식별자.
- 주요 원칙: P10(SSOT)·P16(살아있는 경로)·P20(폴백 금지)·P21(투명성)·P22(정합성)·P23(일관성)·P24(단순성)·P25(격리된 실패).
- 판정 절차·P1~P25 전체 표: `docs/절차규칙/절차규칙_세션절차_상세.md` "판정 게이트" 참조.

### 스킬 자동 호출
- 버그·문제 분석: `/problem-solve` | 백엔드: `/backend-fix` | 프론트엔드: `/frontend-fix`
- DB 스키마 변경: 먼저 `/db-backup` | 매매 로직: 먼저 `/safe-trade`
- 대규모 전수 조사: `/continuity-investigation`
- 코드 작성 시 `docs/절차규칙/코드품질가이드.md` 표준 패턴 준수
