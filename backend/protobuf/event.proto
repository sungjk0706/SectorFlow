syntax = "proto3";

package event;

// 실시간 데이터 이벤트
message Event {
  string type = 1;           // 이벤트 타입 (예: "01", "0B", "real_time_price")
  map<string, string> data = 2;  // 이벤트 데이터
  double timestamp = 3;      // 타임스탬프 (unix timestamp)
  map<string, double> latency_trace = 4;  // 지연 추적 정보
}
