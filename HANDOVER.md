# SectorFlow Handover

## 세션 개요 (최근)
- **2026-07-22 (최근)**: B-22-b 세션 — deps.py+ws.py+config.py+stock_classification.py 주석 dead code 4건 제거 (P16). **수정 파일 4개** (백엔드 4파일, 테스트 수정 없음 — 주석/죽은 변수만 제거): (1) `backend/app/web/deps.py` (26줄→19줄) — `get_current_user` 함수 내 주석 처리된 JWT 검증 코드 12줄 제거 (실행되지 않는 죽은 코드, `# TODO: 개발 완료 후 토큰 검증 재활성화` 마커 포함). 유지: `get_current_user` 본체 (현재 개발 모드로 항상 "dev" 반환, docstring에 개발 모드 명시). (2) `backend/app/web/routes/ws.py` (206줄→200줄) — `ws_prices` 함수 내 주석 처리된 토큰 검증 5줄 제거 + `_send_initial_snapshot_delayed` 내 죽은 변수 `settings`/`_trade_mode` 및 `get_settings_snapshot` import 제거 (할당 후 사용처 없음). 유지: `ws_prices` 본체 (현재 개발 모드로 `username = "dev"` 사용, docstring에 개발 모드 명시), `_send_initial_snapshot_delayed` 본체 (state.data_ready_event/bootstrap_event/sector_summary_ready_event 대기 로직 전부 유지). (3) `backend/app/config.py` (79줄→68줄) — `Settings` 클래스 내 주석 처리된 설정 항목 6개 제거: `ADMIN_USERNAME`/`ADMIN_PASSWORD` (관리자모드 제거됨), `BROKER_BASE_URL`/`BROKER_APP_KEY`/`BROKER_APP_SECRET`/`BROKER_ACCOUNT_NO` (이미 다른 곳으로 이동됨). 유지: `ENCRYPTION_KEY`/`TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`/`LOG_LEVEL`/`TRADING_LOG_PATH` (전부 살아있는 설정), `_load_all_dotenv`/`_resolve_env_file`/`get_settings` (살아있는 함수). (4) `backend/app/web/routes/stock_classification.py` (372줄→369줄) — 죽은 변수 `_DATA_DIR` 및 `from pathlib import Path` import 제거 (사용처 없음) + 주석 줄 69 `get_merged_sector` 참조를 `get_merged_sectors_batch`로 정정 (B-21-c에서 `get_merged_sector` 함수 제거됨, 주석-코드 불일치 해소). 유지: `broadcast_stock_classification_changed`/`_trigger_recompute`/`_maybe_warning`/8개 엔드포인트 (전부 살아있는 경로). **사전조사(규칙 0-2)**: (1) 의존성 — 4개 파일 683줄 전체 조사. `verify_token` (auth.py:30)은 deps.py/ws.py 주석 제거 후 호출처 0건 → 별도 세션 대상 (B-22-b 범위 외, HANDOVER.md 미해결 문제에 기록). `sector_data_provider.py:154`·`sector_calculator.py:28/169`의 `get_merged_sector` 참조 주석도 별도 세션 대상. P2/P5/P10/P12/P13/P19/P21/P23 위반 없음. (2) 영향범위 — 백엔드 4파일. UI 영향 없음. 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P16(살아있는 경로) + P24(단순성 — deps 26→19, ws 206→200, config 79→68, classification 372→369) + Code Removal Rules(제거된 `get_merged_sector` 참조 주석 정리). (4) 공통 자산 — 해당 없음 (삭제 작업). **검증**: py_compile OK + ruff OK + pytest 111 passed(관련 3파일) + 2844 passed(전체, 이전 2844와 동일 — 회귀 없음) + 런타임 기동 96ms 정상 (RuntimeWarning 0건, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결 정상) + 잔존 프로세스 0건. **B-22-b 완료**: deps+ws+config+stock_classification 주석 dead code 전부 해결. **B-22 분할**: B-22-a(ws_manager dead code 3건) 완료 → B-22-b(주석 dead code 4건) 완료 → B-22-c(settings.py silent except+status.py dead variables+P24 점검) 잔여.
- **2026-07-22 (이전)**: B-22-a 세션 — ws_manager.py dead code 3건 제거 (P16). **수정 파일 3개** (백엔드 2파일 + 테스트 1파일): (1) `backend/app/web/ws_manager.py` (351줄→321줄) — `_send_realdata_immediate` (real-data 즉시 전송, production 호출 0건, `broadcast()`가 `_send_realdata_encoded` 사용), `_send` (단일 전송, production 호출 0건, `send_to()`가 담당), `broadcast_threadsafe` (스레드 안전 브로드캐스트, production 호출 0건, market_close_pipeline은 `_loop.call_soon_threadsafe` 사용) 제거. 유지: `register`/`unregister`/`set_active_page`/`clear_active_page`/`get_active_pages`/`set_subscribed_fids`/`clear_subscribed_fids`/`_stamp`/`_send_broadcast`/`_send_realdata_encoded`/`broadcast_to_pages`/`_send_to_pages_immediate`/`broadcast`/`send_to`/`_send_sigterm`/`close_all`/`_send_initial_data_on_connect`/`client_count` (전부 production 호출). (2) `backend/app/services/market_close_pipeline.py` — `_broadcast_confirmed_progress` docstring에서 `broadcast_threadsafe()` 참조 제거 (실제로는 `_loop.call_soon_threadsafe()` 사용). (3) `backend/tests/test_web_ws_manager.py` (795줄→723줄) — `TestSendRealdataImmediate` 2건 + `TestBroadcastThreadsafe` 2건 + `TestSend` 2건 = 6건 제거. **사전조사(규칙 0-2)**: (1) 의존성 — 14개 파일 1918줄 전체 조사. P2/P5/P10/P12/P13/P19/P21/P23 위반 없음. P16 위반 7건 + P20 위반 1건 + 잠재적 2건 발견. (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향 없음. 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P16(살아있는 경로) + P24(단순성 — ws_manager 351→321, test 795→723). (4) 공통 자산 — 해당 없음 (삭제 작업). **검증**: py_compile OK + pytest 69 passed(ws_manager) + 2844 passed(전체, 이전 2850 - 삭제 6건 = 2844, 회귀 없음) + 런타임 기동 363ms 정상 (RuntimeWarning 0건, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결 정상) + 잔존 프로세스 0건. **B-22 분할**: B-22-a(ws_manager dead code 3건) 완료 → B-22-b(deps.py+ws.py+config.py+stock_classification.py 주석 dead code 4건) 잔여 → B-22-c(settings.py silent except+status.py dead variables+P24 점검) 잔여.
- **2026-07-22 (이전)**: B-21-c 세션 — stock_classification_data + sector_mapping + sector_stock_cache dead code 제거 (P16). **수정 파일 5개** (백엔드 3파일 + 테스트 2파일): (1) `backend/app/core/stock_classification_data.py` (238줄→233줄) — `load_custom_data_readonly` 제거 (하위 호환성용 빈 데이터 로드, production 호출 0건, 테스트만 참조). 유지: `StockClassificationData` (load_custom_data가 반환, stock_classification.py:194·ws.py:72에서 .sectors/.stock_moves 속성 사용 — 살아있는 경로), `load_custom_data` (stock_classification.py:171·ws.py:52 호출), `update_sector_in_cache`/`rename_sector`/`create_sector`/`delete_sector`/`move_stock`/`sync_sector_from_custom_sectors` (전부 production 호출). (2) `backend/app/core/sector_mapping.py` (99줄→70줄) — `get_merged_sector` 제거 (단일 종목 업종 조회, production 호출 0건, `get_merged_sectors_batch` 배치 버전으로 대체됨, 테스트만 9회 참조) + `get_merged_sectors_batch` docstring에서 "1353회 개별 await get_merged_sector() 호출을 1회 await로 대체" 역사적 언급 제거. 유지: `get_merged_sectors_batch` (sector_data_provider·engine_sector_confirm·sector_filter 호출), `get_merged_all_sectors` (stock_classification·ws 호출). (3) `backend/app/core/sector_stock_cache.py` (140줄→125줄) — `save_filter_summary_meta_cache` 제거 (filter_summary 메타 저장, production 호출 0건). 유지: `load_filter_summary_meta_cache` (app.py:74에서 await 호출 — 살아있는 경로), `assemble_filter_summary` (stock_classification·ws 호출), `save/load/clear_pending_settings` (settings·engine_lifecycle 호출), `_init_cache_table` (내부 헬퍼, 살아있는 함수들에서 호출). (4) `backend/tests/test_stock_classification_data.py` (298줄→292줄) — `test_load_custom_data_readonly_returns_empty` 1건 제거 + import `load_custom_data_readonly` 제거. (5) `backend/tests/test_sector_mapping.py` (304줄→185줄) — `TestGetMergedSector` 클래스 9건 제거 + docstring "3함수 검증"→"2함수 검증" + `get_merged_sector` 설명 줄 제거. **사전조사(규칙 0-2)**: (1) 의존성 — subagent 병렬 조사로 4개 파일 883줄 전체 추적. **subagent 분류 오류 2건 정정**: (a) `StockClassificationData` — DEAD로 분류했으나 `load_custom_data`가 반환하는 살아있는 경로 (호출처에서 .sectors/.stock_moves 속성 사용) → 유지. (b) `load_filter_summary_meta_cache` — DEAD로 분류했으나 app.py:74에서 await 호출 → 유지 (제거 후 복구). trading_calendar.py: DEAD CODE 없음 (모든 심볼이 ALIVE 또는 ALIVE 체인의 INTERNAL) → B-21-c 대상 아님. P20 위반 2건(줄 311 폴백, 줄 211 silent except) + P24 위반 1건(_compute_holidays 104줄) + P10 위반 1건(_KST 중복 정의)은 별도 세션 대상. (2) 영향범위 — 백엔드 3파일 + 테스트 2파일. UI 영향 없음. 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P16(살아있는 경로) + P24(단순성 — classification 238→233, mapping 99→70, cache 140→125, test_classification 298→292, test_mapping 304→185). (4) 공통 자산 — 해당 없음 (삭제 작업). **검증**: py_compile + ruff OK + pytest 59 passed(관련 3파일) + 2850 passed(전체, 이전 2860 - 삭제 테스트 10건 = 2850, 회귀 없음) + 런타임 기동 123ms 정상 (RuntimeWarning 0건, 1356종목 로드·업종순위 재계산 완료·필터 요약 메타 캐시 로드 완료·LS/키움 토큰 발급·LS 연결 정상) + 잔존 프로세스 0건. **B-21-c 완료**: classification+mapping+cache dead code 전부 해결. **B-21 전체 완료**: B-21-a(journal 12건) + B-21-b(logger 1건+encryption 3건) + B-21-c(classification 1건+mapping 1건+cache 1건) = 총 19건 dead code 제거. 다음: B-22 (Web API 계층, 14파일).
- **2026-07-22 (이전)**: B-21-b 세션 — logger.py + encryption.py dead code 제거 (P16). **수정 파일 4개** (백엔드 2파일 + 테스트 2파일): (1) `backend/app/core/logger.py` (426줄→415줄) — `get_logger` 함수 제거 (deprecated, production 호출처 0건, test_logger.py만 참조). 유지: `setup_console_intercept`(main.py 호출), `setup_loguru`(logging_config.py 호출), `stop_file_writers`(web/app.py 호출), `log_progress`/`log_progress_end`(kiwoom_stock_rest·market_close_pipeline 호출), `InterceptHandler`(표준 logging→loguru 리다이렉트), `_async_file_writer_loop`(파일 쓰기 태스크), `_info_file_sink`/`_stdout_sink`(로그 싱크). `except: pass` 5건(200/203/299/328/344줄) 유지 — 로그 싱크 I/O 실패 시 앱 크래시 방지용 정당한 처리 (P20 위반 아님, `logger.warning`으로 바꾸면 무한 재귀). `_async_file_writer_loop` 78줄 유지 — 단일 응집 asyncio 태스크 루프, 분할 시 상태 공유 복잡도 증가 (P24 "불필요한 추상화 금지"). (2) `backend/tests/test_logger.py` (423줄→401줄) — `TestGetLogger` 클래스 2건 제거 + import `get_logger` 제거 + docstring "get_logger: deprecated 경고" 제거. (3) `backend/app/core/encryption.py` (87줄→59줄) — `SENSITIVE_KEYS`/`encrypt_sensitive`/`decrypt_sensitive` 제거 (production 호출처 0건, test_encryption.py만 참조). 유지: `_get_fernet`/`encrypt_value`/`decrypt_value` (engine_settings·telegram_bot·settings_file 호출). "Fernet 없으면 평문 반환" 패턴 유지 — 보안 설계 결정, 규칙 0-4(핵심 로직 변경 시 UI 기준 설명 + 승인) 해당으로 별도 보고 후 사용자 승인 필요 (미해결 문제 기록). (4) `backend/tests/test_encryption.py` (270줄→145줄) — `TestSensitiveKeys` 4건 + `TestEncryptSensitive` 5건 + `TestDecryptSensitive` 5건 + `TestSensitiveRoundtrip` 1건 = 15건 제거 + import `SENSITIVE_KEYS`/`encrypt_sensitive`/`decrypt_sensitive` 제거 + docstring "encrypt_sensitive/decrypt_sensitive는 SENSITIVE_KEYS 필드만 처리 확인" 제거. **사전조사(규칙 0-2)**: (1) 의존성 — grep 전체 코드베이스. logger.py: `get_logger` production 호출 0건 (테스트만). encryption.py: `encrypt_sensitive`/`decrypt_sensitive`/`SENSITIVE_KEYS` production 호출 0건 (테스트만). lock_manager.py: **태스크 노트 "전체 파일 dead code" 주장은 오류** — main.py가 5개 심볼 활발히 사용 (`acquire_lock`/`read_lock_pid`/`format_duplicate_message`/`register_cleanup`/`LOCK_FILE_PATH`), `release_lock`도 `register_cleanup` 내부 cleanup 핸들러에서 호출 → 살아있는 경로, dead code 없음. (2) 영향범위 — 백엔드 2파일 + 테스트 2파일. UI 영향 없음. 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P16(살아있는 경로) + P24(단순성 — logger 426→415, encryption 87→59, test_logger 423→401, test_encryption 270→145). (4) 공통 자산 — 해당 없음 (삭제 작업). **검증**: py_compile + ruff OK + pytest 57 passed(logger+encryption) + 2860 passed(전체, 이전 2877 - 삭제 테스트 17건 = 2860, 회귀 없음) + 런타임 기동 126ms 정상 (RuntimeWarning 없음, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결 정상·종료 시 잠금 파일 삭제 정상) + 잔존 프로세스 0건. **B-21-b 완료**: logger.py + encryption.py dead code 전부 제거. lock_manager.py는 살아있는 경로 확인 (태스크 노트 오류 정정). **B-21 분할**: B-21-a(journal.py) 완료 → B-21-b(logger.py+encryption.py) 완료 → B-21-c(trading_calendar.py+stock_classification_data.py+sector_mapping.py+sector_stock_cache.py) 잔여.
- **2026-07-22 (이전)**: B-21-a 세션 — journal.py dead code 12개 함수 제거 (P16). **수정 파일 2개** (백엔드 1파일 + 테스트 1파일): (1) `backend/app/core/journal.py` (324줄→123줄) — production 호출처 0건인 함수 12개 제거: `_get_conn`/`_ensure_loaded`/`_migrate_from_json` (no-op 잔재, 메모리 전용 변경 후 의미 없음), `close_db_connection` (no-op, database.py에 동명 함수 존재), `_read_all_entries` (삭제된 replay_journal/get_journal_stats에서만 호출), `_perform_compaction` (내부에서도 호출 안 됨), `record_fill_event` (체결 기록, production 호출 0건), `oms_get_pending_orders`/`oms_update_order_status`/`oms_get_next_seq`(+`_next_seq`) (OMS API, production 호출 0건), `replay_journal`/`clear_journal`/`get_journal_stats` (장애 복구/초기화/통계, production 호출 0건). 유지: `start/stop_consumer_task` (app.py 호출, no-op이지만 살아있는 경로), `record_settings_change` (settings_store.py 호출), `record_order_request` (trading.py 호출), `_append_entry` (내부 호출), `JournalEventType`/`JournalEntry` (데이터 클래스). 부수 정리: docstring에서 삭제된 기능(체결 기록, 장애 복구 재생) 언급 제거, `Callable` import 제거 (사용처 없음). (2) `backend/tests/test_journal.py` (502줄→169줄) — 삭제된 12개 함수 관련 테스트 클래스 전체 제거 (TestNoopFunctions 4건 중 3건, TestReadAllEntries 3건, TestPerformCompaction 2건, TestRecordFillEvent 1건, TestOmsGetPendingOrders 3건, TestOmsUpdateOrderStatus 3건, TestOmsGetNextSeq 2건, TestReplayJournal 5건, TestClearJournal 2건, TestGetJournalStats 2건 = 26건). 유지: JournalEventType/JournalEntry/_append_entry/record_settings_change/record_order_request/start_consumer_task/stop_consumer_task 테스트. docstring에서 삭제된 API 언급 제거, import 정리. **사전조사(규칙 0-2)**: (1) 의존성 — subagent 병렬 조사로 11개 파일 1951줄 전체 추적. journal.py 19개 함수 중 12개 dead code (production 호출처 0건, 테스트만 참조). `journal.`/`_journal.` 참조 5건: app.py(start/stop_consumer_task), settings_store.py(record_settings_change), trading.py(record_order_request) — 유지 대상 3개만 production 호출. (2) 영향범위 — 백엔드 1파일 + 테스트 1파일. UI 영향 없음 (저널은 백엔드 내부 기능). 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P16(살아있는 경로 — 호출되지 않는 dead code 제거) + P24(단순성 — 324줄→123줄, 502줄→169줄). (4) 공통 자산 — 해당 없음 (삭제 작업). **검증**: py_compile + ruff OK + pytest 34 passed(journal+web_app) + 138 passed(settings_store+trading) + 2877 passed(전체, 이전 2903 - 삭제 테스트 26건 = 2877, 회귀 없음) + 런타임 기동 192ms 정상 (RuntimeWarning 없음, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결 정상) + 잔존 프로세스 0건. **B-21-a 완료**: journal.py dead code 12개 함수 전부 제거. **B-21 분할**: B-21-a(journal.py) 완료 → B-21-b(logger.py+encryption.py+lock_manager.py) → B-21-c(trading_calendar.py+stock_classification_data.py+sector_mapping.py+sector_stock_cache.py) 순서.
- **2026-07-22 (이전)**: 전역매매설정 "리스크 매니저" → "매매 안전장치" 용어 변경 + 마스터 OFF 시 하위 일괄 비활성화 (P21/P23). **수정 파일 1개** (프론트엔드 1파일): `frontend/src/pages/general-settings.ts` +30/-20. (1) 용어 변경 — 섹션 제목 "전역매매설정 (리스크 매니저)" → "전역매매설정 (매매 안전장치)", 마스터 토글 라벨 "자동 매매 중단 기능" → "매매 안전장치", 설명 문구 "리스크 매니저 OFF 시 모든 조건 비활성화" → "매매 안전장치 OFF 시 모든 조건이 적용되지 않습니다", 매수/매도 차단 라벨 "리스크 조건 충족 시" → "안전장치 조건 충족 시", 코드 주석 3곳 동일 변경 (P23 용어 일관성). (2) 하위 일괄 비활성화 — 하위 7개 컨트롤(일일 손실 한도·손실률, 일일 수익 한도·수익률, 연속 손실 횟수, 매수 차단, 매도 차단)을 `riskManagerChildren` 컨테이너 div로 래핑. 마스터 토글 OFF 시 `setDisabled(riskManagerChildren, true)` → 회색(opacity 0.4) + 클릭 차단, ON 시 다시 활성화. 저장 실패 시 롤백에도 setDisabled 복원 추가. (3) `syncFromSettings` — `risk_manager_on` 상태에 따라 `setDisabled(riskManagerChildren, !r.risk_manager_on)` 일괄 적용. (4) cleanup — `riskManagerChildren = null` 추가. **사전조사(규칙 0-2)**: (1) 의존성 — 단일 파일 내 변경, 백엔드/테스트 영향 없음. (2) 영향범위 — 프론트엔드 1파일. 핵심 매매 로직 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P21(OFF 시 비활성화로 사용자 혼란 방지) + P23(용어 통일, 기존 `setDisabled()` 공통 함수 재사용) + P24(컨테이너 1개로 감싸 setDisabled 1회 호출). (4) 공통 자산 — `setDisabled()`(`components/common/ui-styles.ts:591`) 기존 함수 재사용. **검증**: npm run typecheck 통과 + npm run build 성공 (1.47s) + 잔존 "리스크 매니저"/"리스크 조건" 0건 확인.
- **2026-07-22 (이전)**: 일일 손실 한도 개선 3단계 — 프론트엔드 일일 손실 한도 행을 토글 + `createMoneyInput` 패턴으로 교체 (P21/P23). **수정 파일 1개** (프론트엔드 1파일): `frontend/src/pages/general-settings.ts` 4곳 수정. (1) 60줄 변수 선언 — `dailyLossInput` 타입 `createNumInput`→`createMoneyInput`, 신규 변수 `dailyLossToggle`/`dailyLossControls` 추가 (기존 `dailyLossRateToggle`/`dailyLossRateControls` 패턴과 일치). (2) 526-556줄 일일 손실 한도 행 교체 — 기존 `createNumInput` + `createSettingRow('일일 손실 한도 (원, 음수)', ...)` 단순 행 → `createMoneyInput`(value:-500000, step:10000, min:-10억, max:0, 음수 콤마 포맷) + `createToggleLabelControlsRow`(labelText:'일일 손실 한도 (원)', toggleOn:true, onToggle에서 `daily_loss_limit_on` 저장). 토글 기본 ON은 2단계 백엔드 기본값 True와 일치. (3) 1223-1229줄 로드 시 상태 반영 — `const lossOn = r.daily_loss_limit_on !== false` (기본 ON), `dailyLossToggle?.setOn(lossOn)`, `dailyLossControls` disable 처리 추가. (4) 1411-1413줄 cleanup — `dailyLossToggle`/`dailyLossControls` null 할당 추가. 부수 정리: 8줄 `createSettingRow` import 제거 (교체 후 사용처 0건, TS6133 해결). **사전조사(규칙 0-2)**: (1) 의존성 — `dailyLossInput` 변수 4곳 참조(60/527/1226/1412줄), `createMoneyInput`/`createToggleLabelControlsRow` 이미 import 됨. `daily_loss_limit_on` 키 — `vals`(Record<string, unknown>) 동적 할당, 타입 변경 불필요. (2) 영향범위 — 프론트엔드 1파일. 백엔드/테스트 변경 없음 (2단계에서 백엔드 연결 완료). 핵심 매매 로직 변경 아님 → 규칙 0-4/0-5 미해당. (3) 아키텍처 부합 — P21(토글로 ON/OFF 상태 화면 표시, 기존 항상 실행이지만 화면에 토글 없어 사용자 인지 불가 → 개선) + P23(다른 4개 한도 행과 동일 `createToggleLabelControlsRow` 패턴 통일, 금액 입력 `createMoneyInput` 통일). (4) 공통 자산 — `createMoneyInput`(1단계 음수 지원 추가)/`createToggleLabelControlsRow` 기존 공통 컴포넌트 재사용, 신규 구현 없음. **검증**: npm run build 성공 (tsc + vite, 63 모듈 변환 OK, 타입 오류 없음) + 코드 검토 (변수 선언/행 생성/로드 시 상태 반영/cleanup 4곳 일관 적용, 기존 일일 수익 한도 행 패턴과 동일 구조). **일일 손실 한도 개선 1+2+3단계 전부 완료**.
- **2026-07-22 (이전)**: 일일 손실 한도 개선 1+2단계 — `createMoneyInput` 음수 지원 추가 + 백엔드 `daily_loss_limit_on` 키 추가 (P21/P23). **수정 파일 7개** (프론트엔드 2파일 + 백엔드 3파일 + 테스트 2파일): (1) `frontend/src/components/common/setting-row.ts` (1단계) — `createMoneyInput`에 `min`/`max` 옵션 추가 + `fmtMoney()` 헬퍼로 음수도 콤마 포맷 (`-500000` → `-500,000`). 기존 `currentValue > 0 ? toLocaleString() : '0'` 조건을 `v === 0 ? '0' : v.toLocaleString()`로 변경하여 음수 지원. 스핀 버튼 ▼는 `Math.max(0, ...)` → `Math.max(minVal, ...)`로 변경. 기존 양수 사용처(`daily_profit_limit`, `max_daily_total_buy_amt`)는 `min`/`max` 미지정 시 기본값 `0`/`Infinity`로 동작 동일. (2) `backend/app/core/settings_defaults.py` (2단계) — `daily_loss_limit_on: True` 추가 (기본 ON, 기존 항상 실행 동작 유지). (3) `backend/app/core/engine_settings.py` — `daily_loss_limit_on: bool(merged.get("daily_loss_limit_on", True))` 캐스팅 추가. (4) `backend/app/services/risk_manager.py` — `_sync_thresholds()`에 `self.daily_loss_limit_on = bool(cache.get("daily_loss_limit_on", True))` 추가. `check_buy_order_allowed()` 127줄 `if today_pnl <= self.daily_loss_limit:` → `if self.daily_loss_limit_on and today_pnl <= self.daily_loss_limit:` (OFF 시 매수 손실 한도 체크 스킵). `check_sell_order_allowed()` 203줄 동일 적용 (OFF 시 매도 손실 한도 체크 스킵). (5) `frontend/src/types/index.ts` — `daily_loss_limit_on?: boolean` 타입 추가. (6) `backend/tests/test_risk_manager.py` — fixture에 `rm.daily_loss_limit_on = True` 추가 + `test_daily_loss_limit_off_skips_check` 신규 테스트 (OFF 시 한도 초과여도 매수 허용 검증) + `test_sync_defaults_when_keys_missing`에 `daily_loss_limit_on is True` 검증 추가. (7) `backend/tests/test_engine_settings.py` — `test_risk_fields_defaults`에 `daily_loss_limit_on is True` 검증 추가. **사전조사(규칙 0-2)**: (1) 의존성 — `createMoneyInput` 사용처 2개(`daily_profit_limit`, `max_daily_total_buy_amt`) 모두 양수 전용, `min`/`max` 기본값으로 영향 없음. `daily_loss_limit` 사용처 2개(`risk_manager.py` 매수 127줄/매도 203줄). (2) 영향범위 — 프론트엔드 2파일 + 백엔드 3파일 + 테스트 2파일. (3) 아키텍처 부합 — P21(기본값 True로 기존 동작 유지, 사용자 모르는 변경 없음) + P23(`daily_loss_limit_on`이 다른 `_on` 키와 동일 패턴, `createMoneyInput`으로 금액 입력 통일). (4) 공통 자산 — `createMoneyInput` 기존 컴포넌트 확장으로 재사용. **검증**: npm run build 성공 + pytest 2904 passed(전체) + 런타임 기동 확인 (DEFAULT_SETTINGS/build_engine_settings_dict/RiskManager._sync_thresholds 모두 기본값 True, 오버라이드 False 정상 동작) + 잔존 프로세스 0건.
- **2026-07-22 (이전)**: B-20 세션 — 알림 Telegram/Notification 3개 파일 3건 해결 (P16/P21). **수정 파일 3개** (백엔드 2파일 + 테스트 1파일): (1) `backend/app/services/telegram_bot.py` (527줄→505줄) — `stop` 메서드 제거(B20-01, P16): 동기 취소 전용 메서드, production 호출처 0건(테스트 4건만), docstring에 "엔진 종료 경로는 stop_async를 사용한다" 명시됨, stop_async가 유일한 종료 경로. `get_poll_ok_age_sec` 메서드 제거(B20-02, P16): 마지막 폴링 성공 후 경과 초 반환, production 호출처 0건(테스트 2건만), 모니터링용으로 추가되었으나 현재 사용되지 않음. `_last_poll_ok_mon` 필드 제거(B20-02 연동): `__init__`/`stop_async`/`_poll_one`에서 `_last_poll_ok_mon` 참조 3건 모두 제거. (2) `backend/app/web/app.py` — shutdown 경로에 `NotificationWorker.get_instance().shutdown()` 호출 추가(B20-03, P21): `stop_engine()` 이후에 알림 워커 종료 호출 추가, 엔진이 더 이상 알림을 생성하지 않는 시점에 큐 잔량 처리(최대 10초 대기 후 취소), 종료 시 큐에 남은 알림 누락 방지. (3) `backend/tests/test_telegram_bot.py` (1297줄→1240줄) — `TestStop` 4건 제거(B20-01), `TestGetPollOkAgeSec` 2건 제거(B20-02), `test_stop_async_clears_poll_ok_mon` 1건 제거(B20-02 연동), `test_defaults`의 `_last_poll_ok_mon` assertion 제거(B20-02 연동), `test_poll_one_success`의 `_last_poll_ok_mon` assertion 제거(B20-02 연동), `import time` 제거(사용처 없음). **사전조사(규칙 0-2)**: (1) 의존성 — subagent 병렬 조사로 3개 파일 28개 함수 전체 추적. `telegram_bot.py` 19개 함수 중 2개 dead code 후보(`stop`, `get_poll_ok_age_sec`), 나머지는 내부 호출 체인으로 살아있는 경로. `telegram.py` 2개 함수 모두 살아있는 경로. `notification_worker.py` 7개 메서드 중 `shutdown`이 production에서 호출되지 않음(app.py shutdown 경로 누락) → P21 관점에서 기능 보완 필요. (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향 없음 (알림은 백엔드 내부 기능, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 2건 B20-01/B20-02) + P21(종료 시 알림 누락 방지 B20-03). 핵심 매매 로직/수신률/업종 점수/매도 조건 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — 제거 대상 2건은 dead code이므로 공통 자산 재사용 해당 없음. **검증**: py_compile 5파일 OK + ruff OK + pytest 146 passed(telegram_bot/telegram/notification_worker) + 2903 passed(전체, B-19 2910 - 제거 테스트 7건 = 2903, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 440ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-20 완료**: 알림 Telegram/Notification 3개 파일 3건 전부 해결. 다음: B-21 (기타 Core 유틸, 11개 파일 1951줄).
- **2026-07-22 (이전)**: B-19 세션 — WS 구독 제어 및 업종 데이터 제공자 2개 파일 4건 해결 (P16/P20/P24). **수정 파일 6개** (백엔드 4파일 + 테스트 2파일): (1) `backend/app/services/ws_subscribe_control.py` (262줄→230줄) — `stop_industry` 함수 제거(B19-01, P16): body가 `return {"ok": True, "status": ...}`만 있는 하위 호환용 스텁, production 호출처 2건(ws_subscribe.py)이 스텁을 호출하지만 아무 동작 없음. `on_setting_changed` 함수 제거(B19-02, P16): `key == "quote_auto_subscribe"` 브랜치가 `pass`만 수행하는 no-op, 호출처 1건(engine_service.py)에서 `schedule_engine_task`로 예약하지만 의미 없는 동작. 섹션 주석 "설정 변경 즉시 반영"도 함께 제거. (2) `backend/app/services/engine_service.py` — `_apply_ws_subscribe_control_change` 함수 제거(B19-02 연동, P16): `on_setting_changed`를 `schedule_engine_task`로 예약하는 유일한 호출처, `on_setting_changed`가 no-op이므로 이 함수도 no-op. 68행 호출처도 함께 제거. (3) `backend/app/web/routes/ws_subscribe.py` — sector/industry 그룹 해지 시 `stop_industry()` 스텁 호출 대신 `get_subscribe_status()` 직접 반환(B19-01 연동). (4) `backend/app/services/sector_data_provider.py` (309줄→282줄) — `get_buy_targets_sector_stocks` 루프 중복 제거(B19-03, P24): buy_targets 루프(25행)와 blocked_targets 루프(25행)가 19개 필드 거의 동일 반복 → 공통 헬퍼 `_build_target_entry(bt)` 추출, 두 루프에서 호출. `_on_filter_settings_changed` try/except 중복 제거(B19-04, P20/P24): `recompute_sector_summary_now`가 내부에서 이미 try/except 처리하는데 `_on_filter_settings_changed`도 try/except로 감싸 중복 → 외부 try/except 제거, 직접 `await recompute_sector_summary_now()` 호출. (5) `backend/tests/test_web_ws_routes.py` — `stop_industry` mock 4건 수정/제거(B19-01): `test_sector_stops_industry`/`test_industry_stop_success`는 `get_subscribe_status` 직접 반환 검증으로 수정, `test_industry_stop_failure`/`test_sector_stop_industry_failure`는 스텁 제거로 실패 케이스 불가능 → 제거. (6) `backend/tests/test_sector_data_provider.py` — `test_exception_logged` 1건 제거(B19-04): 제거된 try/except 검증 테스트. **사전조사(규칙 0-2)**: (1) 의존성 — subagent 병렬 조사로 2개 파일 19개 함수 전체 추적. `ws_subscribe_control.py` 12개 함수 중 2개 no-op 후보(`stop_industry` 스텁, `on_setting_changed` pass 브랜치) + `on_setting_changed` 연쇄로 `engine_service.py`의 `_apply_ws_subscribe_control_change`도 no-op. `sector_data_provider.py` 7개 함수 모두 살아있는 경로(dead code 없음), P24 중복 2건(buy_targets/blocked_targets 루프, try/except 중복). (2) 영향범위 — 백엔드 4파일 + 테스트 2파일. UI 영향 없음 (모두 내부 구조 개선, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(no-op 2건 B19-01/B19-02) + P20(try/except 중복 B19-04) + P24(루프 중복 B19-03, try/except 중복 B19-04). 핵심 매매 로직/수신률/업종 점수/매도 조건 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — 제거/개선 대상이 명확한 패턴 중복이므로 공통 자산 재사용 해당 없음. **검증**: py_compile 5파일 OK + ruff OK + pytest 62 passed(ws_subscribe/sector_data/web_ws_routes/engine_service) + 2910 passed(전체, B-18 2913 - 제거 테스트 3건 = 2910, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 152ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·업종순위 재계산 완료·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-19 완료**: WS 구독 제어 및 업종 데이터 제공자 2개 파일 4건 전부 해결. 다음: B-20 (알림 Telegram/Notification, 3개 파일 668줄).
- **2026-07-22 (이전)**: B-18 세션 — 스케줄러 및 장마감 파이프라인 3개 파일 6건 해결 (P16/P20). **수정 파일 4개** (백엔드 2파일 + 테스트 2파일): (1) `backend/app/services/data_manager.py` (237줄→45줄) — `get_account_profit_rate` 함수 제거(B18-01, P16): 계좌 수익률 조회(kt00018), production 호출처 0건(테스트 7건만). `get_main_account_info` 함수 제거(B18-02, P16): 계좌 메인 정보 조회(kt00001), production 호출처 0건(테스트 7건만). `_load_broker_settings` 함수 제거(B18-06 연동, P16/P20): `get_main_account_info`에서만 호출되어 함께 제거, silent `except Exception: return None`도 함께 해결. `httpx`/`effective_trade_mode` import 제거(사용처 없음). 파일 헤더 docstring "계좌 수익률, 종목명 조회"→"종목명 조회"로 정리. (2) `backend/app/services/daily_time_scheduler.py` (1506줄→1443줄) — `_freeze_krx_amt29_baseline` 함수 제거(B18-03, P16): body가 `pass`만 있는 stub, production 호출처 0건(테스트 1건만), 섹션 주석 "ka10001 확정 데이터 갱신"도 함께 제거. `_apply_detail_to_entry` 함수 제거(B18-04, P16): ka10086 응답 처리 헬퍼, production 호출처 0건(테스트 6건만). `_fire_ws_disconnect_only` + `_ws_disconnect_only` 함수 제거(B18-05, P16): WS 구독 해제 전용 트리거, `_fire_ws_disconnect_only` production 호출처 0건(테스트 1건만), `_ws_disconnect_only`도 `_fire_ws_disconnect_only`에서만 호출되어 함께 제거(테스트 1건). (3) `backend/tests/test_data_manager.py` (504줄→110줄) — `TestLoadBrokerSettings` 4건 제거(B18-06), `TestGetAccountProfitRate` 7건 제거(B18-01), `TestGetMainAccountInfo` 7건 제거(B18-02), import에서 `get_account_profit_rate`/`get_main_account_info`/`_load_broker_settings`/`AsyncMock` 제거(사용처 없음). 파일 헤더 docstring "계좌 수익률, 종목명 조회, 브로커 설정 로드 검증"→"종목명 조회, REST 기본 주소 조회 검증"으로 정리. (4) `backend/tests/test_daily_time_scheduler.py` (2337줄→2202줄) — `TestFireWrappers` 1건 제거(B18-05), `TestWsDisconnectOnly` 1건 제거(B18-05), `TestFreezeKrxAmt29Baseline` 1건 제거(B18-03), `TestApplyDetailToEntry` 6건 제거(B18-04), import에서 `_fire_ws_disconnect_only`/`_ws_disconnect_only`/`_freeze_krx_amt29_baseline`/`_apply_detail_to_entry` 제거. **사전조사(규칙 0-2)**: (1) 의존성 — subagent 병렬 조사로 3개 파일 75개 함수 전체 추적. `market_close_pipeline.py` 18개 함수 모두 살아있는 경로(dead code 없음, _step6은 의도적 누락 — 5단계→7단계 직접 진행). `daily_time_scheduler.py` 51개 함수 중 3개 dead code 후보(`_freeze_krx_amt29_baseline`/`_apply_detail_to_entry`/`_fire_ws_disconnect_only`) + `_ws_disconnect_only`도 `_fire_ws_disconnect_only`에서만 호출되어 함께 dead code(subagent가 잘못 "살아있는 경로"로 판정한 것을 직접 grep으로 바로잡음). `data_manager.py` 6개 함수 중 3개 dead code 후보(`get_account_profit_rate`/`get_main_account_info`/`_load_broker_settings`). (2) 영향범위 — 백엔드 2파일 + 테스트 2파일. UI 영향 없음 (모두 미사용 함수 제거, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 6건 B18-01~05 + B18-06 연동) + P20(silent except 1건 B18-06, 제거로 해결). 핵심 매매 로직/수신률/업종 점수/매도 조건 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — 제거 대상 6건 모두 사용처 없는 dead code이므로 공통 자산 재사용 해당 없음. **추가 발견(이번 세션 범위 외)**: `market_close_pipeline.py`(1407줄)/`daily_time_scheduler.py`(1443줄) P24 파일 길이 초과 — 파일 분할은 대형 작업이므로 별도 세션으로 이월. **검증**: py_compile 5파일 OK + ruff OK + pytest 285 passed(market_close/scheduler/data_manager) + 2913 passed(전체, B-17 2941 - 제거 테스트 28건 = 2913, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 95ms 정상 기동, RuntimeWarning/Traceback 없음, 168ms 연산 준비 완료·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-18 완료**: 스케줄러 및 장마감 파이프라인 3개 파일 6건 전부 해결. 다음: B-19 (WS 구독 제어 및 업종 데이터 제공자, 2개 파일 572줄).
- **2026-07-22 (이전)**: B-17 세션 — Domain 계층 6개 파일 3건 해결 (P16/P24). **수정 파일 9개** (백엔드 4파일 + 테스트 5파일): (1) `backend/app/domain/sector_calculator.py` (213줄→195줄) — `compute_full_sector_summary` 미사용 파라미터 16개 제거(B17-01, P16/P24): `latest_index`, `sort_keys`, `block_rise_pct`, `block_fall_pct`, `min_strength`, `max_sectors`, `high_5d_cache`, `orderbook_cache`, `program_net_buy_cache`, `boost_high_on`/`boost_high_score`, `boost_order_ratio_on`/`boost_order_ratio_pct`/`boost_order_ratio_score`, `boost_program_net_buy_on`/`boost_program_net_buy_score` — 과거 이 함수가 매수 타겟 생성까지 수행했던 시절의 잔재, 현재는 `build_buy_targets_from_settings`가 별도 수행. 시그니처 30→14 파라미터로 감소. `Literal` import 제거(더 이상 사용되지 않). docstring에 "매수 타겟 생성은 build_buy_targets_from_settings에서 별도 수행" 명시. (2) `backend/app/services/sector_data_provider.py` — `get_sector_summary_inputs()` 반환에서 `latest_index` 키 제거(B17-01 연동): 빈 dict `{}` 반환, `compute_full_sector_summary`에서 `latest_index` 파라미터 제거로 사용처 없음. (3) `backend/app/core/stock_filter.py` (322줄→269줄) — `is_excluded_with_ka10100` 함수 제거(B17-02, P16): production 호출처 0건(테스트 11건만), `is_excluded`가 production 사용 중 — B15-05(`get_spec` 제거)/B16-07(`call_tr` 제거)과 동일 패턴. (4) `backend/app/domain/models.py` (81줄→65줄) — `SortKey`/`_SORT_LABEL`/`DEFAULT_SORT_KEYS` 상수 제거(B17-03, P16): 외부 import 0건, 파일 내 참조도 없음(정의만 존재) — `_SORT_LABEL`은 `SortKey` 타입 힌트로만 사용, `DEFAULT_SORT_KEYS`는 정의만 있고 사용처 없음. `Literal` import 제거. (5) `backend/tests/test_stock_filter.py` — `TestIsExcludedWithKa10100` 클래스 9건 제거(B17-02), import에서 `is_excluded_with_ka10100` 제거. (6) `backend/tests/test_sector_calculator.py` — `latest_index={}` 7건 제거(B17-01). (7) `backend/tests/test_sector_calculator_integration.py` — `latest_index={}` 1건 제거(B17-01). (8) `backend/tests/test_sector_data_provider.py` — `latest_index` 키 검증 1건 제거 + mock dict에서 `latest_index` 키 제거(B17-01). (9) `backend/tests/test_telegram_bot.py` — mock dict에서 `latest_index` 키 3건 제거(B17-01). **사전조사(규칙 0-2)**: (1) 의존성 — `compute_full_sector_summary` 호출처 3건(sector_data_provider/engine_sector_confirm/telegram_bot) 모두 `**compute_inputs` + 슬라이더 파라미터만 전달, 미사용 16개 파라미터는 호출처에서 전달하지 않음(기본값 사용). `is_excluded_with_ka10100` production 호출처 grep 0건(테스트 11건만). `SortKey`/`_SORT_LABEL`/`DEFAULT_SORT_KEYS` 외부 import grep 0건. subagent 조사에서 간접 호출 헬퍼(`create_buy_targets`/`calculate_boost_score`/`check_stock_guards`/`rank_to_tiered_score` 등)을 dead code로 잘못 분류한 것을 바로잡음 — P16은 "실제 실행 경로에 연결되어 있는가"가 기준이므로 `build_buy_targets_from_settings` → `create_buy_targets` → 헬퍼 함수 호출 체인은 살아있는 경로. (2) 영향범위 — 백엔드 4파일 + 테스트 5파일. UI 영향 없음 (모두 미사용 파라미터/함수/상수 제거, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 3건 B17-01/B17-02/B17-03) + P24(시그니처 30→14 파라미터 감소 B17-01). 핵심 매매 로직/수신률/업종 점수 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — 제거 대상 3건 모두 사용처 없는 dead code이므로 공통 자산 재사용 해당 없음. **검증**: py_compile 12파일 OK + ruff OK + pytest 376 passed(sector/buy_filter/stock_filter/domain) + 2941 passed(전체, B-16-b 2950 - is_excluded_with_ka10100 9건 = 2941, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 123ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-17 완료**: Domain 계층 6개 파일 3건 전부 해결. 다음: B-18-a/b (스케줄러 및 장마감 파이프라인, 3개 파일 3150줄 — 분할 강력 권장).
- **2026-07-22 (이전)**: B-16-b 세션 — LS증권 구현 ls_rest + ls_providers 2개 파일 7건 해결 (P16/P23/P24). **B-16 전체 3개 파일 12건(B-16-a 5건 + B-16-b 7건) 완료**. **수정 파일 3개**: (1) `backend/app/core/ls_rest.py` (639줄→452줄) — `except Exception as e:` 7곳(line 85/174/206/267/350/457/562)에 `exc_info=True` 추가(B16-06, P23): `kiwoom_connector.py`(12/12), `kiwoom_stock_rest.py`(6/6), `kiwoom_order.py`(1/1), `ls_connector.py`(B16-02 4/4)와 일관성 — LS rest 파일은 0/7이었음. `call_tr` 메서드 dead code 제거(B16-07, P16): production 호출처 0건(테스트 8건만), B15-05(`get_spec` 제거)/B15-06(`__enter__`/`__exit__` 제거)과 동일 패턴 — `call_api`가 production 사용 중이므로 중복 미사용. `get_daily_history` 메서드 dead code 제거(B16-08, P16): production 호출처 0건(테스트 1건만). `get_themes` 메서드 dead code 제거(B16-09, P16): production 호출처 0건(테스트 1건만). `buy_order`/`sell_order` 중복 → `_place_order` 헬퍼 추출(B16-10, P23/P24): 104줄×2 중 차이는 `BnsTpCode: "2"` vs `"1"`, 로그 "매수" vs "매도" 뿐 → 헬퍼 1개 + 얇은 래퍼 2개로 분리, B15-10/B15-11(kiwoom 중복 헬퍼 추출)과 동일 패턴, 약 80줄 감소. (2) `backend/app/core/ls_providers.py` (198줄→190줄) — `get_deposit_detail`/`get_balance_detail` dead 분기 제거(B16-11, P16/P24): 양분기가 모두 `return res` — 조건식 `if not res or res.get("rsp_cd") not in ("00040", "00000")`이 dead code, kiwoom은 조건 없이 직접 위임. `LsOrderProvider.send_order` dead 변수/분기 제거(B16-12, P16/P24): `ls_order_type = 1 if order_type == 'buy' else 2` → 항상 1 또는 2 → else 분기 도달 불가 → `order_type` 직접 분기로 단순화. **동작 변화**: 'buy'/'sell' 정상 입력 — 변화 없음. 잘못된 입력(예: 'cancel') — 기존: 조용히 매도 처리 → 변경: 에러 반환 (버그 수정, 정상 매매 경로 영향 없음). (3) `backend/tests/test_ls_rest.py` (681줄→560줄) — `TestLsRestCallTr` 8건 제거(B16-07), `test_get_daily_history_delegates` 1건 제거(B16-08), `test_get_themes_delegates` 1건 제거(B16-09), 파일 헤더 주석에서 `call_tr`/`get_daily_history`/`get_themes` 제거, 섹션 주석 `get_balance / get_daily_history / get_themes`→`get_balance`로 정리. **사전조사(규칙 0-2)**: (1) 의존성 — `except Exception as e:` 7곳 `exc_info=True` 누락(kiwoom connector 12/12, kiwoom stock_rest 6/6, kiwoom order 1/1, ls connector B16-02 4/4와 대조), `call_tr` production 호출처 grep 0건(테스트 8건만), `get_daily_history`/`get_themes` production 호출처 grep 0건(테스트 각 1건만), `buy_order`/`sell_order` 104줄 중 차이 2곳만(`BnsTpCode`, 로그 라벨), `get_deposit_detail`/`get_balance_detail` 양분기 동일 `return res`, `LsOrderProvider.send_order` `ls_order_type` 변수 할당 후 else 분기 도달 불가. (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향 없음 (모두 내부 구조 정리, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 5건 B16-07/08/09/11/12) + P23(exc_info 일관성 7건 B16-06 + 중복 추출 1건 B16-10) + P24(중복 추출 1건 B16-10 + dead 분기 2건 B16-11/12). 핵심 매매 로직 변경: B16-12만 잘못된 입력 처리 변경 (정상 입력 변화 없음, 버그 수정). (4) 공통 자산 — B16-10 헬퍼는 파일 내에서만 사용 → module-level 메서드로 추출(kiwoom B15-10/B15-11과 동일 방식). B16-06 `exc_info=True` 패턴은 기존 4파일에서 이미 적용된 표준. **검증**: py_compile 4파일 OK + ruff OK + pytest 253 passed(test_ls*, B-16-a 263 - call_tr 8건 - get_daily_history 1건 - get_themes 1건 = 253, 회귀 없음) + 2950 passed(전체, B-16-a 2960 - 10건 = 2950, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 161ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-16 완료**: LS증권 구현 3개 파일 12건(B-16-a 5건 + B-16-b 7건) 전부 해결.
- **2026-07-21 (이전)**: B-16-a 세션 — LS증권 구현 ls_connector 1개 파일 5건 해결 (P16/P20/P23/P24). **B-16 전체 3개 파일 중 1개 완료, 2개 B-16-b 이월**. **수정 파일 2개**: (1) `backend/app/core/ls_connector.py` (895줄→839줄) — `_make_queue_callback()` 헬퍼 메서드 추출(B16-01, P23/P24): `connect()`(line 357-371)와 `_reconnect_loop()`(line 766-780)에 동일하게 복제된 `_queue_put_with_drop` 중첩함수(각 12줄)를 헬퍼 1개로 통합 → kiwoom B15-02와 동일 패턴. `except Exception` 4곳에 `exc_info=True` 추가(B16-02, P23): line 382 초기 연결 실패(kiwoom B15-03 동일), line 794 재연결 로그인 설정, line 823 재연결 실패, line 866 토큰 조회(kiwoom B15-04 동일). `connect()` 초기 연결 실패 로그 `logger.warning`→`logger.error`로 kiwoom(line 236)과 일치(B16-04, P23). `_recv_loop`의 `queue_callback` None 폴백 dead code 제거(B16-03, P20/P16): line 141-143 `else: # 대체: 기존 방식 유지; await self._on_message(internal_msg)` 제거 — production에서 `set_queue_callback` 항상 호출됨(`engine_loop.py:316-317`), kiwoom은 폴백 없이 `self._queue_callback(msg)` 직접 호출. `subscribe_stocks`/`unsubscribe_stocks`를 `subscribe_stocks_tr`/`unsubscribe_stocks_tr` US3 고정 래퍼로 통합(B16-05, P23/P24): 58줄 중복 로직(US3 고정 vs tr_cd 매개변수)을 각 8줄 래퍼로 단순화, 반환값 변환 `(success, fail) → fail == 0`. (2) `backend/tests/test_ls_connector.py` (1120줄→1090줄) — `test_fallback_on_message_when_no_queue` 테스트 제거(B16-03): 폴백 경로 제거로 더 이상 유효하지 않은 테스트. **사전조사(규칙 0-2)**: (1) 의존성 — `_queue_put_with_drop` 중첩함수 `connect()`/`_reconnect_loop()` 2곳 동일 복제(kiwoom B15-02와 동일 패턴), `except Exception` 4곳 `exc_info=True` 누락(kiwoom connector는 12/12 보유), `_recv_loop` `queue_callback` None 폴백(kiwoom은 폴백 없음), `subscribe_stocks`/`subscribe_stocks_tr` US3 고정 로직 중복. (2) 영향범위 — 백엔드 1파일 + 테스트 1파일. UI 영향 없음 (모두 내부 구조 정리, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 1건 B16-03) + P20(None 폴백 1건 B16-03) + P23(exc_info 일관성 4건 B16-02 + 로그 레벨 1건 B16-04) + P24(중복 추출 2건 B16-01/B16-05). 핵심 매매 로직/수신률/업종 점수 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — B16-01 헬퍼는 kiwoom `_make_queue_callback()`(line 472-490)과 동일 패턴 — LS 파일 내 module-level 메서드로 추출. **검증**: py_compile 2파일 OK + ruff OK + pytest 263 passed(test_ls*, 이전 264 - 제거한 테스트 1건 = 263, 회귀 없음) + 2960 passed(전체, B-15-b 2961 - 1건 = 2960, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 183ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급·LS 연결(테스트모드) 정상, 잔존 프로세스 0건. **B-16-b (다음 세션)**: `ls_rest.py` (639줄) + `ls_providers.py` (198줄) 조사·수정.
- **2026-07-21 (이전)**: B-15-b 세션 — 키움증권 구현 stock_rest + providers + order 3개 파일 7건 해결 (P16/P23/P24). **B-15 전체 5개 파일 9건(B-15-a 7건 + B-15-b 7건) 완료**. **수정 파일 4개**: (1) `backend/app/core/kiwoom_stock_rest.py` (436줄→407줄) — `except Exception as e:` 6곳(line 128/182/227/257/304/422)에 `exc_info=True` 추가(B15-08, P23): `kiwoom_connector.py`(12/12 보유)와 일관성. `_pct` 함수(line 42-47) dead code 제거(B15-09, P16): production 사용처 0건, 테스트가 자기 자신만 테스트. `_build_ka10081_request()` 헬퍼 추출(B15-10, P23/P24): `fetch_ka10081_daily_price`/`fetch_ka10081_daily_5d_data`에 동일 복제된 URL/stk_cd/body 조합 로직(10+줄)을 헬퍼 1개로 통합. `_ensure_descending_by_dt()` 헬퍼 추출(B15-10, P23/P24): 두 함수에 동일 복제된 내림차순 정렬 로직(3줄×2)을 헬퍼 1개로 통합. `_fetch_all_stocks_ka10081()` 공통 루프 헬퍼 추출(B15-11, P23/P24): `fetch_ka10081_all_stocks_daily_confirmed`/`fetch_ka10081_all_stocks_5day`가 45줄 중 44줄 동일(호출 함수만 상이) → 공통 루프 헬퍼 + 2개 래퍼(각 6줄)로 분리. `hit_429` 미사용 변수 3곳(line 75/163/352) `_`로 대체(B15-14, P24). (2) `backend/app/core/kiwoom_order.py` (73줄→76줄) — `import logging`/`logger = logging.getLogger(__name__)`를 함수 내부(line 11-12)에서 모듈 레벨로 이동(B15-12, P23/P24): 다른 core 파일 21개는 모두 모듈 레벨. `_BROKER_DISPLAY`도 모듈 레벨로 이동. `except Exception as e:`(line 21)에 `exc_info=True` 추가(B15-13, P23). (3) `backend/tests/test_kiwoom_stock_rest.py` — `TestPct` 클래스 6개 테스트 제거(B15-09), 파일 헤더 주석에서 `_pct` 제거. (4) `backend/app/core/kiwoom_providers.py` (342줄, 변경 없음) — 조사 결과 P1~P24 위반 없음 (이미 B-14-a에서 `UnifiedStockRecord` P4 재검토 완료, `_n`/`_f` 중복은 `kiwoom_rest.py`와의 cross-file 문제로 B-15-b 범위 외). **사전조사(규칙 0-2)**: (1) 의존성 — `_pct` production 사용처 grep 0건(테스트 6건만), `except Exception as e:` 6곳의 `exc_info=True` 누락(`kiwoom_connector.py` 12/12 보유와 대조), `_build_ka10081_request`/`_ensure_descending_by_dt`/`_fetch_all_stocks_ka10081` 중복 로직 2함수쌍에서 동일 확인, `hit_429` 변수 3곳 할당 후 미사용, `kiwoom_order.py` logger 함수 내부(다른 core 21개는 모듈 레벨). (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향 없음 (모두 내부 구조 정리, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 1건 B15-09) + P23(exc_info 일관성 7건 B15-08/B15-13 + 로거 위치 1건 B15-12) + P24(중복 추출 3건 B15-10/B15-11 + 미사용 변수 1건 B15-14). 핵심 매매 로직/수신률/업종 점수 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — 헬퍼 3개는 파일 내에서만 사용되므로 module-level 함수로 추출(별도 공통 모듈 불필요). **검증**: py_compile 4파일 OK + ruff OK + pytest 330 passed(test_kiwoom*, B-15-a 336 - TestPct 6건 = 330, 회귀 없음) + 2961 passed(전체, B-15-a 2967 - 6건 = 2961, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 94ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-15 완료**: 키움증권 구현 5개 파일 14건(B-15-a 7건 + B-15-b 7건) 전부 해결. 다음: B-16 (증권사 구현: LS증권, 총 1732줄 — B-16-a/B-16-b 분할 권장). **미해결 문제 2건 발견 기록**: (1) `kiwoom_rest.py`의 `except Exception as e:` 8곳도 `exc_info=True` 누락 (B-15-a 세션에서 누락, B-15-b 범위 외 — 별도 세션 필요), (2) `kiwoom_providers.py:75` `KiwoomAccountProvider`가 `kiwoom_account_no_real` 미사용 (AuthProvider는 real 우선, 잠재적 real/sim 불일치 — 별도 조사 필요).
- **2026-07-21 (이전)**: B-15-a 세션 — 키움증권 구현 connector + rest 2개 파일 7건 해결 (P16/P23/P24). **B-15 전체 5개 파일 중 2개 완료, 3개 B-15-b 이월**. **수정 파일 4개**: (1) `backend/app/core/kiwoom_connector.py` (554줄→520줄) — `set_queue_callback`의 `from backend.app.services.core_queues import put_tick_with_drop_policy; self._queue_put_with_drop = put_tick_with_drop_policy` dead code 제거(B15-01, P16): `self._queue_put_with_drop` 속성 참조처 전 코드베이스 grep 0건, 실제 누락 정책은 `connect()`/`_reconnect_loop()`에서 인라인 별도 구현. `_make_queue_callback()` 헬퍼 메서드 추출(B15-02, P23/P24): `connect()`(line 224-241)와 `_reconnect_loop()`(line 445-462)에 동일하게 복제된 `_queue_put_with_drop` 중첩함수(각 14줄)를 헬퍼 1개로 통합 → 27줄+29줄 감소, 헬퍼 18줄 추가. `connect()`의 `except Exception: logger.error("...초기 웹소켓 연결 실패...")`에 `exc_info=True` 추가(B15-03, P23): 같은 파일 다른 6곳 `except Exception:`은 모두 `exc_info=True` 포함. `_get_token_async`의 `except Exception as e: logger.warning("...라우터 인증 캐시에서 REST API 조회 실패: %s", ..., e)`에 `exc_info=True` 추가(B15-04, P23). (2) `backend/app/core/kiwoom_rest.py` (655줄→636줄) — `get_spec` 메서드 제거(B15-05, P16): production 호출처 0건(테스트 3건만), `broker_router.get_spec` 직접 호출 가능한 dead wrapper. `__enter__`/`__exit__` 동기 컨텍스트 매니저 제거(B15-06, P16): production 사용처 0건(테스트 1건만), `__aenter__`/`__aexit__` 비동기 버전은 유지. `fetch_ka20001_index`의 JSON 파싱 실패 로그 `logger.info`→`logger.warning` 변경(B15-07, P23): 같은 파일/`kiwoom_connector.py:111`의 유사 JSON 파싱 실패는 `logger.warning` 사용 → 로그 레벨 일관성. (3) `backend/tests/test_kiwoom_connector.py` — `test_set_queue_callback`의 `with patch("backend.app.services.core_queues.put_tick_with_drop_policy", MagicMock())` 제거(B15-01). (4) `backend/tests/test_kiwoom_rest.py` — `TestKiwoomRestGetSpec` 클래스 3개 테스트 제거(B15-05), `test_sync_context_manager` 제거(B15-06), 파일 헤더 주석에서 `__enter__/__exit__`/`get_spec` 제거. **사전조사(규칙 0-2)**: (1) 의존성 — `self._queue_put_with_drop` 참조처 grep 0건, `get_spec` 호출처 `backend/app/` 내 0건(테스트 3건), `__enter__`/`__exit__` 사용처 production 0건(테스트 1건), `_queue_put_with_drop` 중첩함수 `connect()`/`_reconnect_loop()` 2곳 + `ls_connector.py` 2곳 동일 패턴 확인. (2) 영향범위 — 백엔드 2파일 + 테스트 2파일. UI 영향 없음 (모두 내부 구조 정리, 사용자 화면 변화 없음). (3) 아키텍처 부합 — P16(dead code 3건 제거: B15-01/B15-05/B15-06) + P23(exc_info 일관성 2건 B15-03/B15-04 + 로그 레벨 1건 B15-07) + P24(중복 함수 1건 추출 B15-02). 핵심 매매 로직/수신률/업종 점수 변경 없음 → 규칙 0-4/0-5 미해당. (4) 공통 자산 — B15-02 헬퍼는 `core_queues.py:71`의 `put_tick_with_drop_policy`와 유사하지만 다른 큐(`self._ws_queue` vs 전역 `tick_queue`)를 다루므로 별개 — connector 내 헬퍼 메서드로 추출. **검증**: py_compile 4파일 OK + ruff OK + pytest 336 passed(test_kiwoom*) + 2967 passed(전체, B-14-b 대비 4건 감소 = 제거한 테스트 4건 정확히 일치, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 95ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-15-b (다음 세션)**: `kiwoom_stock_rest.py` (436줄) + `kiwoom_providers.py` (342줄) + `kiwoom_order.py` (73줄) 조사·수정.
- **2026-07-21 (이전)**: B-14-b 세션 — Broker 추상화 공통 P20/P21 폴백 2건 해결 (B14-05/B14-06). **B-14 전체 9건 완료 (B-14-a 6건 + B-14-b 2건 + B14-09 재검토 P4 위반 아님)**. **수정 파일 2개**: (1) `backend/app/core/broker_router.py` (281줄→278줄) — `_build`의 `_create_provider` None 반환 시 기본 증권사로 조용히 대체하던 폴백 분기 제거(B14-05, P20/P21/P16): `engine_settings._normalize_broker_config`(`engine_settings.py:319-328`)가 모든 feature를 `broker`(기본 "kiwoom")로 채우므로 빈 문자열 불가능 → None 반환 경로는 dead code. 폴백 대신 `raise ValueError(f"지원하지 않는 증권사: {broker_name!r}")` 전파 (기존 `broker_registry.py:158`/`connector_manager.py:64`/`broker_urls.py:64`/`settings_store.py:277` 패턴 재사용, P23). `_build` 미사용 `default_broker` 변수 제거. `get_provider`의 페이지 오버라이드 증권사 Provider 생성 중 `ValueError` 시 전역 Provider로 조용히 폴백하던 `try/except ValueError` 제거(B14-06, P20/P21): `validate_page_overrides`가 사전 검증하므로 런타임 ValueError는 비정상 상태 → 예외 그대로 전파. `_create_provider` None 반환 시에도 동일하게 ValueError 전파 (B14-05과 패턴 일치). (2) `backend/tests/test_broker_router.py` — `test_build_provider_none_falls_back_to_default`→`test_build_provider_none_raises_value_error` 갱신(B14-05, "대체" 로그 검증 → `pytest.raises(ValueError, match="지원하지 않는 증권사")` 검증), `test_get_provider_value_error_falls_back`→`test_get_provider_value_error_propagates` 갱신(B14-06, 전역 Provider 폴백 검증 → `pytest.raises(ValueError, match="unsupported")` 검증). `import pytest` 추가. **사전조사(규칙 0-2)**: (1) 의존성 — `broker_router._build`의 `default_broker` 참조처 `_load_specs`(line 71-76)만 사용 확인(_build에서는 미사용), `get_provider(feature, page)` page 인자 호출처 production 0건(테스트만, 페이지별 증권사 지정은 아직 UI 미노출 인프라), `raise ValueError(f"지원하지 않는 증권사: ...")` 기존 패턴 4곳 확인. (2) 영향범위 — 백엔드 1파일 + 테스트 1파일. UI 영향: 정상 사용 시 변화 없음(두 폴백 모두 정상 경로 도달 불가). 비정상 상황(설정 손상/미래 페이지별 증권사 기능)에서만 "조용히 대체" → "에러 메시지와 함께 중단"으로 변경 (P21 사용자 투명성). (3) 아키텍처 부합 — P16(dead code 제거) + P20(폴백 금지) + P21(사용자 투명성) + P23(기존 ValueError 패턴 재사용). (4) 공통 자산 — 기존 `raise ValueError(f"지원하지 않는 증권사: {broker_name}")` 패턴 재사용, 신규 패턴 생성 없음. **검증**: py_compile 2파일 OK + ruff OK + pytest 51 passed(test_broker_router) + 2971 passed(전체, B-14-a 대비 동일 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 121ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-14 완료**: Broker 추상화 공통 7개 파일 9건 전부 해결. 다음: B-15 (증권사 구현: 키움증권, 총 2060줄 — B-15-a/B-15-b 분할 권장).
- **2026-07-21 (이전)**: B-14-a 세션 — Broker 추상화 공통 기계적 위반 6건 해결 (P4/P16/P23/P24). **B-14 위반 9건 중 6건 완료, 2건 B-14-b 이월**. **수정 파일 5개**: (1) `backend/app/core/broker_factory.py` (58줄→36줄) — `create_connector(settings)` 함수 제거(B14-02, P16): 호출처 0건 grep 확인(`get_router`/`reset_router`만 사용 중, `ConnectorManager._create_single`이 실제 경로). 함수 내 `if broker_name == "kiwoom": return create_connector_func()` 키움 특화 분기도 함께 제거되어 B14-01(P4) 자동 해결. 미사용 import `BROKER_DISPLAY_NAMES` 제거. (2) `backend/app/core/broker_urls.py` (82줄→80줄) — 하위 호환 dead 상수 3개 제거(B14-03, P16): `KIWOOM_REST_BASE`/`KIWOOM_WS_URI`/`KIWOOM_TOKEN_PATH` 사용처 0건 grep 확인. `KIWOOM_REST_REAL`만 유지(`kiwoom_rest.py` 사용 중) — `_BROKER_URL_DEFAULTS["kiwoom"]["rest_base"]`에서 직접 정의. (3) `backend/app/core/broker_registry.py` (184줄→179줄) — `_LazyRegistry`/`_LazyConnectorRegistry` 중복 클래스 통합(B14-04, P23/P24): 로더 함수를 생성자 인자로 받는 `_LazyBrokerRegistry` 제네릭 클래스 1개로 5개 중복 메서드(`get`/`__getitem__`/`__contains__`/`keys`/`_ensure`) 제거. `_load_provider_registry`/`_load_connector_registry` 로더 함수 분리. (4) `backend/app/core/broker_router.py` (290줄→281줄) — `validate_page_overrides`의 `if isinstance(trading_config, dict): pass # order만 사용` dead 분기 제거(B14-07, P16): 아무 동작 없는 분기. (5) `backend/app/core/connector_manager.py` (292줄) — `_connect_one`의 `logger.warning("[연결] %s 연결 실패: %s", ..., e)`에 `exc_info=True` 추가(B14-08, P23): 같은 파일 다른 로그 4곳(line 116/136/140 등)은 모두 `exc_info=True` 포함 → 일관성 위반 해결. **B14-09 재검토**: `UnifiedStockRecord`(`broker_providers.py:17-24`)는 P4 위반 아님 — 범용 공통 데이터 클래스(증권사 중립적 필드 `code`/`name`/`market_code`/`nxt_enable`/`raw_item`), LS에서도 사용 가능하도록 공통에 둔 설계. 현재 키움만 사용하지만 P4 위반 아님. **사전조사(규칙 0-2)**: (1) 의존성 — `broker_factory.create_connector` 호출처 grep 0건, `KIWOOM_REST_BASE`/`KIWOOM_WS_URI`/`KIWOOM_TOKEN_PATH` 사용처 grep 0건, `_LazyRegistry`/`_LazyConnectorRegistry` 참조처 `broker_registry.py` 내부만 확인, `UnifiedStockRecord` 사용처 3곳(`kiwoom_stock_rest.py` + 테스트 2곳) 확인. (2) 영향범위 — 백엔드 5파일, 테스트 영향 없음 (외부 인터페이스 `PROVIDER_REGISTRY`/`CONNECTOR_REGISTRY`/`get_router`/`reset_router`/`KIWOOM_REST_REAL` 동일). UI 영향 없음 (내부 구조 리팩토링). (3) 아키텍처 부합 — P4(kiwoom 특화 분기 제거) + P16(dead code 3건 제거) + P23(exc_info 일관성) + P24(중복 클래스 통합). (4) 공통 자산 — `_LazyBrokerRegistry` 제네릭 클래스로 기존 두 클래스의 공통 패턴 추출. **검증**: py_compile 7파일 OK + ruff OK + pytest 342 passed(broker/connector 관련) + 2971 passed(전체, B-13-b 대비 동일 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 359ms 정상 기동, RuntimeWarning/Traceback 없음, LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-14-b (다음 세션)**: 핵심 동작 변경 2건(B14-05/B14-06 P20/P21 폴백) 진행 예정 — 규칙 0-4 적용, UI 기준 설명 + 승인 필수.
- **2026-07-21 (이전)**: B-13-b 세션 — 설정 관리 P20 핵심 동작 변경 5건 해결 (B13-01/02/04/11). **B-13 전체 12건 완료**. **수정 파일 4개**: (1) `backend/app/core/settings_file.py` — `load_selected_settings`의 `except Exception: logger.error` silent 제거(B13-01, P20): DB 에러 시 예외 전파 → 호출자(`apply_settings_updates:353`)가 빈 dict를 "변경사항 없음"으로 오인하는 것 방지. `_load_db_settings`의 `except Exception: logger.error; return defaults` silent 제거(B13-02, P20): DB 에러 시 예외 전파 → `app.py:82` asyncio.gather가 기동 실패시켜 사용자 저장 설정이 조용히 무시되는 것 방지. `_decrypt_encrypt_fields`의 `decrypt_value(v) or ""` silent 폴백 → 복호화 실패(None) 시 경고 로그 + 빈문자열(B13-04, P20/P21): `engine_settings._dec` 패턴과 동일 (사용자가 자격증명 비었음으로 오인하는 것 방지). `load_selected_settings`의 인라인 복호화 로직을 `_decrypt_encrypt_fields` 호출로 통일(중복 제거, P23). `_collect_save_params`의 `if enc: v = enc` silent 평문 저장 제거(B13-11, P20/보안): 신규 헬퍼 `_encrypt_field_or_raise` 추가 — 암호화 실패(평문 반환/None) 시 `ValueError` 발생으로 평문 저장 차단. (2) `backend/app/core/settings_store.py` — `_prepare_save_payload`의 `if enc:` silent 평문 저장 → `_encrypt_field_or_raise` 재사용(B13-11). `load_integrated_system_settings_for_editing`의 인라인 복호화 → `_decrypt_encrypt_fields` 재사용(B13-04). 미사용 import `decrypt_value`/`encrypt_value` 제거. (3) `backend/tests/test_settings_file_integration.py` — 신규 `TestSettingsFileP20Propagation` 클래스 6개 테스트 추가: `test_load_selected_settings_propagates_db_error`(B13-01), `test_load_integrated_settings_propagates_db_error`(B13-02), `test_decrypt_encrypt_fields_logs_on_failure`(B13-04), `test_encrypt_field_or_raise_blocks_plaintext`/`test_encrypt_field_or_raise_blocks_none`/`test_encrypt_field_or_raise_success`(B13-11). (4) `backend/tests/test_settings_store.py` — `test_encrypt_field_plain_text` patch 경로 수정(`settings_store.encrypt_value`→`settings_store._encrypt_field_or_raise`), 신규 `test_encrypt_field_failure_raises` 추가(B13-11), `test_decrypt_returns_none`→`test_decrypt_returns_none_logs_warning` 갱신(B13-04, patch 경로 `settings_store.decrypt_value`→`encryption.decrypt_value` + 로그 검증). **사전조사(규칙 0-2)**: (1) 의존성 — `load_selected_settings` 참조처(`settings_store.py:353` apply_settings_updates), `_load_db_settings` 참조처(`load_integrated_system_settings`→`app.py:82` asyncio.gather), `decrypt_value(v) or ""` 3곳(settings_file:177/299, settings_store:404), `encrypt_value` silent 평문 2곳(settings_file:376, settings_store:290) 전체 grep 확인. (2) 영향범위 — 백엔드 2파일 + 테스트 2파일. UI 영향: DB 에러/암호화 실패 상황에서만 에러 메시지 정확도 개선 ("변경사항 없음"/"자격증명 비었음"/"암호화됨" 오인 제거). 정상 작동 시 체감 변화 없음. (3) 아키텍처 부합 — P20(폴백 금지) + P21(사용자 투명성) + P23(`engine_settings._dec` 패턴과 일관, `_decrypt_encrypt_fields`/`_encrypt_field_or_raise` 공통 자산 재사용). (4) 공통 자산 — `_decrypt_encrypt_fields`(B13-a에서 분리) 재사용으로 복호화 로직 단일화, `_encrypt_field_or_raise` 신규 헬퍼로 암호화 실패 처리 단일화. **검증**: py_compile 2파일 OK + ruff OK + pytest 172 passed(settings 3파일) + 2971 passed(전체, B-13-a 대비 +7 신규 테스트, 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 119ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-13 완료**: 설정 관리 5개 파일(settings_file/settings_store/engine_settings/settings_defaults/trade_mode) 12건 전부 해결. 다음: B-14 (Broker 추상화 공통).
- **2026-07-21 (이전)**: B-13-a 세션 — 설정 관리 아키텍처 위반 7건 해결 (P24 함수 분할 4건 + P23 로깅 1건 + P20 빈문자열 폴백 1건 + P24 중복 분기 1건). **수정 파일 3개**: (1) `backend/app/core/settings_file.py` — `load_integrated_system_settings` 91줄→본체 9줄 + `_load_db_settings`/`_ensure_broker_specs`/`_decrypt_encrypt_fields`/`_apply_all_migrations` 4개 헬퍼 분리(B13-05, P24). `save_settings` 78줄→본체 10줄 + `_collect_save_params` 헬퍼 + `_UPSERT_SQL` 상수 분리(B13-06, P24). `load_integrated_system_settings`의 DB 기본값 폴백 중 `db_data[key] == ""` 조건 제거(B13-03, P20): 빈 문자열이 유효값인 필드(telegram_chat_id 등)를 기본값으로 덮어쓰지 않음. 증권사 명세 로드 실패 `logger.warning`에 `exc_info=True` 추가(B13-07, P23). (2) `backend/app/core/settings_store.py` — `apply_settings_updates` 126줄→본체 16줄 + `_compute_select_keys`/`_prepare_save_payload`/`_validate_numeric_fields`/`_compute_changed_keys` 4개 헬퍼 분리(B13-08, P24). `_TIME_RE`/`_TIME_FIELDS`/`_RISK_*` 상수 모듈 최상단으로 이동. `build_masked_settings_dict`의 중복 분기(암호문/평문 각각 "***")를 단일 `if v:` 분기로 단순화(B13-09, P24). (3) `backend/app/core/engine_settings.py` — `build_engine_settings_dict` 320줄→본체 11줄 + `_dec`/`_pick_real_or_legacy`/`_pick_account_no`/`_pick_kiwoom_cred`/`_pick_broker_credentials`/`_build_operation_settings`/`_build_telegram_settings`/`_build_sell_settings`/`_build_risk_settings`/`_build_buy_settings`/`_build_sector_and_order_settings`/`_build_boost_settings`/`_build_misc_settings`/`_normalize_broker_config` 13개 모듈 레벨 헬퍼로 분리(B13-10, P24). **사전조사(규칙 0-2)**: (1) 의존성 — `load_integrated_system_settings`/`save_settings`/`build_engine_settings_dict`/`apply_settings_updates` 참조처 전체 grep, `integrated_system_settings_cache` SSOT 사용처 100건+ 확인, `_migrations_completed`/`_broker_specs` 흐름 추적. (2) 영향범위 — 백엔드 3파일, 테스트 3파일. UI 영향 없음 (함수 분리 리팩토링, 동작 변경 없음). (3) 아키텍처 부합 — P20(빈문자열 폴백 제거) + P23(에러 로깅 일관성) + P24(함수 50줄 이하/중복 제거). (4) 공통 자산 — `_UPSERT_SQL`은 `db_writer.py`와 동일 패턴, `_dec`/`_pick_real_or_legacy` 등은 기존 로직 그대로 모듈 레벨로 분리. **검증**: py_compile 3파일 OK + ruff OK + pytest 165 passed(settings/engine_settings) + 2964 passed(전체, 이전 세션과 동일 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 189ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도·정산 상태 로드·업종순위 재계산·LS/키움 토큰 발급 정상, 잔존 프로세스 0건. **B-13-b (다음 세션)**: P20 핵심 동작 변경 5건(B13-01/02/04/11) 진행 예정 — 사용자가 이미 '폴백 제거' 승인 완료.
- **2026-07-21 (이전)**: B-12 세션 — DB 계층 아키텍처 위반 9건 해결 (P20 폴백 4건 + P16 dead code 1건 + P16/P21 태스크 사망 로깅 1건 + P23 용어 1건 + P24 함수 분리 2건). **수정 파일 3개**: (1) `backend/app/db/stock_tables.py` — `load_master_stocks_table` `except Exception: return {}` 폴백 제거(B12-01, P20): DB 에러 시 예외 전파 → 호출자(`engine_cache.py:24`)가 빈 dict를 "데이터 없음"으로 오인하는 것 방지. `load_trading_days_cache` `except Exception: return None` 폴백 제거(B12-02, P20): DB 에러 시 예외 전파 → `trading_calendar.py:60`이 "DB 캐시 없음"으로 오인 방지. `save_trading_days_cache` `except Exception: logger.warning` silent 제거(B12-03, P20): 예외 전파 → 호출자가 저장 실패 인지. `save_settlement_state` `except Exception: logger.error` silent 제거(B12-04, P20): 예외 전파 → 매매 로직 중 정산 저장 실패 시 즉시 중단. `init_cache_tables` 90줄→본체 6줄 + `_create_runtime_tables`/`_create_user_tables` 2개 헬퍼 분리(B12-08, P24). `migrate_master_stocks_table_pk` 55줄→본체 18줄 + `_rebuild_master_stocks_with_pk` 헬퍼 분리(B12-09, P24). 로그 메시지 "사용자 섹터"→"사용자 업종" 2곳 교정(B12-07, P23 용어 사전 부록 L). (2) `backend/app/db/db_writer.py` — `_process_operation` "사용자 정의 쿼리 없음 - 작업 생략" dead 분기 제거(B12-05, P16): 모든 호출처(`trade_history.py` 3곳, `stock_tables.py:119`)가 `query`+`params` 제공, 도달 불가. `start_db_writer`의 `create_task`에 `add_done_callback` 추가(B12-06, P16/P21): DB writer 태스크 조용히 사망 시 로깅 (gateway 루프 `app.py:63`과 일관, B11-12 교훈 동일 패턴). (3) `backend/tests/test_stock_tables.py` — 예외 전파 기대 테스트 4건 갱신: `test_save_exception_logged`→`test_save_exception_propagates`(save_settlement_state), `test_save_exception_logged`→`test_save_exception_propagates`(save_trading_days_cache), `test_load_exception_returns_none`→`test_load_exception_propagates`(load_trading_days_cache), `test_load_exception_returns_empty`→`test_load_exception_propagates`(load_master_stocks_table). **사전조사(규칙 0-2)**: (1) 의존성 — `load_master_stocks_table` 참조처(`engine_cache.py:24`), `load/save_trading_days_cache` 참조처(`trading_calendar.py:59/68/78/275`), `save_settlement_state` 참조처(`settlement_engine.py:205`), `execute_db_write`/`DBWriteOperation` 참조처(`trade_history.py` 3곳, `stock_tables.py:119`, 테스트 3파일), `_persist` 호출처(`settlement_engine.py` 8곳 — 모두 try/except 없이 직접 호출, 예외 전파 시 매매 중단이 적절), `initialize_trading_calendar_cache` 호출처(`app.py:80` asyncio.gather — 예외 시 기동 실패), `refresh_trading_days_for_year` 호출처(`daily_time_scheduler.py:1380` schedule_engine_task 컨텍스트 — add_done_callback이 예외 로깅). (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향: DB 에러 상황에서만 메시지 정확도 개선 ("데이터 없음" 오인 → "DB 에러" 명확한 메시지). 정상 작동 시 체감 변화 없음. (3) 아키텍처 부합 — P20(폴백 금지) + P16(살아있는 경로/dead code 제거) + P21(사용자 투명성) + P23(용어 통일) + P24(함수 50줄 이하). (4) 공통 자산 — `add_done_callback` 패턴은 `engine_lifecycle.py:289` `schedule_engine_task` 및 `app.py:63` gateway 루프와 동일 패턴 적용. **검증**: py_compile 4파일 OK + ruff OK + pytest 208 passed(db/stock_tables/db_writer/engine_cache/settlement 키) + 2964 passed(전체, 이전 세션과 동일 회귀 없음) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 133ms 정상 기동, RuntimeWarning/Traceback 없음, 1356종목 로드·거래일 캐시 4개 연도 로드·정산 상태 로드 정상, 잔존 프로세스 0건. **B-12 완료**: DB 계층 4개 파일 698줄 전수조사, 9건 전부 해결.
- **2026-07-21 (이전)**: B-11-b 세션 — 파이프라인(Compute/Gateway) 아키텍처 위반 잔여 4건 해결 (B11-08~10 P20 폴백 + B11-11 P11 폴링→이벤트). B-11 전체 12건 완료. **수정 파일 3개**: (1) `pipeline_compute_tick_handlers.py` — PGM 핸들러 `except ValueError: tval=0` 폴백 제거(B11-08): `tval` 누락/오류 시 로깅+스킵(0 대체 금지, 사용자가 순매수 0을 실데이터로 오인하는 것 방지). 0D/PGM 핸들러 `state.master_stocks_cache.get(nk, {})` 빈 dict 폴백 제거(B11-09/10): `nk not in cache` 명시적 분기로 캐시 미스(비정상)와 미구독 종목(정상) 구분, 캐시 미스 시 로깅+스킵. `_apply_01_radar_and_receive_rate`에 `_pc._receive_rate_event.set()` 추가(B11-11 연계). (2) `pipeline_compute.py` — Phase 1 `_phase1_wait_threshold` 폴링→이벤트 전환(B11-11, 사용자 승인 대안1): `while + asyncio.sleep(1.0)` 폴링 → `LazyEvent.wait()` + 200ms 디바운스. `LazyEvent` import(`engine_utils` 공통 자산 재사용, P23). `_receive_rate_event` 모듈 전역 추가. `reset_sector_threshold()`에서 이벤트 클리어(WS 구독 세션 시작 시 초기화). 디바운스 패턴: `event.wait() → event.clear() → asyncio.sleep(0.2) → 처리` — 대기 중 새 틱이 event를 set하면 다음 반복이 즉시 웨이크업(신호 손실 없음). (3) `test_pipeline_compute.py` — Phase 1 테스트 9개 이벤트 기반 mock 패턴으로 갱신(`_make_event_mock` 헬퍼 추가 — `wait` 호출 카운트로 `_compute_running` 제어). PGM/0D 캐시 미스·tval 누락/오류 스킵 테스트 추가 3건. `test_invalid_tval_defaults_to_zero` → `test_invalid_tval_skips` + `test_missing_tval_skips` + `test_cache_miss_skips`(PGM). 0D `test_cache_miss_skips` 추가. **사전조사(규칙 0-2)**: (1) 의존성 — `_subscribed_dynamic` 참조처 7파일 확인, `master_stocks_cache[` 참조처 전체 grep, `_receive_rate_dirty`/`_sector_threshold_passed`/`mark_sector_threshold_passed` 참조처 확인. (2) 영향범위 — 백엔드 2파일 + 테스트 1파일. UI 영향: PGM 순매수 0 잘못 표시 제거(사용자 체감), 수신율 게이지 갱신 빈도 변경(1초 폴링→틱 기반 200ms 디바운스, 체감 미미). (3) 아키텍처 부합 — P20(폴백 금지) + P11(이벤트 기반) + P23(LazyEvent 공통 자산 재사용). (4) 공통 자산 — `LazyEvent`(`engine_utils.py`) 재사용. **B11-11 규칙 0-5 적용**: 사용자 설계 로직(Phase 1 임계값 게이트) 변경으로 사유·영향·대안 3가지 상세 보고 후 사용자 승인(대안1 전면 전환) 받아 진행. **검증**: py_compile 3파일 OK + pytest 93 passed(test_pipeline_compute) + 2964 passed(전체) + 런타임 기동(`python -W error::RuntimeWarning main.py`) RuntimeWarning/Traceback 없음, 잔존 프로세스 0건. **B-11 전체 완료**: B-11-a 8건 + B-11-b 4건 = 12건 전부 해결.
- **2026-07-21 (이전)**: B-11-a 세션 — 파이프라인(Compute/Gateway) 아키텍처 위반 8건 해결 (B11-01~07, B11-12). **분할 사유**: B-11 위반 12건을 2개 하위 세션으로 분할 (B-11-a 8건 기계적·저위험 + B-11-b 4건 구조적·중위험). **수정 파일 1개 + 신규 1개**: (1) `pipeline_compute_tick_handlers.py` (신규 320줄) — 틱 핸들러 4개(`_handle_real_01_tick`/`_handle_real_0d_tick`/`_handle_real_pgm_tick`/`_handle_real_0j_tick`) + 코얼레싱(`_coalesce_batch`) 이관. `_handle_real_01_tick` 108줄→~50줄 + 3개 헬퍼 분리(`_apply_01_radar_and_receive_rate`/`_apply_01_price_to_positions`/`_check_01_auto_sell`, B11-02). `_coalesce_batch` 53줄→~30줄 + `_coalesce_real_items` 헬퍼 분리(B11-07). state·수신세트·플래그는 `pipeline_compute` 모듈 전역을 지연 import로 참조 (테스트 patch 경로 유지). (2) `pipeline_compute.py` 863줄→672줄 — 틱 핸들러 제거 + 분리 모듈 import. `_compute_loop_impl` 69줄→~40줄 + `_drain_control_queue`/`_process_tick_batch` 헬퍼 분리(B11-04). `_process_control_signal` 60줄→~35줄 + `_handle_dynamic_reg`/`_handle_dynamic_unreg` 헬퍼 분리(B11-05, 지역 import `state` 유지 — 테스트 patch 호환). `_handle_real_tick` 66줄→~18줄 + `_extract_real_items`/`_dispatch_real_item` 헬퍼 분리(B11-06). `_sector_recompute_loop_impl` 123줄→8줄 본체 + `_phase1_wait_threshold`/`_evaluate_threshold`/`_phase2_batch_recompute_loop` 3개 헬퍼 분리(B11-03). `start_compute_loop`의 `create_task` 2개에 `add_done_callback` 추가(B11-12, P16/P21 — 태스크 조용히 사망 시 로깅, gateway 루프 app.py:63과 일관). **사전조사(규칙 0-2)**: (1) 의존성 — 전체 grep으로 `pipeline_compute` import 심볼 확인 (engine_account_notify/engine_snapshot/market_close_pipeline/daily_time_scheduler/engine_loop/ws.py + 테스트 2파일). 테스트가 `compute_mod._current_receive_rate`/`_receive_rate_dirty`/`_sector_threshold_passed`/`_compute_running` 모듈 전역을 직접 참조 → 수신율 로직은 분리 불가 (B11-01 잔여 172줄 초과분은 B-11-b 검토). (2) 영향범위 — 백엔드 2파일 + 테스트 2파일. UI 영향 없음 (내부 구조 리팩토링). (3) 아키텍처 부합 — P24(함수 50줄·파일 500줄) + P16/P21(add_done_callback 배선). (4) 공통 자산 — `_normalize_real_type`/`_real_item_stk_cd`/`_base_stk_cd`/`_parse_fid10_price` 기존 유틸 재사용. **검증**: py_compile 2파일 OK + pytest 106 passed(pipeline_compute+pipeline_gateway) + 2961 passed(전체) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 188ms 정상 기동, RuntimeWarning/Traceback 없음, Phase 1 임계값 통과→Phase 2 진입 정상, 잔존 프로세스 0건. **잔여 B-11-b (다음 세션)**: B11-08~10(P20 폴백 3건 — PGM `except ValueError: tval=0`, `get(nk,{})` 폴백 2건) + B11-11(P11 폴링→이벤트 전환 — 사용자 설계 로직, 규칙 0-5 적용, 사유·영향·대안 상세 보고 후 승인 필수).
- **2026-07-21 (이전)**: B-10-b 세션 — 엔진 계좌/서비스 계층 아키텍처 위반 8건 해결 (B10-01, B10-12~17). **분할 사유**: B-10 위반 17건을 2개 하위 세션으로 분할 (B-10-a 11건 + B-10-b 8건). **수정 파일 7개 + 신규 2개**: (1) `kiwoom_account_parsing.py` (신규 244줄) — 키움 파싱 5함수 + 헬퍼 2개 이동 (B10-01, P4 위반 해결). (2) `engine_account_rest.py` 400줄→176줄 — 키움 파싱 함수 본체 제거. (3) `engine_account.py` — import 경로 변경 + `_fetch_account_data` 82줄→46줄 + 3개 헬퍼 분리 (B10-14) + `_update_account_memory_inner` 60줄→15줄 + 2개 헬퍼 분리 (B10-17). (4) `engine_service.py` 217줄→249줄 — `apply_settings_change` 186줄→49줄 + 10개 그룹 헬퍼 분리 (B10-13). (5) `engine_account_notify.py` 504줄→412줄 — `notify_desktop_sector_score` 74줄→32줄 + 4개 페이로드 헬퍼 분리 (B10-15) + `broadcast_account_update` 64줄→15줄 + 3개 헬퍼 (B10-16). (6) `engine_account_broadcast.py` (신규 124줄) — account-update 브로드캐스트 함수 이동 (B10-12, B10-16). (7) `kiwoom_providers.py` — 파싱 함수 임시 추가 후 제거 (kiwoom_account_parsing.py로 단일화). (8) `engine_radar.py` — `_parse_float_loose` import 경로 변경. (9) `test_engine_account_rest.py` + `test_engine_account_notify.py` — import 경로 변경. **사전조사(규칙 0-2)**: (1) 의존성 — 전체 grep으로 `_parse_int_loose`/`_parse_float_loose`/`real04_*`/`parse_kt*` 참조처 확인 (engine_radar.py 1건 발견→수정). (2) 영향범위 — 백엔드 7파일 + 테스트 2파일. UI 영향 없음 (내부 구조 리팩토링). (3) 아키텍처 부합 — P4(증권사명 분리) + P24(함수 50줄·파일 500줄 기준). (4) 공통 자산 — `_base_stk_cd`/`_real_item_stk_cd`/`_parse_fid10_price` 기존 유틸 재사용. **검증**: py_compile 8파일 OK + pytest 2961 passed (test_pipeline_compute 2건 실패→engine_radar.py import 수정 후 통과) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 206ms 정상 기동, RuntimeWarning 없음. **잔여**: B10-02 (B-14로 이월 — kiwoom_providers.py 중복 구현 dead code 조사).
- **2026-07-21 (이전)**: B-10-a 세션 — 엔진 계좌/서비스 dead code 제거 + silent except 로깅 추가 (B-10 위반 17건 중 11건 해결). **분할 사유**: B-10 위반 17건을 한 세션에 처리하기 어려워 AGENTS.md 규칙 0-1(세션당 1단계)에 따라 2개 하위 세션으로 분할 — B-10-a(11건, 기계적·저위험) + B-10-b(8건, 구조적·중위험, 다음 세션). **수정 파일 5개**: (1) `engine_account_notify.py` — 레거시 데스크톱 콜백 변수 8개(B10-03) + no-op register/unregister 함수 11개(B10-04) + dead 함수 4개(`notify_desktop_buy_radar_only`/`notify_desktop_account_tabs_refresh`/`notify_raw_real_data`/`notify_ws_subscribe_status`, B10-05~08) 제거. 미사용 import `Callable`/`time` 제거. 파일 632줄→504줄. (2) `engine_account.py` — dead 분기 2곳(`state.refresh_account_snapshot_meta`/`state.update_account_memory`, B10-09) 제거 + `_apply_balance_realtime` silent except→로깅 추가(B10-10, "잔고 회복 후 매수 재평가 실패 — 사용자가 수동으로 매수 후보 확인 필요"). (3) `engine_service.py` — `apply_settings_change` 끝 silent except→로깅 추가(B10-11, "매수 재평가 무효화 실패 — 설정 변경 후 매수 후보가 갱신되지 않을 수 있음"). (4) `engine_state.py` — dead 속성 `refresh_account_snapshot_meta`/`update_account_memory` 제거(B10-09). (5) `test_engine_account_notify.py` — 제거한 dead 함수 2개 테스트 클래스(`TestNotifyRawRealData`/`TestNotifyWsSubscribeStatus`) + import 제거. **사전조사(규칙 0-2)**: (1) 의존성 — 전체 코드베이스 grep으로 호출처/참조처 확인. dead 변수 8개·no-op 함수 11개·dead 함수 4개 모두 app/ 내 호출처 전무(`notify_raw_real_data`/`notify_ws_subscribe_status`는 테스트만 존재). dead 분기 2곳은 `engine_state.py:40-41`에서 `None` 초기화 후 다른 곳에서 대입 전무 → 항상 False. (2) 영향범위 — 백엔드 4파일 + 테스트 1파일. UI 영향 없음 (모두 dead code이거나 내부 로깅 추가). (3) 아키텍처 부합 — P16(살아있는 경로, dead code 제거) + P20(폴백 금지, silent except→로깅) + P21(사용자 투명성, 실패 시 사용자 인지 가능한 로그 메시지). (4) 공통 자산 — 본 수정은 삭제이지 신규 구현 아님. **검증**: py_compile 5파일 OK + pytest 31 passed(test_engine_account_notify) + 246 passed(account/engine_service/engine_state/engine_bootstrap/engine_snapshot) + 런타임 기동(`python -W error::RuntimeWarning main.py`) 170ms 정상 기동, RuntimeWarning/Traceback 없음, 잔존 프로세스 0건. **잔여 B-10-b (다음 세션)**: B10-01~02(키움 파싱 5함수 → kiwoom_providers.py 이동, P4/P10) + B10-12~17(파일/함수 분할, P24 — engine_account_notify.py 504줄→500줄 이하, apply_settings_change 186줄·_fetch_account_data 82줄·notify_desktop_sector_score 74줄·broadcast_account_update 64줄·_update_account_memory_inner 60줄 분할).
- **2026-07-18 (이전)**: `snapshot_history` 계열 dead code 전체 삭제 (P16 살아있는 경로 위반 해소). 12파일 수정: **백엔드 production 6파일** + **백엔드 테스트 3파일** + **프론트엔드 3파일** + **문서 1파일(ARCHITECTURE.md)**. **삭제 대상**: `snapshot_history`는 "수익 이력 차트" 기능용으로 과거 기획되었으나 (1) `state.snapshot_history.append(...)` 호출 0건으로 항상 빈 리스트, (2) `notify_snapshot_history_update()` 본체가 `pass` no-op, (3) 백엔드에서 `snapshot-update` WS 이벤트 발생 코드 0건, (4) 프론트엔드 `snapshotHistory` 상태를 읽는 UI 컴포넌트 0건. ARCHITECTURE.md 276라인에 이미 dead code 사례로 명시됨. **사전조사 (규칙 0-2)**: (1) 의존성 — 전체 코드베이스 grep으로 호출처/참조처 0건 재확인 (단, 초기화만 수행하는 `clear()` 3곳 + no-op `notify_snapshot_history_update` 1곳은 삭제 대상). (2) 영향범위 — 백엔드 6파일 + 테스트 3파일 + 프론트엔드 3파일 + 문서 1파일. (3) 아키텍처 부합 — P16(살아있는 경로, dead code 제거) + P10(SSOT, 빈 리스트 중복 관리 제거) + P24(단순성). (4) 공통 자산 — 본 수정은 삭제이지 신규 구현 아님. **수정 내역**: (a) `engine_state.py` — `self.snapshot_history: list = []` 필드 제거. (b) `engine_lifecycle.py` — 엔진 시작 시 `snapshot_history = []` 초기화 제거. (c) `engine_snapshot.py` — `get_snapshot_history` import 제거 + initial-snapshot dict의 `snapshot_history` 키 제거 + `_reset_realtime_fields()`의 `snapshot_history.clear()` 제거. (d) `engine_account.py` — `get_snapshot_history()` 함수 제거. (e) `engine_account_notify.py` — `notify_snapshot_history_update()` no-op 함수 제거. (f) `settings.py` — `state.snapshot_history.clear()` 제거 + `notify_snapshot_history_update` import/호출 제거 + 로그 메시지 "수익 이력 초기화" 제거 + 주석 번호 8/9 → 8로 재정리. (g) `test_engine_account.py` — `get_snapshot_history` import 제거 + `TestGetSnapshotHistory` 클래스 2 테스트 제거. (h) `test_engine_snapshot.py` — `get_snapshot_history` patch 5곳 제거 + `assert result["snapshot_history"] == []` 1곳 제거 + `mock_state.snapshot_history = MagicMock()` 4곳 제거 + `mock_state.snapshot_history.clear.assert_called_once()` 1곳 제거. (i) `test_web_routes.py` — `notify_snapshot_history_update` patch 1곳 제거 + `mock_state.snapshot_history = MagicMock()` 1곳 제거. (j) `frontend/src/types/index.ts` — `SnapshotHistory` 인터페이스 제거. (k) `frontend/src/stores/uiStore.ts` — `SnapshotHistory` import + `snapshotHistory` UIState 필드 + initialState + `applySnapshotUpdate()` 함수 + `applyTestDataResetCompleted`/`applyInitialSnapshotUI`의 `snapshotHistory` 설정 제거. (l) `frontend/src/binding.ts` — `applySnapshotUpdate`/`SnapshotHistory` import + `snapshot-update` 이벤트 리스너 제거. (m) `ARCHITECTURE.md` 276라인 — dead code 위반 사례에서 `notify_snapshot_history_update` 제거 (해결 표시). 검증: py_compile 9파일 OK + pytest 2966 passed / 0 failed (이전 2968에서 2 감소 = 제거한 `TestGetSnapshotHistory` 2 테스트) + `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러, 2.14s) + 코드베이스 전체 grep 잔존 0건 (ARCHITECTURE.md 역사적 기록만 제외). **아키텍처 원칙 부합**: P16(살아있는 경로 — dead code 제거) + P10(SSOT — 빈 리스트 중복 관리 제거) + P24(단순성). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 일반설정 페이지 UI 중복 라벨 제거 (2건). 1파일 수정: `frontend/src/pages/general-settings.ts`. **해결 문제**: (a) "전역매매설정 (리스크 매니저)" 섹션 내 마스터 토글 행 라벨이 "리스크 매니저"로 섹션 제목 및 설명 문구와 동일 단어 3연속 반복 → `자동 매매 중단 기능`으로 변경 (line 516). 섹션 제목이 정체(리스크 매니저) 담당, 설명 문구가 동작 담당, 토글 라벨이 스위치 역할 담당으로 각 영역 중복 없이 자기 역할만 담당. (b) 일반설정 5개 탭 중 4개 탭(자동매매/시간 설정/투자모드/텔레그램)이 탭 바 라벨과 동일 글자를 탭 본문 최상단 `sectionTitle(...)`로 또 표시 → 4개 호출 제거 (lines 195/416/745/835). API 설정 탭과 동일 구조로 통일 (본문 바로 실제 설정 내용으로 시작). **사전조사 (규칙 0-2)**: (1) 의존성 — `sectionTitle` 함수 자체는 서브섹션(전역매매설정/화면 표시/구독 한도/사전 준비 시간 설정/주 사용 증권사/가상 투자금)에서 계속 사용되어 import 유지, 4개 탭 최상단 호출만 제거. (2) 영향범위 — 프론트엔드 1파일, 백엔드/설정 저장/탭 전환 동작 영향 없음 (표시 텍스트만). (3) 아키텍처 부합 — P23(일관성, 모든 탭이 API 설정 탭과 동일 구조로 통일) + P24(단순성, 중복 표시 제거) + P21 위반 없음 (탭 바에 이미 같은 타이틀 표시되어 정보 손실 없음). (4) 공통 자산 — 본 수정은 호출 제거이지 함수 자체 제거가 아님. 검증: `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러, 63 모듈 변환, 1.98s). **아키텍처 원칙 부합**: P23(탭 간 구조 일관성 + 중복 단어 제거) + P24(불필요한 반복 표시 제거) + P21(정보 손실 없이 탭 바 라벨로 동일 정보 전달). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 업종순위 진행 바 시각 개선 — 상위 N 파랑 그라데이션 적용. 1파일 수정: `frontend/src/pages/sector-ranking-list.ts` `renderScoreBar()` fill 단색 파랑 → `linear-gradient(to right, downLight, down)` 전환 (공통 `progress-bar.ts` `_colorGradient` 패턴과 동일 시각 언어, P23 일관성). 회색 계열(컷오프 미통과=`inactiveBg`, 통과+N밖=`muted`)은 그라데이션 효과 미미해 단색 유지 (P24 단순성). **사전조사 (규칙 0-2)**: (1) 의존성 — `renderScoreBar`는 `sector-ranking-list.ts` 내부 전용, `data-table` rowFooter 콜백으로만 전달. (2) 영향범위 — 프론트엔드 1파일. (3) 아키텍처 부합 — P23(공통 progress-bar.ts 패턴 일치)/P24(transition 없이 정적 그라데이션)/P21(기존 색상 분기 유지). (4) 공통 자산 — `COLOR.downLight`/`COLOR.down` 이미 ui-styles.ts 정의. **transition 미적용 사유**: `data-table.ts` rowFooter diff가 `isEqualNode` 비교 후 DOM 전체 교체 방식 (line 942~957)이라 width transition 추가해도 점프만 발생하고 부드러운 이동 효과 없음 + 사용자 인지 명확성(P21 — 부드러운 이동 시 "어디로 이동했지?" 혼란 가능) 우선. 검증: `npm run typecheck` + `npm run build` 성공 (tsc 0 에러 + vite 빌드, 701ms). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 업종순위 설정 가산점 만점 표시 수정 + ARCHITECTURE.md 6.2절 문서-코드 불일치 해소 (3세션 분량 누적 커밋). 3파일 수정: (1) `frontend/src/pages/sector-settings.ts` — 가산점 만점 표시를 단일 "만점 = 43점"에서 1차/2차/3차 각각 + 합계 분리 표시로 변경. 헬퍼 함수 `_updateMaxScoreDisplay()` 신규 (백엔드 `sector_score.py` 계산식 `업종 수 × (1 + 슬라이더/100)`과 동일, P10 SSOT) + `_formatScore()` (정수는 정수, 소수는 소수점 1자리) + DOM 구조 단일 span → 컨테이너 span + 자식 span 2개 (`maxScoreDetailEl` 1차/2차/3차 작게 회색 `FONT_SIZE.small`/`COLOR.tertiary`, `maxScoreTotalEl` 합계 크고 진하게 `FONT_SIZE.label`/`COLOR.down`/`fontWeight: bold`, baseline 정렬) + 3개 슬라이더 onChange 6곳 + `syncFromSettings` + `hotStore` 구독에 갱신 호출 추가 (슬라이더/업종수 변경 시 실시간 갱신) + 라벨 텍스트 "만점 = 업종 수 (자동)" → "만점 = 업종 수 × (1 + 슬라이더/100)" (실제 계산식 명시) + `unmount`에 신규 변수 2개 null 처리. 표시 형식: `(1차: 43점 | 2차: 64.5점 | 3차: 21.5점)  합계: 129점 (업종 43개)`. (2) `ARCHITECTURE.md` — 6.2절 3단계 누적 가산점 시스템 설명 11곳 갱신: 685라인 `final_score 정수` → `float` (실제 `models.py:45` float), 687라인 "백분위 평균 순위" → "가중 순위 합" (실제 `sector_score.py:134-178` 가중 순위 합 방식), 689라인 `rank (1=최강, 0=순위 없음/컷오프 미달)` → `rank (1=최강, 모든 업종에 순위 부여 — 컷오프 미달은 is_cutoff_passed=False로 매수 대상 구분)` (실제 `models.py:41` 모든 업종에 rank 부여), 702라인 "순위/백분위 기반" → "순위/가중 순위 합 기반", 710라인 2차 가산점 점수 변환 `percentile_to_score + rank_to_tiered_score` → `rank_to_tiered_score` 단일 함수 + 가중치 공식 `(N-순위+1)/N` 명시, 713-719라인 "사용자 설정 만점 (기본값): sector_bonus_rise_ratio_max = 10 등 고정 만점" → 슬라이더 기반 자동화 만점 설명 (업종 수 × (1+슬라이더/100) 공식, 슬라이더 3개 변수명, ±50%/-100% 동작, 종합 만점 = 합), 722-723라인 `percentile_to_score` 함수 설명 제거 → 2차 가산점 가중치 설명으로 대체, 728/732/744라인 `rank=0` → `is_cutoff_passed=False` (rank는 모든 업종에 부여), 730라인 `정수` → `float`. (3) `HANDOVER.md` — "기타 대기 항목"에 `sector_calculator.py:132` 코드 주석-코드 불일치 1건 기록 (사용자 "문서만 수정, 코드 변경 없음" 명시로 코드 주석은 수정 안 함, 승인 시 별도 세션에서 갱신 권장). **해결 문제**: (a) 만점 표시가 1차 가산점 만점(43점)만 보여주어 사용자가 실제 합산 만점(129점)을 알 수 없었음 → 1차/2차/3차/합계 모두 표시로 P21 투명성 확보. (b) ARCHITECTURE.md 6.2절이 과거 설계 단계의 "백분위 기반" 방식을 설명하고 있었으나 실제 구현은 "가중 순위 합" 방식으로 변경되었고 문서 미갱신 상태 → 11곳 문서-코드 불일치 해소 (P10/P23). 검증: `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러, 63 모듈 변환) + ARCHITECTURE.md 내 `percentile_to_score`/`백분위`/`rank=0`/`rank>0` 잔존 0건 (grep 확인) + 잔존 프로세스 vite dev 서버 1건 (사용자 사전 실행분). **아키텍처 원칙 부합**: P10(프론트엔드 표시 로직이 백엔드 sector_score.py 계산식과 동일, 만점 중복 관리 없음 + ARCHITECTURE.md 6.2절 설명이 실제 코드와 단일 진실 소스 일치) + P21(1차/2차/3차/합계 만점 모두 표시로 슬라이더 효과 즉시 파악 + 합계 시각적 강조) + P23(기존 FONT_SIZE/COLOR 표준 자산 재사용, 신규 색상/크기 도입 없음 + 용어 통일 "가중 순위 합"/"is_cutoff_passed") + P24(단일 컨테이너 + 자식 span 2개 구조, 헬퍼 함수 1개). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 업종순위 테이블 CSS Grid 전환 + UI 개선 완료. 2파일 수정: (1) `frontend/src/components/common/data-table.ts` — `DataTableOptions<T>`에 `rowFooter?: (row: T, index: number) => HTMLElement` 옵션 추가 (가상 스크롤 모드 `renderRow`에서 footer div 생성 — `gridTemplateRows: 'auto auto'` + `gridColumn: '1 / -1'`로 행 전체 너비 하단 별도 줄, cell diffing 시 `isEqualNode` 비교 후 갱신, 옵션 기반으로 기존 7개 페이지 영향 없음). (2) `frontend/src/pages/sector-ranking-list.ts` — `createDataTable` 기반 전면 재작성 (272줄 → 326줄). **해결 문제**: 기존 flex 구조에서 헤더(`headerDefs`)와 데이터(`defs`) 컬럼 CSS 이중 관리 → 업종명 `min-width` 불일치(데이터만 140px) + 패딩 불일치(헤더 2px vs 데이터 4px) + `flex-shrink:0` 누락 → 헤더/데이터 컬럼 너비 상이 → "종목수" 아래에 가산점 표시되는 정렬 불일치. **변경 내용**: 단일 `COLUMNS: ColumnDef<SectorScoreRow>[]` 배열 (6컬럼: rank/sector/total/final_score/rise_ratio/avg_trade_amount, P10 SSOT) + `rowStyle` 콜백 (선택 outline+bgColor / 컷오프 opacity+bgColor, sector-stock.ts 동일 패턴 P23) + `rowFooter` 콜백 (행 전체 너비 5px 진행 바 — 컷오프 미달=회색/상위N=빨강/그 외=중간색) + 가상 스크롤 활성화 (전체 업종 표시, `MAX_ROWS=60` 제거, P21) + 행 클릭 `_rowKey` 역추적 이벤트 위임 (내부 직접 부착) + delta 갱신 `updateItemByKey` 개별 처리 (성능 최적화) + 좌우 여백 제거 (root padding/margin 0, DataTable wrapper border none) + 셀 구분선 숨김 (모든 컬럼 `cellStyle`/`headerStyle`에 `borderLeft: 'none'`, `rowStyle`에 `borderBottom: 'none'`, `headerDiv` borderBottom none). **사용자 결정사항**: 진행 바=행 전체 너비 하단 별도 줄(rowFooter), 행 클릭=내부 직접 부착, 가상 스크롤=활성화. 검증: `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러) + 잔존 프로세스 0건. **아키텍처 원칙 부합**: P10(단일 COLUMNS 배열) + P21(정렬 불일치 구조적 차단 + 60행 제한 제거) + P23(같은 페이지 우측 패널 sector-stock.ts와 동일 DataTable 패턴 + 공통 자산 재사용) + P24(`rowCaches`/`buildRankingRows`/`updateRankingRows` 3함수 → `COLUMNS`+`rowStyle`+`refreshRows`로 단순화). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 리스크 매니저 확장 6세션 (구현 Step 4 — 통합 검증 + test_buy_order_executor + ARCHITECTURE.md 갱신 + 계획서 2개 삭제) 완료. **리스크 매니저 확장 작업 전체 완료 (1~6세션).** 2파일 수정 + 2파일 삭제: (1) `backend/tests/test_buy_order_executor.py` — import에 신규 사유코드 4개 추가 (`BUY_REJECT_RISK_PROFIT`/`_LOSS_RATE`/`_PROFIT_RATE`/`_CONSEC_LOSS`) + `TestMultiRankBuyAlgorithm`에 신규 테스트 4개 추가 (`test_risk_profit_reason_blocks_all`/`test_risk_loss_rate_reason_blocks_all`/`test_risk_profit_rate_reason_blocks_all`/`test_risk_consec_loss_reason_blocks_all` — 기존 `test_loop_breaks_on_global_block` 패턴 복제, `execute_buy`가 신규 사유코드 반환 시 2순위 호출 안 됨 검증, P16 살아있는 경로). (2) `ARCHITECTURE.md` — 7.2절 RiskManager 섹션 갱신 (기존 3조건 + 신규 확장 리스크 4조건 + 토글 계층 3개 명시 + `max_daily_loss_limit` 레거시 호환 주석 + `risk_block_status` WS 브로드캐스트/헤더 칩 표시 명시) + 11.1절 WS 이벤트 표에 `risk_block_status` 이벤트형 추가 (`circuit_breaker_open` 직후). (3) `docs/architecture_risk_manager_extension_design.md` 삭제 (1세션 산출물, 구현 완료로 역할 완수). (4) `docs/plan_risk_manager_extension.md` 삭제 (2세션 산출물, 구현 완료로 역할 완수). 검증: pytest 전체 2968 passed (기존 2964 + 신규 4, 0 failed) + `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러) + 런타임 기동 OK (`-W error::RuntimeWarning`, RuntimeWarning 0건) + `/api/settings` 응답에 신규 키 12개 정상 출력 (기본값: `risk_manager_on=False`/`daily_loss_limit=-500000`/`daily_loss_rate_limit=-5.0`/`daily_profit_limit=500000`/`daily_profit_rate_limit=5.0`/`risk_block_buy_on=True`/`risk_block_sell_on=False`/`consecutive_loss_limit=3`) + `check_sell_order_allowed` await 누락 0건 (Code Removal Rules 규칙 3) + 잔존 프로세스 0건. **리스크 매니저 확장 작업 요약 (1~6세션)**: 1세션(설계서 862줄) → 2세션(태스크 파일 605줄, 사전조사 + 오류 3건 발견) → 3세션(백엔드 설정 계층 + 사유코드 5파일) → 4세션(RiskManager 본체 + 매도 async + WS 브로드캐스트 4파일) → 5세션(프론트엔드 5파일) → 6세션(통합 검증 + 문서 갱신 + 계획서 삭제). 총 수정 파일: 백엔드 5파일 + 프론트엔드 5파일 + 테스트 4파일 + 문서 1파일(ARCHITECTURE.md) + 삭제 2파일(설계서/태스크). 다음 작업: 사용자 다음 우선순위 대기.
- **2026-07-18 (이전)**: 리스크 매니저 확장 5세션 (구현 Step 3 — 프론트엔드 5파일) 완료. 5파일 수정: (1) `frontend/src/types/index.ts` — `AppSettings` 인터페이스에 신규 키 12개 타입 추가 (기존 `subscribe.max_0b_count` 직후, `risk_manager_on`/`daily_loss_limit`/`daily_loss_rate_limit_on`/`_limit`/`daily_profit_limit_on`/`_limit`/`daily_profit_rate_limit_on`/`_limit`/`risk_block_buy_on`/`risk_block_sell_on`/`consecutive_loss_limit_on`/`_limit`). (2) `frontend/src/stores/uiStore.ts` — `riskBlockStatus: { side: string; reason: string } | null` 상태 추가 (기존 `orderTimeBlocked` 직후) + `initialState`/`applyInitialSnapshotUI` 초기화 + `applyRiskBlockStatus()`/`clearRiskBlockStatus()` 함수 (기존 `applyOrderTimeBlocked`/`clearOrderTimeBlocked` 패턴 복제). (3) `frontend/src/binding.ts` — `applyRiskBlockStatus` import + `risk_block_status` WS 이벤트 핸들러 (기존 `order_time_blocked` 핸들러 직후). (4) `frontend/src/layout/header.ts` — `clearRiskBlockStatus` import + 리스크 차단 칩 생성 (클릭 시 해제) + `onStateChange` destructuring `riskBlockStatus` 추가 + 표시 로직 (오류 2 수정 적용 — `COLOR.upBg`/`COLOR.up` 빨강 사용, 설계서 `COLOR.downBg` 파랑에서 수정). (5) `frontend/src/pages/general-settings.ts` — `createToggleLabelControlsRow`/`createSettingRow` import + 리스크 매니저 참조 16개 + "전역매매설정 (리스크 매니저)" 섹션 (마스터 토글 + 일일 손실 한도 + 손실률/수익/수익률/연속손실 토글+입력 + 매수/매도 차단 토글 + 위험성 설명 문구) + `syncFromSettings` 동기화 (12키) + `unmount` 정리 (16참조 null). **사전조사 기반 수정 (오류 2와 유사)**: `daily_loss_limit` 입력 컴포넌트 — 설계서/태스크 파일은 `createMoneyInput` 명시하나, `createMoneyInput`은 음수 미지원 (`currentValue > 0 ? ... : '0'` 패턴, spin down `Math.max(0, ...)` 클램프). 음수 값(-50만원)이 '0'으로 표시되는 버그 발생 → `createNumInput` 사용 (음수 지원, `min: -1000000000`, `max: 0`, `step: 10000`). `daily_profit_limit`(양수)은 `createMoneyInput` 그대로 사용. 검증: `npm run build` 성공 (tsc 타입체크 + vite 빌드, 0 에러, 63 모듈 변환). 다음 세션: 6세션 구현 Step 4 (통합 검증 + test_buy_order_executor + ARCHITECTURE.md 갱신 + 계획서 2개 삭제) 승인 대기.
- **2026-07-18 (이전)**: 리스크 매니저 확장 4세션 (구현 Step 2 — RiskManager 본체 + 매도 async + WS 브로드캐스트) 완료. 4파일 수정. **사용자 승인 사항 (규칙 0-4/0-5)**: (1) 기존 체크(일일 손실 한도/예수금/단일 종목 비중)는 `risk_manager_on=False` 기본값에서도 항상 실행 — 안전장치 회귀 방지 (설계서 3.4.4절 "risk_manager_on=False 시 전체 스킵"에서 수정), (2) 예수금/단일 종목 비중은 `risk_block_buy_on=False` 시에도 항상 실행 — 주문 실행 기본 전제이므로 리스크 매니저 토글과 무관, (3) 신규 조건(손실률/수익/수익률/연속손실)만 `risk_manager_on + risk_block_buy_on` 시에 실행. 수정 내역: (1) `backend/app/services/risk_manager.py` — `_sync_thresholds()` 신규 11키 동기화 추가 (P13) + `_get_consecutive_loss_count()` 헬퍼 신규 (연속 손실 카운트, DESC 정렬 기준) + `_check_extended_buy_risk()` 헬퍼 신규 (신규 4조건 검사 — P24 단순성, `check_buy_order_allowed` 50줄 이하 유지) + `check_buy_order_allowed()` 신규 조건 배선 (P16) + `check_sell_order_allowed()` async 변환 + 6개 조건 확장 (매도 차단 시 "(매도 차단)" 접미사 — P23). (2) `backend/app/services/trading.py` — `check_sell_conditions()` 내 `await risk_mgr.check_sell_order_allowed(...)` 추가 + 차단 시 WS 브로드캐스트 `risk_block_status` 이벤트 추가 (P21 사용자 투명성). (3) `backend/tests/test_risk_manager.py` — `risk_manager` fixture 신규 속성 11개 추가 + `TestCheckSellOrderAllowed` 3개 async 변환 (`@pytest.mark.asyncio` + `await` + `_sync_thresholds` no-op patch) + 신규 테스트 11개 (`TestRiskManagerToggle` 3 + `TestDailyLossRateLimit` 2 + `TestDailyProfitLimit` 1 + `TestDailyProfitRateLimit` 1 + `TestConsecutiveLossLimit` 4). (4) `backend/tests/test_trading.py` — `check_sell_order_allowed` 10곳 `AsyncMock` 변환 + `TestMapRiskReasonToCode` 신규 매핑 4개 추가 + 신규 상수 4개 import 추가. 검증: py_compile 4파일 OK + pytest test_risk_manager + test_trading 85 passed + pytest 전체 회귀 2964 passed / 0 failed + 런타임 기동 OK (`-W error::RuntimeWarning`, RuntimeWarning 0건) + `/api/settings` 응답에 신규 키 12개 포함 확인 + `check_sell_order_allowed` await 누락 0건 (Code Removal Rules 규칙 3) + 잔존 프로세스 0건. **설계서에서의 수정 사항**: 설계서 3.4.4절 "risk_manager_on=False 시 기존 체크 전체 스킵" → "기존 체크 항상 실행, 신규 조건만 스킵"으로 변경 (사용자 승인). `_get_today_pnl_and_principal()` 헬퍼 대신 `_check_extended_buy_risk()` 헬퍼 사용 — `today_pnl` 중복 조회 방지 (P10).
- **2026-07-18 (이전)**: 리스크 매니저 확장 3세션 (구현 Step 1 — 백엔드 설정 계층 + 사유코드) 완료. 5파일 수정: (1) `backend/app/core/settings_defaults.py` — `DEFAULT_USER_SETTINGS`에 신규 키 12개 기본값 추가 (`risk_manager_on=False`, `daily_loss_limit=-500000`, `daily_loss_rate_limit_on=False`/`_limit=-5.0`, `daily_profit_limit_on=False`/`_limit=500000`, `daily_profit_rate_limit_on=False`/`_limit=5.0`, `risk_block_buy_on=True`, `risk_block_sell_on=False`, `consecutive_loss_limit_on=False`/`_limit=3`) — 기존 `max_daily_loss_limit` 레거시 키 유지. (2) `backend/app/core/engine_settings.py` — 신규 키 12개 타입 캐스팅 추가 (기존 리스크 블록 직후, P20 폴백 금지 — `int(_v if _v is not None else 기본값)` 패턴, bool 키는 `merged.get(k, 기본값)` 인자 폴백). (3) `backend/app/core/settings_store.py` — `apply_settings_updates()` 내 `subscribe.max_0b_count` 검증 직후 신규 리스크 키 범위 검증 추가 (`_RISK_INT_KEYS`: `daily_loss_limit`(-10억~0), `daily_profit_limit`(0~10억), `consecutive_loss_limit`(1~100) / `_RISK_FLOAT_KEYS`: `daily_loss_rate_limit`(-100~0), `daily_profit_rate_limit`(0~100) — 범위 위반 시 `ValueError` → 422 차단, P20/P22). (4) `backend/app/services/trading.py` — 사유코드 4개 신규 추가 (`BUY_REJECT_RISK_PROFIT`/`_LOSS_RATE`/`_PROFIT_RATE`/`_CONSEC_LOSS`) + `BUY_GLOBAL_REJECT_REASONS` frozenset에 4개 추가 + `_map_risk_reason_to_code()`에 신규 매핑 4개 추가 (기존 "일일 손실 한도" 이후, "예수금 부족" 이전에 삽입 — 매핑 순서 일관성). (5) `backend/tests/test_settings_store.py` — `TestRiskManagerSettingsValidation` 클래스 신규 추가 (8개 테스트: 양수 `daily_loss_limit` 거부 + 음수 `daily_profit_limit` 거부 + 0 `consecutive_loss_limit` 거부 + 양수 `daily_loss_rate_limit` 거부 + 음수 `daily_profit_rate_limit` 거부 + 비정수 거부 + 비숫자 거부 + 유효 경계값 10개 통과). 검증: py_compile 5파일 OK + pytest 197 passed (test_trading 48 + test_risk_manager 22 + test_settings_store 85 + test_buy_order_executor 42, 신규 8개 포함) + 런타임 기동 OK (`-W error::RuntimeWarning`, RuntimeWarning 0건) + `/api/settings` 응답에 신규 키 12개 포함 확인 (기본값 정상 출력) + 잔존 프로세스 0건. RiskManager 본체 확장(캐시 소비, `check_buy/sell_order_allowed` 조건 추가)은 4세션에서 진행 예정.
- **2026-07-18 (이전)**: 리스크 매니저 확장 태스크 파일 작성 완료 (2세션). 1파일 신규: `docs/plan_risk_manager_extension.md` (605줄). 1세션 설계서 기반으로 심층 사전조사(규칙 0-2 4항목) + 구현 Step 4개 세션 분할(3~6세션) + 테스트 계획 + 런타임 검증 방법 + 사용자 결정 항목 + 위험 대응 + 완료 기준 포함. **사전조사에서 설계서 오류 3건 발견·명시**: (1) `test_risk_manager.py` 이미 존재(286줄) — 설계서 "신규 파일" 오기입 → "기존 확장"으로 수정, (2) UI 칩 색상 불일치 — 설계서 문구 "빨간색" vs 코드 `COLOR.downBg`(파랑) → `COLOR.upBg`(빨강)로 수정 제안, (3) `check_sell_order_allowed()` async 변환 시 기존 테스트 3개(`test_risk_manager.py:253,259,265`) 동기 호출 → `await` + `@pytest.mark.asyncio` 갱신 필요. 의존성 분석: 백엔드 5파일 + 프론트엔드 5파일 + 테스트 4파일(3 수정 + 1 확장). `check_sell_order_allowed` 전체 참조 6곳 검색 완료(Code Removal Rules 규칙 3). 기존 공통 자산 20개 재사용 확인 (P23). 세션 분할: 3세션(백엔드 설정 계층 + 사유코드 4파일) → 4세션(RiskManager 확장 + 매도 async + WS 브로드캐스트 2파일 + 테스트 2파일) → 5세션(프론트엔드 5파일) → 6세션(통합 검증 + test_buy_order_executor + ARCHITECTURE.md 갱신 + 계획서 2개 삭제). 코드 수정 없음 (문서 1개 신규). 다음 세션: 3세션 구현 Step 1 승인 대기 (오류 1~3 수정 승인 + 구현 승인 필요, 규칙 0-4/0-5).
- **2026-07-18 (이전)**: 다운로드 진행률 로그 1줄 갱신 방식 구현 완료. 4파일 수정: (1) `backend/app/core/logger.py` — `log_progress()` / `log_progress_end()` 헬퍼 추가 (TTY 시 `\r` 1줄 갱신, non-TTY 시 `\n` 출력으로 파이프 깨짐 방지, 파일은 DEBUG 기록), `_stdout_sink`에 `_progress_active` 상태 추가 — 진행률 `\r` 갱신 중 다른 로그 끼어들면 `\n`으로 진행 줄 종료 후 출력 (인터리브 커서 꼬임 방지, P23/P24). (2) `backend/app/core/kiwoom_stock_rest.py` — `fetch_ka10081_all_stocks_daily_confirmed` (264 라인) + `fetch_ka10081_all_stocks_5day` (311 라인) per-stock `logger.info("진행 중: N/M (X%)")` → `log_progress("[다운로드]", len(result), total, code=cd)` 교체 + 루프 종료 후 `log_progress_end()`. (3) `backend/app/services/market_close_pipeline.py` — `refresh_confirmed_5d_data` 직접 루프 (1270 라인) 동일 교체 + 루프 종료 후 `log_progress_end()`. (4) `backend/tests/test_logger.py` — `TestLogProgress` 6개 테스트 추가 (TTY `\r` 출력 + non-TTY `\n` 출력 + 0 total 안전 + `log_progress_end` 활성/비활성 + `_stdout_sink` 인터리브 `\n` 삽입). 효과: 1일봉 다운로드 1,356줄 INFO → 콘솔 1줄 `\r` 갱신 + 파일 0줄 (DEBUG). 시작/완료 1줄 INFO + 실패 per-stock WARNING 유지 (P21 사용자 투명성). 검증: py_compile 3파일 OK + pytest 172 passed (test_logger 40 + test_kiwoom_stock_rest 75 + test_market_close_pipeline 57) + 런타임 기동 OK + TTY/non-TTY/인터리브 콘솔 동작 확인.
- **2026-07-18 (이전)**: 종목 구독 한도 설정 키 이관 Step 3 (문서 갱신 + 계획서 삭제) 완료. 1파일 수정 + 2파일 삭제: (1) `ARCHITECTURE.md` 5.1절 "WS 구독 대상" — "200개 한도" → "설정 가능 한도 `subscribe.max_0b_count` 기본 200" (P10 SSOT, P21 사용자 투명성). (2) `ARCHITECTURE.md` 6.3절 "필터링" — 동일 표현 갱신. (3) `docs/architecture_subscribe_limit_config_design.md` 삭제 (tracked, git rm). (4) `docs/plan_subscribe_limit_config.md` 삭제 (untracked, rm). 검증: `ARCHITECTURE.md` 내 "200개 한도"/"_WS_0B_LIMIT" 잔존 표현 grep 0건 + `subscribe.max_0b_count` 2곳 명시 확인. 종목 구독 한도 설정 키 이관 3단계(백엔드→프론트엔드→문서) 전체 완료.
- **2026-07-18 (이전)**: 시간 설정 탭 라벨/설명 문구 정리 완료. 1파일 수정: `frontend/src/pages/general-settings.ts`. (1) 사전 준비 시간 섹션 — 제목 "장 시작 전 사전 준비 시간" → "사전 준비 시간 설정", 설명 문구 제목 중복 제거("너무 늦으면 실시간 데이터가 누락될 수 있습니다."만 남김). (2) 3개 입력칸 라벨/설명 갱신 — "실시간 항목 초기화"→"실시간 데이터 필드 초기화" + "장 시작 전 필드를 비워 새 데이터를 받을 준비를 합니다", "구독 사전 시작"→"NXT 종목 구독 신청" + "NXT 프리마켓 시작 전 구독을 미리 신청합니다", "정규장 사전 구독"→"KRX 종목 추가 구독" + "KRX 정규장 시작 전 KRX 단독 종목 구독을 추가합니다". (3) 1일봉차트 자동다운로드 단일 항목 섹션 제목 제거 — 행 라벨 "1일봉차트 자동다운로드"만 유지 (P24 단순성, 다중 행 섹션은 제목 유지 예외적 단순화). 검증: `npm run typecheck` + `npm run build` 성공.
- **2026-07-18 (이전)**: 종목 구독 한도 설정 키 이관 Step 2 (프론트엔드) 완료. 2파일 수정: (1) `frontend/src/types/index.ts` — `AppSettings`에 `'subscribe.max_0b_count'?: number` 명시적 타입 추가 (P23 일관성). (2) `frontend/src/pages/general-settings.ts` — `createNumInput` import 추가 + 모듈 상태 변수 `subscribeMaxInput` + 시간 설정 탭 끝(거래소 고정 시간 참고 박스 이후)에 "구독 한도" 섹션 추가 + `syncFromSettings` 값 동기화. UI: "종목 동시 구독 최대 개수" 입력칸 (기본값 200, 범위 1~1000, 10 단위 스핀, 자동 저장, UI clamp + 백엔드 422 이중 방어). 검증: `npm run build` 성공 (tsc 타입체크 + vite 빌드, 타입 오류 0건).
- **2026-07-18 (이전)**: 종목 구독 200개 한도 설정 키 이관 Step 1 (백엔드) 완료. `subscribe.max_0b_count` 설정 키 추가 (기본값 200, 범위 1~1000). 백엔드 4파일 수정: (1) `settings_defaults.py` — 신규 키 기본값 200 추가. (2) `engine_settings.py` — 타입 캐스팅 `int(_v if _v is not None else 200)` 추가 (P20 폴백 금지 패턴 준수). (3) `engine_ws_reg.py:258` — 하드코딩 `_WS_0B_LIMIT = 200` → `int(engine_state.state.integrated_system_settings_cache.get("subscribe.max_0b_count", 200))` 교체 (P10 SSOT, P13 메모리 상주). (4) `settings_store.py` — `apply_settings_updates()` 내 범위 검증 추가 (1~1000 외 ValueError → 422 차단, P20/P22). 테스트 6개 신규 추가: `test_engine_ws.py` 한도 적용 로직 2개 (설정값 반영 + 기본값 200 폴백), `test_settings_store.py` 범위 검증 4개 (0/1001/비정수 거부 + 유효범위 통과). 검증: py_compile OK + 런타임 기동 OK (RuntimeWarning 0건) + 전체 회귀 2935 passed / 0 failed.
- **2026-07-18 (이전)**: 09:00 KRX 구독 중복 요청 제거 + 200개 한도 설정화 설계서 작성. (1) `daily_time_scheduler.py` `_on_krx_market_open()`에 조건부 스킵 추가 — 08:59 사전 구독 성공 시 09:00 구독 스킵, 실패/누락 시 09:00 복구 구독 수행 (P16). (2) `docs/architecture_subscribe_limit_config_design.md` 작성 (379줄) + `docs/plan_subscribe_limit_config.md` 태스크 파일 작성 (374줄) — 3단계 세션 분할 (백엔드 → 프론트엔드 → 문서 갱신).

## 현재 상태 (빌드/테스트 스냅샷)
- **백엔드**: pytest 2850 passed / 0 failed
- **런타임**: `python -W error::RuntimeWarning main.py` 기동 성공, RuntimeWarning 0건
- **프론트엔드**: `npm run build` 성공 (snapshot_history 계열 dead code 제거 반영)
- **문서**: `docs/architecture_audit_tasks.md` (823줄→825줄) — 아키텍처 전수 조사 실행 추적용 태스크 파일. B-21 전체 완료 반영. 다음 세션부터 B-22 착수 가능.

## 다음 세션 진행 대기

### 아키텍처 위반 전수 조사 (다단계 작업 — 태스크 파일 작성 완료, 진행 대기)
- **현재 단계**: B-21 전체 완료 (a/b/c 3서브세션, 총 19건 dead code 제거) → 다음 세션 B-22 착수 가능
- **기준 문서**: `ARCHITECTURE.md` (24개 불변 원칙) + `docs/architecture_audit_plan.md` (30세션 분할 계획 + 과거 해결 이력 42건) + `docs/architecture_audit_tasks.md` (실행 추적용 체크리스트)
- **진행 현황**: 30세션 중 15세션 완료 (B-01~B-21, F-01) + B-15-a 부분 완료 / B-15-b 잔여 + 14세션 잔여
- **다음 세션 추천**: **B-22 (P3 — Web API 계층, 14파일)** — B-21 전체 완료로 Core 유틸 dead code 전부 해결. 조사 체크리스트 + 검증 단계는 태스크 파일 섹션 "세션 B-22" 참조.
- **이후 세션 순서** (태스크 파일 섹션 7): B-15-b → B-16-a/b (분할) → B-17 → B-18-a/b (분할) → B-19 → B-20 → B-21(완료) → B-22 → B-23 → F-02 → F-03 → F-04-a/b (분할) → F-05 → F-06-a/b (분할) → F-07
- **세션 진행 규칙**: 각 세션은 AGENTS.md 규칙 0-1(세션당 1단계) 준수 — 한 세션에서 1세션만 진행 후 검증·커밋·HANDOVER 갱신·사용자 보고 후 종료. 다음 세션은 다음 기회에 이어서.
- **분할 권장 세션**: B-15 (2060줄), B-16 (1732줄), B-18 (3150줄), F-04 (3145줄), F-06 (6803줄) — 각 a/b 서브세션 분할 권장 (태스크 파일에 분할 제안 명시)
- **위반 사항 기록**: 각 세션에서 위반 발견 시 `architecture_audit_plan.md` 섹션 7 "발견된 문제 기록"에 ID 부여 기록 (예: `B10-01`). 심각도(CRITICAL/HIGH/MEDIUM/LOW)·상태(발견/수정중/해결/보류) 분류는 태스크 파일 섹션 5 참조.
- **완료 정의** (태스크 파일 섹션 6 = plan 섹션 9): 30세션 모두 완료 + 모든 CRITICAL/HIGH 해결 + 24개 원칙 위반 0건 + 백엔드 런타임 기동 검증 + 프론트엔드 빌드 + pytest 전체 통과

### 리스크 매니저 확장 (다단계 작업 진행 중)
- **현재 단계**: 1세션 (설계) + 2세션 (태스크 파일) + 3세션 (구현 Step 1 — 백엔드 설정 계층 + 사유코드) 완료 → 4세션 (구현 Step 2 — RiskManager 확장) 승인 대기
- **참조 문서**: `docs/architecture_risk_manager_extension_design.md` (862줄, 설계 완료) + `docs/plan_risk_manager_extension.md` (605줄, 태스크 완료)
- **사전조사 발견 오류 3건** (태스크 파일 0절): (1) `test_risk_manager.py` 이미 존재 — "신규" → "기존 확장", (2) UI 칩 색상 `COLOR.downBg`(파랑) → `COLOR.upBg`(빨강) 수정 제안, (3) `check_sell_order_allowed` async 변환 시 기존 테스트 3개 갱신 필요
- **다음 세션**: 4세션 구현 Step 2 — RiskManager 확장 + 매도 체크 async 변환 + WS 브로드캐스트 (2파일: `risk_manager.py`, `trading.py` 매도 체크 부분 + 테스트 2파일: `test_risk_manager.py` 기존 확장, `test_trading.py` 매도 체크 갱신). 핵심 로직 변경 (규칙 0-4 + 0-5 적용 — 사용자 승인 필수).
- **이후 세션들**: 5세션(프론트엔드 5파일) → 6세션(통합 검증 + 문서 갱신 + 계획서 삭제) — 각 세션당 1단계 원칙 준수

### 일일 손실 한도 개선 (다단계 작업 진행 중 — 3단계)
- **현재 단계**: 1단계(`createMoneyInput` 음수 지원) + 2단계(백엔드 `daily_loss_limit_on` 키 추가) 완료 → 3단계(프론트엔드 토글 + `createMoneyInput` 교체) 대기
- **1단계 완료**: `frontend/src/components/common/setting-row.ts` — `createMoneyInput`에 `min`/`max` 옵션 추가 + `fmtMoney()` 헬퍼로 음수 콤마 포맷. 기존 양수 사용처 영향 없음.
- **2단계 완료**: 백엔드 3파일(`settings_defaults.py`, `engine_settings.py`, `risk_manager.py`) + 프론트엔드 타입 1파일(`types/index.ts`) + 테스트 2파일. `daily_loss_limit_on` 기본값 True로 기존 동작 유지. OFF 시 매수/매도 손실 한도 체크 스킵.
- **3단계 대기 (다음 세션)**: `frontend/src/pages/general-settings.ts` — 일일 손실 한도 행(524-539줄)을 `createToggleLabelControlsRow` + `createMoneyInput`(음수, `min: -1000000000, max: 0`)으로 교체. `dailyLossToggle`/`dailyLossControls` 변수 추가(60줄 근처). 다른 리스크 설정(손실률/수익/수익률/연속손실)과 동일 패턴(P23 일관성).

### 기타 대기 항목
- **다운로드 완료 시간 표시 (제안2)**: 1일봉/5일봉 다운로드 버튼 우측에 최근 다운로드 완료 시간 표시. 백엔드 신규 기능 필요 (저장소 설계 사전조사 후 제안).
- **실전모드 보관 기준** (`RETENTION_TRADING_DAYS_REAL = 90`): 추후 논의.
- **`notify_raw_real_data` dead code (P16)**: 별도 검토 필요 시 사용자 지시.
- **추가 컬럼 너비 조정**: 사용자 UI 확인 후 필요 시 해당 페이지만 override로 진행.
- **`sector_calculator.py:132` 코드 주석-코드 불일치 (P10/P23)**: 2026-07-18 ARCHITECTURE.md 6.2절 불일치 수정 세션 중 발견. 주석 "순위/백분위 기반 점수이므로 불필요" → 실제 2차 가산점은 "가중 순위 합" 방식 (백분위 미사용). 사용자가 "문서만 수정, 코드 변경 없음" 명시하여 코드 주석은 수정 안 함. 사용자 승인 시 별도 세션에서 코드 주석 1줄 갱신 권장.
- **`kiwoom_rest.py` `except Exception as e:` 8곳 `exc_info=True` 누락 (P23)**: 2026-07-21 B-15-b 세션 중 발견. `kiwoom_connector.py`(12/12 보유)와 일관성 위반. B-15-a 세션에서 누락됨 (B15-03/B15-04는 `kiwoom_connector.py`에만 적용). 별도 세션에서 8곳에 `exc_info=True` 추가 권장.
- **`kiwoom_providers.py:75` `KiwoomAccountProvider` 계좌번호 real/legacy 불일치 (P10/P17)**: 2026-07-21 B-15-b 세션 중 발견. `KiwoomAuthProvider.__init__`(line 45)는 `kiwoom_account_no_real` 우선 사용, `KiwoomAccountProvider.__init__`(line 75)는 `kiwoom_account_no`만 사용 → 실전모드에서 AccountProvider가 모의 계좌번호 사용 가능. `engine_settings._pick_account_no`(line 50-55)가 정규화 헬퍼 제공하므로 해당 헬퍼 적용 권장. 별도 조사·세션 필요 (잠재적 실전모드 버그).
- **`auth.py:30` `verify_token` dead code (P16)**: 2026-07-22 B-22-b 세션 중 발견. B-22-b에서 deps.py/ws.py의 주석 처리된 `verify_token` 호출 코드를 제거하면서 `verify_token` 함수 자체의 호출처가 0건이 됨 (auth.py:30 정의만 남음). `create_access_token`/`authenticate_user`는 살아있는 경로 (routes/auth.py에서 호출). 별도 세션에서 `verify_token` 함수 제거 또는 프로덕션 전환 시 재활성화 검토 필요.
- **`sector_data_provider.py:154`·`sector_calculator.py:28/169` `get_merged_sector` 참조 주석 (P23)**: 2026-07-22 B-22-b 세션 중 발견. B-21-c에서 `get_merged_sector` 함수 제거되었으나 3곳의 주석/docstring이 여전히 해당 함수 참조. 별도 세션에서 주석 정정 권장 (Code Removal Rules 준수).

## DB 데이터 특성 (참고)
- `master_stocks_table.name`: 최대 14자, 평균 4.8자, 99% ≤ 9자
- `master_stocks_table.sector`: 최대 13자, 평균 6.8자
- `master_stocks_table.code`: 6자
- `stock_5d_bars.trade_amount`: 최대 33,936,947 (8자리)
- `stock_5d_bars.high_price`: 최대 3,015,000 (7자리)
- `trades.price`: 최대 1,858,500 (7자리)
- `trades.qty`: 최대 532 (3자리)
- `trades.total_amt`: 최대 5,128,949원
- `trades.pnl_rate`: 최대 5.47%

## 참고 사항
- `master_stocks_table`의 `cur_price`/`change`/`change_rate`/`trade_amount`는 현재 스냅샷에서 비어 있어, 수치 기준은 `stock_5d_bars`와 `trades`를 사용.
- `auto-width.ts`의 `KOREAN_SCALE` 조정은 너비 추정 정확도에 큰 영향을 줌. 변경 없이는 `종목명` 9자만 되어도 150px 이상을 요구해 공간 낭비가 큼.
- `sector-ranking-list.ts`는 `createDataTable` 기반으로 전환 완료 (CSS Grid + 가상 스크롤 + rowFooter 진행 바). `profit-overview.ts`는 여전히 `DataTable`이 아니므로 별도 처리 필요 시 별도 세션 진행.
- 컬럼 너비 공통 상수(`COLUMN_WIDTH`)는 min/max px 경계값이며, 실제 비율은 데이터 기반 px→% 정규화로 페이지별 컬럼 구성에 자동 적응함. per-page override는 `ColumnDef`의 `minWidth`/`maxWidth` 필드로 이미 지원.
