---
name: problem-solve
description: 문제 해결 표준 절차 (근본 원인 해결)
allowed-tools:
  - read
  - grep
  - glob
  - edit
  - write
  - exec
---

## 문제 해결 절차

### 1. 사전 조사 (필수)
- 대상 파일의 전체 구조와 의존성 확인
- 영향 받는 모든 파일/함수/변수 식별
- 기존 코드 패턴과의 일관성 확인
- 테스트 커버리지 및 기존 테스트 영향 확인
- 아키텍처 원칙 (SSOT, 살아있는 경로, 폴백 금지) 부합 여부 확인

### 2. 요청 유형 분류
- **보고만 원함**: 조사 후 원인과 해결책 보고, 코드 수정 금지
- **수정까지 원함**: 사용자가 "진행해/수정해"라고 명시적으로 승인한 후에만 수정

### 3. 재현 범위 확인
- 특정 페이지만 문제인가?
- 전체 앱인가?
- 개발 서버 vs 빌드 결과인가?
- 새로고침 후에도 반복되는가?

### 4. 진입점 확인
- 프론트엔드: `frontend/src/main.ts`, `frontend/src/layout/shell.ts`
- route 등록, container 렌더링, props/state 전달, CSS 숨김 여부 확인

### 5. 데이터 흐름 확인
- API 응답 → Store → Component → DOM
- WebSocket → binding → Store → Component → DOM
- 각 단계에서 값이 올바르게 흐르는지 추적

### 6. 원인 후보 분리
- 확인된 실패 지점을 원인으로 좁힘
- 최소 수정으로 해당 경로만 복구
- 주변 코드에 영향을 주지 않도록 주의

### 7. 근본 해결
- 증상을 숨기는 임시방편 금지
- 금지: z-index 무작정 올림, `!important`, `height` 임의 추가, `as any`로 덮기, 새로고침만 안내
- 허용: props/state 전달 경로, CSS 충돌, flex 스크롤 체인, 캐시/서비스워커 여부 확인

### 8. 검증
- 잔여 문자열 검색
- 타입 체크, 빌드, 테스트
- 백엔드 수정 시 런타임 기동 검증 필수
- 프론트엔드 수정 시 `npm run build` + 브라우저 화면 확인

### 9. 보고 (필수 포함)
1. 문제 현상
2. 근본 원인 (파일:줄)
3. 수정 방안
4. 수정 영향 범위
5. 검증 방법
6. 추가로 발견한 문제/개선점

### 10. 후속 작업
- 사용자 승인 후에만 Git 커밋
- 사용자 승인 후에만 `HANDOVER.md` 업데이트

## 금지 표현
"아마도", "그럴 것 같습니다", "일단 해보죠", "추후 개선", "나중에 리팩토링"

## 허용 표현
"파일 A의 n번째 줄에서 확인됨", "검색 결과 해당 클래스가 남아 있음", "빌드 로그에서 타입 오류가 확인됨", "이 부분은 아직 확인 필요"
